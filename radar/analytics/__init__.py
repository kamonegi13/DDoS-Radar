"""Analytics package — derived data structures used by the discovery
pipeline (Tier 3) and downstream UI panels.

Modules:
  cooccurrence  — pairwise country co-occurrence matrix (G.2)
  dbscan_cluster — pure-Python DBSCAN over country-pair distances (G.3a)
"""
