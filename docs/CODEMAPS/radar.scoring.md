# Codemap: `radar/scoring.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 1601
- **Module purpose**: radar.scoring -- Sequence scorer, HOD Z-score, CF helpers, misc scoring functions,

## Top-level constants

- `_HOD_PREFILL_L3_URL`
- `_HOD_PREFILL_L7_URL`
- `_HOD_PREFILL_TS_URL`
- `_HOD_PREFILL_L3_TS_URL`
- `ENTROPY_HISTORY_SIZE`
- `_CF_MIN_INTERVAL`
- `VELOCITY_BONUS_CAP`
- `ACCELERATION_BONUS_MAX`
- `VELOCITY_MIN_OBSERVATIONS`
- `SILENT_DIVERGENCE_BONUS`
- `CONTEXT_ALIGNMENT_BONUS`
- `CONTEXT_ALIGNMENT_THRESHOLD`
- `DERIVE_TL_FORMULA_REF`
- `_LOW_WEIGHT_FLOOR`

## Classes

### `Signal` (L33–66)
_Atomic observation emitted by a sensor — input unit for scoring engine._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L50–66 |  |

### `ScenarioContribution` (L977–1000)
_A single signal's contribution to a scenario via one participant country._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L988–1000 |  |

### `ScenarioState` (L1004–1047)
_Result of computing a scenario's score._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L1016–1047 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `rationale_to_signal` | L69–91 | Convert a legacy RationaleEntry to a Signal for scenario scoring. |
| `resolve_seq_fire_targets` | L94–128 | Decide which countries should receive a sequence event registration. |
| `select_secondary_ec_hits` | L131–157 | Group sensor data rows by secondary effective_core (ADR-009 stage 2). |
| `register_sequence_event` | L160–183 | Register an event in the escalation chain log. |
| `compute_sequence_bonus` | L185–234 | Validate the escalation chain within a 24h window and return bonus score and status string. |
| `_fetch_cf_timeseries_hourly` | L242–263 | Fetch hourly L3+L7 combined attack timeseries for a theater. |
| `_fetch_single_ts` | L266–309 | Fetch a single CF Radar timeseries (L3 or L7) for a theater. |
| `compute_avg_spike_raw` | L312–337 | Compute avg_spike from pre-fetched origin distribution dicts. |
| `prefill_hod_baseline_bg` | L340–459 | Background thread: populate HOD baseline for ALL 24 UTC hour slots using |
| `record_hod_sample` | L462–470 | Record one HOD (Hour-of-Day) sample per UTC hour per theater. |
| `compute_hod_zscore` | L473–497 | Compute Z-score of current_spike against same-UTC-hour historical distribution. |
| `compute_adaptive_zscore` | L501–525 | Compute Z-score using per-sensor/per-theater running statistics (Welford). |
| `compute_origin_entropy` | L528–544 | Compute Shannon entropy of an attack origin distribution. |
| `track_entropy_change` | L553–597 | Track entropy changes with a moving average for anomaly detection. |
| `get_fallback_coord` | L600–602 |  |
| `fetch_cf_data` | L611–626 |  |
| `fetch_cf_data_cached` | L628–657 | Cache CF API calls within the scoring loop. |
| `parse_origins` | L659–668 |  |
| `calculate_overlap` | L670–676 |  |
| `compute_idf_weights` | L679–711 | Build TF-IDF style inverse-document-frequency weights for ASN keys. |
| `calculate_overlap_idf` | L714–736 | IDF-weighted histogram intersection (Phase 4 candidate replacement). |
| `fetch_asn_origins` | L742–761 |  |
| `compute_confidence` | L763–768 |  |
| `classify_direction` | L775–853 | Classify a signal's direction as ADVERSARY_OFFENSIVE, FRIENDLY_DEFENSIVE, |
| `compute_temporal_context` | L856–887 | Compute temporal context features for a sensor signal. |
| `compute_spatial_context` | L890–934 | Compute spatial context: geographic relationship between signal source and target. |
| `compute_target_context` | L937–971 | Compute target context: relationship between signal and monitored target. |
| `_linear_regression_slope` | L1060–1070 | Least-squares slope (shared with engine.py). |
| `compute_scenario_velocity` | L1073–1084 | Compute scenario score velocity (pts/sec) from recent observations. |
| `compute_scenario_acceleration` | L1087–1104 | Compute scenario score acceleration (pts/sec²) from consecutive |
| `compute_velocity_bonus` | L1107–1130 | Compute scoring bonuses from scenario velocity and acceleration. |
| `compute_convergence_bonus_scenario` | L1140–1146 |  |
| `derive_tl` | L1149–1159 |  |
| `_maybe_persist_tl_conclusion` | L1168–1195 | v2.0 shadow-write: persist the focused TL as a Conclusion row. |
| `_maybe_persist_per_domain_conclusion` | L1198–1219 | v2.0 shadow-write: persist the per-domain (cyber/physical/info) row. |
| `_maybe_persist_trend_conclusion` | L1222–1245 | v2.0 shadow-write: derive the 3-window trend from THREAT_LEVEL ledger. |
| `_maybe_persist_attack_mode_conclusion` | L1248–1273 | v2.0 shadow-write: persist the rule-based ATTACK_MODE classification. |
| `_maybe_persist_anomaly_conclusions` | L1276–1300 | v2.0 shadow-write: persist top-N ANOMALY rows for this scoring tick. |
| `apply_hysteresis_to_tl` | L1303–1314 | Scenario TL hysteresis: de-escalation (higher TL#) is limited to one |
| `dedup_by_source_country_max` | L1317–1325 |  |
| `compute_scenario_score` | L1328–1419 |  |
| `_maybe_sample_v1_v2_diff` | L1422–1436 | v2.0 Phase 1 priority 6: rollout monitoring shadow-write. |
| `compute_weight_utilization` | L1451–1532 | Per-participant weight utilization vs configured share. |
| `compute_weight_utilization_timeseries` | L1535–1601 | Time-bucketed observed_share / utilization_pct per participant. |

## Signatures

```python
# L50  (Signal)
def to_dict(self) -> dict
# L988  (ScenarioContribution)
def to_dict(self) -> dict
# L1016  (ScenarioState)
def to_dict(self) -> dict
# L69
def rationale_to_signal(rat, source_country: str, observed_at: float, evidence_url: str | None, llm_reasoning: str | None) -> Optional[Signal]
# L94
def resolve_seq_fire_targets(core_theater: str | None, effective_cores: list, scenario_wide: bool) -> list[str]
# L131
def select_secondary_ec_hits(effective_cores: list, primary_ec: str, data: list, country_field: str) -> dict
# L160
def register_sequence_event(theater: str, event_type: str, meta: dict, dedup_window: int, scenario_id: str | None)
# L185
def compute_sequence_bonus(theater: str) -> tuple
# L242
def _fetch_cf_timeseries_hourly(theater: str, days: int) -> dict
# L266
def _fetch_single_ts(url: str, base_params: dict, theater: str) -> dict
# L312
def compute_avg_spike_raw(o_l3: dict, o_l7: dict, b_data: dict, adversary_codes: list) -> float
# L340
def prefill_hod_baseline_bg(theaters: list, adversary_codes: list) -> None
# L462
def record_hod_sample(theater: str, ts: float, avg_spike: float) -> None
# L473
def compute_hod_zscore(theater: str, current_spike: float, current_ts: float) -> tuple
# L501
def compute_adaptive_zscore(sensor_name: str, theater: str, current_value: float, fallback_threshold: float) -> tuple
# L528
def compute_origin_entropy(distribution: dict) -> float
# L553
def track_entropy_change(theater: str, current_entropy: float) -> dict
# L600
def get_fallback_coord(code: str) -> dict
# L611
def fetch_cf_data(url: str, params: dict) -> list
# L628
def fetch_cf_data_cached(url: str, params: dict, ttl: float) -> list
# L659
def parse_origins(origins_list: list) -> dict
# L670
def calculate_overlap(dist1: dict, dist2: dict) -> float
# L679
def compute_idf_weights(distributions: dict) -> dict
# L714
def calculate_overlap_idf(dist1: dict, dist2: dict, idf_weights: dict) -> float
# L742
def fetch_asn_origins(target_code: str) -> dict
# L763
def compute_confidence(spike_factor: float, code: str, is_new_actor: bool, is_state_asn: bool) -> str
# L775
def classify_direction(sensor_name: str, domain: str, source_country: str, target_country: str, adversary_states: list, strategic_theaters: list, **kwargs) -> tuple[str, float]
# L856
def compute_temporal_context(sensor_name: str, current_ts: float, theater: str) -> dict
# L890
def compute_spatial_context(sensor_name: str, theater: str, source_country: str, adversary_states: list) -> dict
# L937
def compute_target_context(sensor_name: str, theater: str, source_country: str, adversary_states: list, strategic_theaters: list, core_country: str) -> dict
# L1060
def _linear_regression_slope(xs: list[float], ys: list[float]) -> float
# L1073
def compute_scenario_velocity(score_series: list[tuple[float, float]]) -> float
# L1087
def compute_scenario_acceleration(score_series: list[tuple[float, float]]) -> float
# L1107
def compute_velocity_bonus(velocity: float, acceleration: float) -> tuple[float, float]
# L1140
def compute_convergence_bonus_scenario(active_domains: list[str]) -> float
# L1149
def derive_tl(total_score: float, active_domains: list[str], physical_score: float) -> int
# L1168
def _maybe_persist_tl_conclusion(state: 'ScenarioState') -> None
# L1198
def _maybe_persist_per_domain_conclusion(state: 'ScenarioState') -> None
# L1222
def _maybe_persist_trend_conclusion(state: 'ScenarioState') -> None
# L1248
def _maybe_persist_attack_mode_conclusion(state: 'ScenarioState') -> None
# L1276
def _maybe_persist_anomaly_conclusions(state: 'ScenarioState') -> None
# L1303
def apply_hysteresis_to_tl(new_tl: Optional[int], prev_tl: Optional[int]) -> tuple[Optional[int], bool]
# L1317
def dedup_by_source_country_max(contributions: list[ScenarioContribution]) -> list[ScenarioContribution]
# L1328
def compute_scenario_score(scenario, all_signals: list[Signal], is_focused: bool, global_signal_weight: float, domain_cap: float) -> ScenarioState
# L1422
def _maybe_sample_v1_v2_diff(state: 'ScenarioState') -> None
# L1451
def compute_weight_utilization(scenario, contributions: list[ScenarioContribution], divergence_threshold_pct: float) -> list[dict]
# L1535
def compute_weight_utilization_timeseries(scenario, rows: list[tuple[float, str, float]], hours: int, bucket_count: int) -> dict
```
