# Codemap: `radar/scoring.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 1535
- **Module purpose**: radar.scoring -- Sequence scorer, HOD Z-score, CF helpers, misc scoring functions,

## Top-level constants

- `_HOD_PREFILL_L3_URL`
- `_HOD_PREFILL_L7_URL`
- `_HOD_PREFILL_TS_URL`
- `_HOD_PREFILL_L3_TS_URL`
- `ENTROPY_HISTORY_SIZE`
- `_CF_MIN_INTERVAL`
- `VELOCITY_BONUS_CAP`
- `ACCELERATION_BONUS_MAX`
- `VELOCITY_MIN_OBSERVATIONS`
- `SILENT_DIVERGENCE_BONUS`
- `CONTEXT_ALIGNMENT_BONUS`
- `CONTEXT_ALIGNMENT_THRESHOLD`
- `DERIVE_TL_FORMULA_REF`
- `_LOW_WEIGHT_FLOOR`

## Classes

### `Signal` (L32–65)
_Atomic observation emitted by a sensor — input unit for scoring engine._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L49–65 |  |

### `ScenarioContribution` (L915–938)
_A single signal's contribution to a scenario via one participant country._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L926–938 |  |

### `ScenarioState` (L942–985)
_Result of computing a scenario's score._

| Method | Lines | Summary |
|--------|-------|---------|
| `to_dict` | L954–985 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `rationale_to_signal` | L68–90 | Convert a legacy RationaleEntry to a Signal for scenario scoring. |
| `resolve_seq_fire_targets` | L93–127 | Decide which countries should receive a sequence event registration. |
| `select_secondary_ec_hits` | L130–156 | Group sensor data rows by secondary effective_core (ADR-009 stage 2). |
| `register_sequence_event` | L159–182 | Register an event in the escalation chain log. |
| `compute_sequence_bonus` | L184–233 | Validate the escalation chain within a 24h window and return bonus score and status string. |
| `_fetch_cf_timeseries_hourly` | L241–262 | Fetch hourly L3+L7 combined attack timeseries for a theater. |
| `_fetch_single_ts` | L265–308 | Fetch a single CF Radar timeseries (L3 or L7) for a theater. |
| `compute_avg_spike_raw` | L311–336 | Compute avg_spike from pre-fetched origin distribution dicts. |
| `prefill_hod_baseline_bg` | L339–458 | Background thread: populate HOD baseline for ALL 24 UTC hour slots using |
| `record_hod_sample` | L461–469 | Record one HOD (Hour-of-Day) sample per UTC hour per theater. |
| `compute_hod_zscore` | L472–496 | Compute Z-score of current_spike against same-UTC-hour historical distribution. |
| `compute_adaptive_zscore` | L500–524 | Compute Z-score using per-sensor/per-theater running statistics (Welford). |
| `compute_origin_entropy` | L527–543 | Compute Shannon entropy of an attack origin distribution. |
| `track_entropy_change` | L552–596 | Track entropy changes with a moving average for anomaly detection. |
| `get_fallback_coord` | L599–601 |  |
| `fetch_cf_data` | L610–625 |  |
| `fetch_cf_data_cached` | L627–656 | Cache CF API calls within the scoring loop. |
| `parse_origins` | L658–667 |  |
| `calculate_overlap` | L669–675 |  |
| `fetch_asn_origins` | L680–699 |  |
| `compute_confidence` | L701–706 |  |
| `classify_direction` | L713–791 | Classify a signal's direction as ADVERSARY_OFFENSIVE, FRIENDLY_DEFENSIVE, |
| `compute_temporal_context` | L794–825 | Compute temporal context features for a sensor signal. |
| `compute_spatial_context` | L828–872 | Compute spatial context: geographic relationship between signal source and target. |
| `compute_target_context` | L875–909 | Compute target context: relationship between signal and monitored target. |
| `_linear_regression_slope` | L998–1008 | Least-squares slope (shared with engine.py). |
| `compute_scenario_velocity` | L1011–1022 | Compute scenario score velocity (pts/sec) from recent observations. |
| `compute_scenario_acceleration` | L1025–1042 | Compute scenario score acceleration (pts/sec²) from consecutive |
| `compute_velocity_bonus` | L1045–1068 | Compute scoring bonuses from scenario velocity and acceleration. |
| `compute_convergence_bonus_scenario` | L1078–1084 |  |
| `derive_tl` | L1087–1097 |  |
| `_maybe_persist_tl_conclusion` | L1106–1133 | v2.0 shadow-write: persist the focused TL as a Conclusion row. |
| `_maybe_persist_per_domain_conclusion` | L1136–1157 | v2.0 shadow-write: persist the per-domain (cyber/physical/info) row. |
| `_maybe_persist_trend_conclusion` | L1160–1183 | v2.0 shadow-write: derive the 3-window trend from THREAT_LEVEL ledger. |
| `_maybe_persist_attack_mode_conclusion` | L1186–1207 | v2.0 shadow-write: persist the rule-based ATTACK_MODE classification. |
| `_maybe_persist_anomaly_conclusions` | L1210–1234 | v2.0 shadow-write: persist top-N ANOMALY rows for this scoring tick. |
| `apply_hysteresis_to_tl` | L1237–1248 | Scenario TL hysteresis: de-escalation (higher TL#) is limited to one |
| `dedup_by_source_country_max` | L1251–1259 |  |
| `compute_scenario_score` | L1262–1353 |  |
| `_maybe_sample_v1_v2_diff` | L1356–1370 | v2.0 Phase 1 priority 6: rollout monitoring shadow-write. |
| `compute_weight_utilization` | L1385–1466 | Per-participant weight utilization vs configured share. |
| `compute_weight_utilization_timeseries` | L1469–1535 | Time-bucketed observed_share / utilization_pct per participant. |

## Signatures

```python
# L49  (Signal)
def to_dict(self) -> dict
# L926  (ScenarioContribution)
def to_dict(self) -> dict
# L954  (ScenarioState)
def to_dict(self) -> dict
# L68
def rationale_to_signal(rat, source_country: str, observed_at: float, evidence_url: str | None, llm_reasoning: str | None) -> Optional[Signal]
# L93
def resolve_seq_fire_targets(core_theater: str | None, effective_cores: list, scenario_wide: bool) -> list[str]
# L130
def select_secondary_ec_hits(effective_cores: list, primary_ec: str, data: list, country_field: str) -> dict
# L159
def register_sequence_event(theater: str, event_type: str, meta: dict, dedup_window: int, scenario_id: str | None)
# L184
def compute_sequence_bonus(theater: str) -> tuple
# L241
def _fetch_cf_timeseries_hourly(theater: str, days: int) -> dict
# L265
def _fetch_single_ts(url: str, base_params: dict, theater: str) -> dict
# L311
def compute_avg_spike_raw(o_l3: dict, o_l7: dict, b_data: dict, adversary_codes: list) -> float
# L339
def prefill_hod_baseline_bg(theaters: list, adversary_codes: list) -> None
# L461
def record_hod_sample(theater: str, ts: float, avg_spike: float) -> None
# L472
def compute_hod_zscore(theater: str, current_spike: float, current_ts: float) -> tuple
# L500
def compute_adaptive_zscore(sensor_name: str, theater: str, current_value: float, fallback_threshold: float) -> tuple
# L527
def compute_origin_entropy(distribution: dict) -> float
# L552
def track_entropy_change(theater: str, current_entropy: float) -> dict
# L599
def get_fallback_coord(code: str) -> dict
# L610
def fetch_cf_data(url: str, params: dict) -> list
# L627
def fetch_cf_data_cached(url: str, params: dict, ttl: float) -> list
# L658
def parse_origins(origins_list: list) -> dict
# L669
def calculate_overlap(dist1: dict, dist2: dict) -> float
# L680
def fetch_asn_origins(target_code: str) -> dict
# L701
def compute_confidence(spike_factor: float, code: str, is_new_actor: bool, is_state_asn: bool) -> str
# L713
def classify_direction(sensor_name: str, domain: str, source_country: str, target_country: str, adversary_states: list, strategic_theaters: list, **kwargs) -> tuple[str, float]
# L794
def compute_temporal_context(sensor_name: str, current_ts: float, theater: str) -> dict
# L828
def compute_spatial_context(sensor_name: str, theater: str, source_country: str, adversary_states: list) -> dict
# L875
def compute_target_context(sensor_name: str, theater: str, source_country: str, adversary_states: list, strategic_theaters: list, core_country: str) -> dict
# L998
def _linear_regression_slope(xs: list[float], ys: list[float]) -> float
# L1011
def compute_scenario_velocity(score_series: list[tuple[float, float]]) -> float
# L1025
def compute_scenario_acceleration(score_series: list[tuple[float, float]]) -> float
# L1045
def compute_velocity_bonus(velocity: float, acceleration: float) -> tuple[float, float]
# L1078
def compute_convergence_bonus_scenario(active_domains: list[str]) -> float
# L1087
def derive_tl(total_score: float, active_domains: list[str], physical_score: float) -> int
# L1106
def _maybe_persist_tl_conclusion(state: 'ScenarioState') -> None
# L1136
def _maybe_persist_per_domain_conclusion(state: 'ScenarioState') -> None
# L1160
def _maybe_persist_trend_conclusion(state: 'ScenarioState') -> None
# L1186
def _maybe_persist_attack_mode_conclusion(state: 'ScenarioState') -> None
# L1210
def _maybe_persist_anomaly_conclusions(state: 'ScenarioState') -> None
# L1237
def apply_hysteresis_to_tl(new_tl: Optional[int], prev_tl: Optional[int]) -> tuple[Optional[int], bool]
# L1251
def dedup_by_source_country_max(contributions: list[ScenarioContribution]) -> list[ScenarioContribution]
# L1262
def compute_scenario_score(scenario, all_signals: list[Signal], is_focused: bool, global_signal_weight: float, domain_cap: float) -> ScenarioState
# L1356
def _maybe_sample_v1_v2_diff(state: 'ScenarioState') -> None
# L1385
def compute_weight_utilization(scenario, contributions: list[ScenarioContribution], divergence_threshold_pct: float) -> list[dict]
# L1469
def compute_weight_utilization_timeseries(scenario, rows: list[tuple[float, str, float]], hours: int, bucket_count: int) -> dict
```
