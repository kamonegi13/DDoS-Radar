# Codemap: `radar/conclusions/attack_mode_extensions.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 227
- **Module purpose**: Scenario-specific attack mode extensions (ADR-V2-002, Phase 2.5 hook).

## Top-level constants

- `_EXT_CONFIDENCE_FLOOR`
- `_EXT_CONFIDENCE_CEIL`
- `_EXT_BASE_DEFAULT`
- `_RESERVED_MODES`

## Classes

### `ExtensionMatch` (L77–82)
_One scenario_extensions rule that fired against the current state._

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_coerce_floors` | L85–97 | Coerce JSON dict → dict[str, float]; reject silently on bad shape. |
| `_coerce_str_list` | L100–103 |  |
| `_load_scenario_extensions` | L106–122 | Read `attack_mode_extensions` from the scenario preset. |
| `_evaluate_one` | L125–188 | Evaluate a single extension rule against domain scores + active set. |
| `evaluate_extensions` | L191–227 | Evaluate every extension declared on the scenario against the state. |

## Signatures

```python
# L85
def _coerce_floors(raw: Any) -> dict[str, float]
# L100
def _coerce_str_list(raw: Any) -> tuple[str, ...]
# L106
def _load_scenario_extensions(scenario_id: str) -> list[dict[str, Any]]
# L125
def _evaluate_one(ext: dict[str, Any], *, cyber: float, physical: float, info: float, active_countries: Iterable[str]) -> Optional[ExtensionMatch]
# L191
def evaluate_extensions(state: 'ScenarioState') -> list[ExtensionMatch]
```
