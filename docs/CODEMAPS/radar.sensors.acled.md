# Codemap: `radar/sensors/acled.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 199
- **Module purpose**: ACLED (Armed Conflict Location & Event Data) client.

## Top-level constants

- `_ACLED_BASE`
- `_ACLED_TIMEOUT_SEC`
- `_ISO2_TO_ACLED_COUNTRY`

## Classes

### `ACLEDEvent` (L61–76)
_One ACLED political-violence event, normalized for ETL consumption._

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_country_iso2_from_acled` | L79–86 | Reverse ``_ISO2_TO_ACLED_COUNTRY`` so we can tag inbound rows. |
| `_parse_event_at` | L89–100 | ACLED returns ``event_date`` as YYYY-MM-DD. Time-of-day is rarely |
| `fetch_events` | L103–199 | Pull ACLED events for the given ISO-2 countries in [since, until]. |

## Signatures

```python
# L79
def _country_iso2_from_acled(name: str) -> Optional[str]
# L89
def _parse_event_at(raw_date: str, raw_time: Optional[str]) -> Optional[float]
# L103
def fetch_events(countries: list[str], *, since_epoch: float, until_epoch: Optional[float], limit: int, api_key: Optional[str], api_email: Optional[str], session: Optional[requests.Session]) -> list[ACLEDEvent]
```
