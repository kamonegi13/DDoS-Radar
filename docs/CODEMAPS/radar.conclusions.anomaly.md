# Codemap: `radar/conclusions/anomaly.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 230
- **Module purpose**: Anomaly conclusion deriver — emits one Conclusion(ANOMALY) per

## Top-level constants

- `FORMULA_REF`
- `THRESHOLD_REF`
- `DEFAULT_LIMIT`
- `_RECENCY_TIME_CONSTANT_HOURS`
- `_NOVELTY_WINDOW`
- `_NOVELTY_FLOOR`
- `_NOVELTY_LOOKBACK_SEC`
- `_MAX_IMPORTANCE`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_recency_decay` | L73–75 |  |
| `_novelty_factor` | L78–80 |  |
| `_scenario_relevance` | L83–93 | Per-country: llm_country_weight × participant_weight. |
| `_state_summary` | L96–100 |  |
| `_unavailable` | L103–123 |  |
| `_ledger_similar_count` | L126–151 | Count prior ANOMALY conclusions for (scenario, signal_source) in the |
| `derive_anomaly` | L154–230 | Build the list of Conclusion(ANOMALY) rows for this scenario tick. |

## Signatures

```python
# L73
def _recency_decay(observed_at: float, now: float) -> float
# L78
def _novelty_factor(similar_count: int) -> float
# L83
def _scenario_relevance(contribution: 'ScenarioContribution') -> float
# L96
def _state_summary(contribution: 'ScenarioContribution') -> str
# L103
def _unavailable(db: 'RadarDB', state: 'ScenarioState', now: float) -> Conclusion
# L126
def _ledger_similar_count(db: 'RadarDB', scenario_id: str, signal_source: str, now: float) -> tuple[int, str]
# L154
def derive_anomaly(db: 'RadarDB', state: 'ScenarioState', *, now: Optional[float], limit: int) -> List[Conclusion]
```
