# Codemap: `radar/conclusions/per_domain.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 173
- **Module purpose**: Per-domain conclusion deriver — ADR-V2-004 (5 states per cyber/physical/info).

## Top-level constants

- `FORMULA_REF`
- `DOMAINS`
- `ACTIVE_FLOOR`
- `ELEVATED_FLOOR`
- `DEGRADE_DELTA`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `derive_per_domain` | L56–133 | Build the per-domain Conclusion from the in-flight ScenarioState. |
| `_classify` | L136–145 |  |
| `_serialize` | L148–149 |  |
| `_extract_prior_scores` | L152–162 |  |
| `_confidence` | L165–173 | 3 active domains → high confidence; 1 active + 2 silent → low. |

## Signatures

```python
# L56
def derive_per_domain(db: 'RadarDB', state: 'ScenarioState', *, now: Optional[float]) -> Conclusion
# L136
def _classify(domain: str, cur: float, prior: Optional[float]) -> str
# L148
def _serialize(domain_states: dict[str, str]) -> str
# L152
def _extract_prior_scores(prior: Optional[Conclusion]) -> dict[str, float]
# L165
def _confidence(domain_scores: dict[str, float], available: list[str]) -> float
```
