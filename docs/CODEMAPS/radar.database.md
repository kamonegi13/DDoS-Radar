# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 5995
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1087–1175)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1100–1103 |  |
| `execute` | L1105–1116 |  |
| `executemany` | L1118–1128 |  |
| `executescript` | L1130–1140 |  |
| `commit` | L1142–1148 |  |
| `rollback` | L1150–1156 |  |
| `close` | L1158–1162 |  |
| `writing` | L1165–1172 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1174–1175 |  |

### `RadarDB` (L1178–5979)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1187–1197 |  |
| `_check_integrity_or_recreate` | L1199–1229 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1231–1253 |  |
| `_ensure_schema` | L1255–1261 |  |
| `_post_baseline_indexes` | L1263–1307 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1309–1316 |  |
| `set_schema_version` | L1318–1341 |  |
| `_run_migrations` | L2345–2380 | Apply any pending schema migrations in order. |
| `baseline_get` | L2383–2392 |  |
| `baseline_set` | L2394–2401 |  |
| `baseline_all` | L2403–2412 |  |
| `baseline_len` | L2414–2416 |  |
| `airspace_get` | L2419–2424 |  |
| `airspace_set` | L2426–2433 |  |
| `airspace_all` | L2435–2439 |  |
| `airspace_len` | L2441–2443 |  |
| `hod_record` | L2446–2464 |  |
| `hod_record_many` | L2466–2484 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2486–2497 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L2499–2506 |  |
| `hod_all_entries` | L2508–2518 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L2520–2527 |  |
| `hod_distinct_hours` | L2529–2536 |  |
| `hod_total_points` | L2538–2542 |  |
| `hod_total_points_theater` | L2544–2550 |  |
| `gdelt_dow_record` | L2553–2567 |  |
| `gdelt_dow_last_bucket` | L2569–2574 |  |
| `gdelt_dow_same_weekday` | L2576–2583 |  |
| `gdelt_dow_total_points` | L2585–2587 |  |
| `ts_append` | L2590–2602 |  |
| `ts_get` | L2604–2609 |  |
| `ts_get_since` | L2611–2616 |  |
| `ts_distinct_theaters` | L2618–2622 |  |
| `ts_total_points` | L2624–2626 |  |
| `sensor_obs_record` | L2632–2649 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L2651–2660 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L2663–2676 |  |
| `series_get` | L2678–2683 |  |
| `alert_append` | L2686–2698 |  |
| `alert_list` | L2700–2705 |  |
| `alert_count` | L2707–2709 |  |
| `alert_clear` | L2711–2714 |  |
| `seq_append` | L2719–2733 |  |
| `seq_exists_since` | L2735–2741 |  |
| `seq_events_since` | L2743–2749 |  |
| `seq_all_events` | L2751–2757 |  |
| `seq_cleanup` | L2759–2762 |  |
| `seq_total` | L2764–2766 |  |
| `seq_distinct_theaters` | L2768–2772 |  |
| `seq_clear` | L2774–2777 |  |
| `tl_observation_append` | L2780–2795 |  |
| `scenario_history_start` | L2797–2802 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L2805–2855 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L2857–2873 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L2875–2891 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L2893–2965 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L2968–3000 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3002–3041 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L3044–3058 |  |
| `focus_switch_stats` | L3060–3088 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3090–3153 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L3156–3173 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L3175–3196 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L3198–3215 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L3217–3235 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3237–3321 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3324–3354 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3356–3444 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3446–3465 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3467–3476 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3478–3502 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L3504–3523 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L3525–3535 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L3537–3547 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L3549–3561 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L3563–3585 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L3587–3612 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L3614–3663 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L3667–3678 |  |
| `scenario_list` | L3680–3692 |  |
| `scenario_upsert` | L3694–3728 |  |
| `scenario_update_state` | L3738–3764 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L3766–3782 |  |
| `scenario_delete` | L3784–3786 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L3788–3814 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L3816–3826 |  |
| `scenario_change_log` | L3828–3833 |  |
| `threat_append` | L3845–3852 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L3854–3883 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L3885–3894 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L3896–3916 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L3918–3922 |  |
| `threat_last_scoped` | L3924–3930 |  |
| `threat_clear` | L3932–3935 |  |
| `zscore_stats_get` | L3938–3954 |  |
| `zscore_stats_update` | L3956–3988 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L3990–4004 |  |
| `fetch_log_append` | L4007–4017 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4019–4046 | Per-sensor reliability aggregates over the given time window. |
| `llm_call_log_append` | L4049–4068 | Persist a single LLM call attempt and its downstream queue verdict. |
| `auto_judge_decision_log` | L4070–4096 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4098–4108 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4110–4128 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4130–4153 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4155–4251 | Aggregate stats per caller and overall verdict counts. |
| `ct_log_known_cas` | L4254–4268 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L4270–4291 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L4293–4307 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L4309–4319 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L4321–4346 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L4348–4370 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L4373–4379 |  |
| `sensor_cache_get` | L4381–4388 |  |
| `sensor_cache_all` | L4390–4397 |  |
| `sensor_cache_count` | L4399–4401 |  |
| `noise_excl_add` | L4404–4415 |  |
| `noise_excl_remove` | L4417–4421 |  |
| `noise_excl_list` | L4423–4440 |  |
| `noise_excl_match` | L4442–4449 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L4452–4463 |  |
| `confirmed_threat_list` | L4465–4481 |  |
| `daily_summary_upsert` | L4484–4499 |  |
| `daily_summary_get` | L4501–4514 |  |
| `forecast_log_add` | L4517–4526 |  |
| `forecast_log_resolve` | L4528–4535 |  |
| `forecast_log_get` | L4537–4553 |  |
| `forecast_accuracy_summary` | L4555–4577 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L4580–4621 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L4623–4638 |  |
| `startup_cleanup` | L4641–4644 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L4646–4663 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L4665–4673 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L4675–4679 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L4681–4893 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L4895–4918 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L4921–4942 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L4944–4959 | Load climate events since a given timestamp. |
| `climate_events_prune` | L4961–4965 | Remove climate events older than given timestamp. |
| `intel_upsert` | L4968–4990 | Insert or replace an LLM intel item. |
| `intel_update_status` | L4992–5001 |  |
| `intel_get` | L5003–5011 |  |
| `intel_list` | L5013–5039 |  |
| `intel_find_by_raw_url` | L5041–5052 | Find intel items with matching raw_url across all source_types. |
| `intel_status_counts` | L5054–5081 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L5083–5106 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L5108–5118 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L5120–5131 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L5133–5148 |  |
| `intel_source_get` | L5150–5160 |  |
| `intel_source_upsert` | L5162–5171 |  |
| `intel_source_update_credibility` | L5173–5182 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L5184–5201 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L5203–5210 |  |
| `intel_source_reset_credibility` | L5212–5229 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L5231–5296 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L5298–5343 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L5347–5349 |  |
| `user_get` | L5351–5357 |  |
| `user_get_role` | L5359–5363 |  |
| `user_create` | L5365–5375 |  |
| `user_exists` | L5377–5381 |  |
| `user_update_last_login` | L5383–5386 |  |
| `user_update_role` | L5388–5391 |  |
| `user_update_password` | L5393–5400 |  |
| `user_delete` | L5402–5406 |  |
| `user_list` | L5408–5412 |  |
| `user_settings_get` | L5414–5420 |  |
| `user_settings_create` | L5422–5431 |  |
| `user_settings_update` | L5449–5465 |  |
| `token_revoke` | L5467–5473 |  |
| `token_is_revoked` | L5475–5479 |  |
| `user_get_invalidate_ts` | L5481–5489 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L5492–5506 |  |
| `hidden_signal_list` | L5508–5537 |  |
| `coverage_upsert` | L5540–5559 |  |
| `coverage_list` | L5561–5569 |  |
| `disconf_add` | L5572–5585 |  |
| `disconf_list` | L5587–5595 |  |
| `disconf_retract` | L5597–5605 |  |
| `ach_matrix_create` | L5608–5617 |  |
| `ach_matrix_list` | L5619–5626 |  |
| `ach_matrix_get` | L5628–5658 |  |
| `ach_hypothesis_add` | L5660–5671 |  |
| `ach_evidence_add` | L5673–5688 |  |
| `ach_score_set` | L5690–5704 |  |
| `dissent_add` | L5707–5717 |  |
| `dissent_list` | L5719–5727 |  |
| `dissent_resolve` | L5729–5738 |  |
| `assumption_add` | L5741–5761 |  |
| `_assumption_log` | L5763–5775 |  |
| `assumption_list` | L5777–5787 |  |
| `assumption_get` | L5789–5793 |  |
| `assumption_update` | L5795–5819 |  |
| `assumption_lock` | L5821–5844 |  |
| `assumption_invalidate` | L5846–5859 |  |
| `assumption_change_log` | L5861–5880 |  |
| `premortem_add` | L5883–5898 |  |
| `premortem_list` | L5900–5909 |  |
| `premortem_resolve` | L5911–5919 |  |
| `decision_log` | L5922–5937 |  |
| `decision_list` | L5939–5972 |  |
| `close` | L5975–5979 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L49–90 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v37_decisions_revocation` | L93–156 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L159–200 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L203–215 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L218–281 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L284–311 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1100  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1105  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1118  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1130  (_CooperativeConn)
def executescript(self, sql: str)
# L1142  (_CooperativeConn)
def commit(self)
# L1150  (_CooperativeConn)
def rollback(self)
# L1158  (_CooperativeConn)
def close(self)
# L1165  (_CooperativeConn)
def writing(self)
# L1174  (_CooperativeConn)
def __getattr__(self, name)
# L1187  (RadarDB)
def __init__(self, db_path: str)
# L1199  (RadarDB)
def _check_integrity_or_recreate(self)
# L1231  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1255  (RadarDB)
def _ensure_schema(self)
# L1263  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1309  (RadarDB)
def schema_version(self) -> int
# L1318  (RadarDB)
def set_schema_version(self, ver: int)
# L2345  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2383  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2394  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2403  (RadarDB)
def baseline_all(self) -> dict
# L2414  (RadarDB)
def baseline_len(self) -> int
# L2419  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2426  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2435  (RadarDB)
def airspace_all(self) -> dict
# L2441  (RadarDB)
def airspace_len(self) -> int
# L2446  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2466  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2486  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L2499  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L2508  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L2520  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L2529  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L2538  (RadarDB)
def hod_total_points(self, table: str) -> int
# L2544  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L2553  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L2569  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L2576  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L2585  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L2590  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L2604  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L2611  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L2618  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L2624  (RadarDB)
def ts_total_points(self) -> int
# L2632  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L2651  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L2663  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L2678  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L2686  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L2700  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L2707  (RadarDB)
def alert_count(self) -> int
# L2711  (RadarDB)
def alert_clear(self)
# L2719  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L2735  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L2743  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L2751  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L2759  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L2764  (RadarDB)
def seq_total(self) -> int
# L2768  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L2774  (RadarDB)
def seq_clear(self)
# L2780  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L2797  (RadarDB)
def scenario_history_start(self) -> float | None
# L2805  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L2857  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L2875  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L2893  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L2968  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3002  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L3044  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L3060  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3090  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L3156  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L3175  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L3198  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L3217  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3237  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3324  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3356  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3446  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3467  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3478  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L3504  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L3525  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L3537  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L3549  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L3563  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L3587  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L3614  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L3667  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L3680  (RadarDB)
def scenario_list(self) -> list[dict]
# L3694  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L3738  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L3766  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L3784  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3788  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3816  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L3828  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L3845  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L3854  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L3885  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L3896  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L3918  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L3924  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L3932  (RadarDB)
def threat_clear(self)
# L3938  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L3956  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L3990  (RadarDB)
def zscore_stats_all(self) -> list
# L4007  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4019  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4049  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str)
# L4070  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4098  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4110  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4130  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4155  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4254  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L4270  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L4293  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L4309  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L4321  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L4348  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L4373  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L4381  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L4390  (RadarDB)
def sensor_cache_all(self) -> dict
# L4399  (RadarDB)
def sensor_cache_count(self) -> int
# L4404  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L4417  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L4423  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L4442  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L4452  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L4465  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L4484  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L4501  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L4517  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L4528  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L4537  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L4555  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L4580  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L4623  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L4641  (RadarDB)
def startup_cleanup(self)
# L4646  (RadarDB)
def shutdown(self)
# L4665  (RadarDB)
def wal_checkpoint(self)
# L4675  (RadarDB)
def periodic_cleanup(self)
# L4681  (RadarDB)
def _prune_stale_rows(self) -> dict
# L4895  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L4921  (RadarDB)
def climate_events_save(self, events: list[dict])
# L4944  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L4961  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L4968  (RadarDB)
def intel_upsert(self, item: dict)
# L4992  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L5003  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L5013  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L5041  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L5054  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L5083  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L5108  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L5120  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L5133  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L5150  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L5162  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L5173  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L5184  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L5203  (RadarDB)
def intel_source_list(self) -> list[dict]
# L5212  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L5231  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L5298  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L5347  (RadarDB)
def user_count(self) -> int
# L5351  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L5359  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L5365  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L5377  (RadarDB)
def user_exists(self, username: str) -> bool
# L5383  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L5388  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L5393  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L5402  (RadarDB)
def user_delete(self, user_id: int)
# L5408  (RadarDB)
def user_list(self) -> list[dict]
# L5414  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L5422  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L5449  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L5467  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L5475  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L5481  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L5492  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L5508  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L5540  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L5561  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L5572  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L5587  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L5597  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L5608  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L5619  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L5628  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L5660  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L5673  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L5690  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L5707  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L5719  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5729  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L5741  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L5763  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L5777  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L5789  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L5795  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L5821  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L5846  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L5861  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L5883  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L5900  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5911  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L5922  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L5939  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L5975  (RadarDB)
def close(self)
# L49
def _migration_v40_schema_version_singleton(conn) -> None
# L93
def _migration_v37_decisions_revocation(conn) -> None
# L159
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L203
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L218
def _migration_v36_bg_observer(conn) -> None
# L284
def _migration_v24_generated_country(conn) -> None
```
