# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 6933
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1527–1615)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1540–1543 |  |
| `execute` | L1545–1556 |  |
| `executemany` | L1558–1568 |  |
| `executescript` | L1570–1580 |  |
| `commit` | L1582–1588 |  |
| `rollback` | L1590–1596 |  |
| `close` | L1598–1602 |  |
| `writing` | L1605–1612 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1614–1615 |  |

### `RadarDB` (L1618–6925)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1627–1637 |  |
| `_check_integrity_or_recreate` | L1639–1669 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1671–1693 |  |
| `_ensure_schema` | L1695–1701 |  |
| `_post_baseline_indexes` | L1703–1764 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1766–1773 |  |
| `set_schema_version` | L1775–1798 |  |
| `_run_migrations` | L2915–2950 | Apply any pending schema migrations in order. |
| `baseline_get` | L2953–2962 |  |
| `baseline_set` | L2964–2971 |  |
| `baseline_all` | L2973–2982 |  |
| `baseline_len` | L2984–2986 |  |
| `airspace_get` | L2989–2994 |  |
| `airspace_set` | L2996–3003 |  |
| `airspace_all` | L3005–3009 |  |
| `airspace_len` | L3011–3013 |  |
| `hod_record` | L3016–3034 |  |
| `hod_record_many` | L3036–3054 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L3056–3067 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L3069–3076 |  |
| `hod_all_entries` | L3078–3088 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L3090–3097 |  |
| `hod_distinct_hours` | L3099–3106 |  |
| `hod_total_points` | L3108–3112 |  |
| `hod_total_points_theater` | L3114–3120 |  |
| `gdelt_dow_record` | L3123–3137 |  |
| `gdelt_dow_last_bucket` | L3139–3144 |  |
| `gdelt_dow_same_weekday` | L3146–3153 |  |
| `gdelt_dow_total_points` | L3155–3157 |  |
| `ts_append` | L3160–3172 |  |
| `ts_get` | L3174–3179 |  |
| `ts_get_since` | L3181–3186 |  |
| `ts_distinct_theaters` | L3188–3192 |  |
| `ts_total_points` | L3194–3196 |  |
| `sensor_obs_record` | L3202–3219 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L3221–3230 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L3233–3246 |  |
| `series_get` | L3248–3253 |  |
| `alert_append` | L3256–3268 |  |
| `alert_list` | L3270–3275 |  |
| `alert_count` | L3277–3279 |  |
| `alert_clear` | L3281–3284 |  |
| `seq_append` | L3289–3303 |  |
| `seq_exists_since` | L3305–3311 |  |
| `seq_events_since` | L3313–3319 |  |
| `seq_all_events` | L3321–3327 |  |
| `seq_cleanup` | L3329–3332 |  |
| `seq_total` | L3334–3336 |  |
| `seq_distinct_theaters` | L3338–3342 |  |
| `seq_clear` | L3344–3347 |  |
| `tl_observation_append` | L3350–3365 |  |
| `scenario_history_start` | L3367–3372 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L3375–3425 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L3427–3443 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L3445–3461 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L3463–3535 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L3538–3570 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3572–3611 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L3614–3628 |  |
| `focus_switch_stats` | L3630–3658 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3660–3723 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L3726–3743 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L3745–3766 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L3768–3785 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L3787–3805 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3807–3891 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3894–3924 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3926–4014 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L4016–4035 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L4037–4046 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L4048–4072 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L4074–4093 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L4095–4105 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L4107–4117 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L4119–4131 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L4133–4155 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L4157–4182 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L4184–4233 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L4237–4248 |  |
| `scenario_list` | L4250–4262 |  |
| `scenario_upsert` | L4264–4298 |  |
| `scenario_update_state` | L4308–4334 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L4336–4352 |  |
| `scenario_delete` | L4354–4356 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L4358–4384 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L4386–4396 |  |
| `scenario_change_log` | L4398–4403 |  |
| `threat_append` | L4415–4422 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L4424–4453 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L4455–4464 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L4466–4486 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L4488–4492 |  |
| `threat_last_scoped` | L4494–4500 |  |
| `threat_clear` | L4502–4505 |  |
| `zscore_stats_get` | L4508–4524 |  |
| `zscore_stats_update` | L4526–4558 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L4560–4574 |  |
| `fetch_log_append` | L4577–4587 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4589–4616 | Per-sensor reliability aggregates over the given time window. |
| `config_change_log_append` | L4619–4657 | Append a row to the unified config audit ledger. Returns the new |
| `config_change_log_recent` | L4659–4691 | Return recent config change rows for the Audit UI. Optional domain |
| `config_change_log_domains` | L4693–4706 | Return distinct domains observed in the audit ledger over the |
| `llm_call_log_append` | L4709–4744 | Persist a single LLM call attempt and its downstream queue verdict. |
| `llm_shadow_invocation_append` | L4746–4778 | Phase 9.2 C4 — record a SHADOW_DUAL parallel invocation. Linked |
| `auto_judge_decision_log` | L4780–4806 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4808–4818 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4820–4838 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4840–4863 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4865–4961 | Aggregate stats per caller and overall verdict counts. |
| `llm_routing_stats` | L4963–5203 | Phase 8 (LLM survey v10) — per-model + per-use_case rollups. |
| `llm_embedding_stats` | L5205–5260 | Phase 9.2 C9 — embedding pipeline rollups for the LLM Console |
| `ct_log_known_cas` | L5263–5277 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L5279–5300 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L5302–5316 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L5318–5328 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L5330–5355 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L5357–5379 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L5382–5388 |  |
| `sensor_cache_get` | L5390–5397 |  |
| `sensor_cache_all` | L5399–5406 |  |
| `sensor_cache_count` | L5408–5410 |  |
| `noise_excl_add` | L5413–5424 |  |
| `noise_excl_remove` | L5426–5430 |  |
| `noise_excl_list` | L5432–5449 |  |
| `noise_excl_match` | L5451–5458 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L5461–5472 |  |
| `confirmed_threat_list` | L5474–5490 |  |
| `daily_summary_upsert` | L5493–5508 |  |
| `daily_summary_get` | L5510–5523 |  |
| `cooccurrence_update` | L5529–5570 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L5572–5587 |  |
| `startup_cleanup` | L5590–5593 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L5595–5612 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L5614–5622 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L5624–5628 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L5630–5824 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L5826–5849 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L5852–5873 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L5875–5890 | Load climate events since a given timestamp. |
| `climate_events_prune` | L5892–5896 | Remove climate events older than given timestamp. |
| `intel_upsert` | L5899–5921 | Insert or replace an LLM intel item. |
| `intel_update_status` | L5923–5932 |  |
| `intel_get` | L5934–5942 |  |
| `intel_list` | L5944–5970 |  |
| `intel_find_by_raw_url` | L5972–5983 | Find intel items with matching raw_url across all source_types. |
| `intel_recent_by_source` | L5985–5998 | Phase 9.2 C8 — recent intel rows for the same source_type, used |
| `intel_status_counts` | L6000–6027 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L6029–6052 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L6054–6064 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L6066–6077 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L6079–6094 |  |
| `intel_source_get` | L6096–6106 |  |
| `intel_source_upsert` | L6108–6117 |  |
| `intel_source_update_credibility` | L6119–6128 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L6130–6147 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L6149–6156 |  |
| `intel_source_reset_credibility` | L6158–6175 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L6177–6242 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L6244–6289 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L6293–6295 |  |
| `user_get` | L6297–6303 |  |
| `user_get_role` | L6305–6309 |  |
| `user_create` | L6311–6321 |  |
| `user_exists` | L6323–6327 |  |
| `user_update_last_login` | L6329–6332 |  |
| `user_update_role` | L6334–6337 |  |
| `user_update_password` | L6339–6346 |  |
| `user_delete` | L6348–6352 |  |
| `user_list` | L6354–6358 |  |
| `user_settings_get` | L6360–6366 |  |
| `user_settings_create` | L6368–6377 |  |
| `user_settings_update` | L6395–6411 |  |
| `token_revoke` | L6413–6419 |  |
| `token_is_revoked` | L6421–6425 |  |
| `user_get_invalidate_ts` | L6427–6435 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L6438–6452 |  |
| `hidden_signal_list` | L6454–6483 |  |
| `coverage_upsert` | L6486–6505 |  |
| `coverage_list` | L6507–6515 |  |
| `disconf_add` | L6518–6531 |  |
| `disconf_list` | L6533–6541 |  |
| `disconf_retract` | L6543–6551 |  |
| `ach_matrix_create` | L6554–6563 |  |
| `ach_matrix_list` | L6565–6572 |  |
| `ach_matrix_get` | L6574–6604 |  |
| `ach_hypothesis_add` | L6606–6617 |  |
| `ach_evidence_add` | L6619–6634 |  |
| `ach_score_set` | L6636–6650 |  |
| `dissent_add` | L6653–6663 |  |
| `dissent_list` | L6665–6673 |  |
| `dissent_resolve` | L6675–6684 |  |
| `assumption_add` | L6687–6707 |  |
| `_assumption_log` | L6709–6721 |  |
| `assumption_list` | L6723–6733 |  |
| `assumption_get` | L6735–6739 |  |
| `assumption_update` | L6741–6765 |  |
| `assumption_lock` | L6767–6790 |  |
| `assumption_invalidate` | L6792–6805 |  |
| `assumption_change_log` | L6807–6826 |  |
| `premortem_add` | L6829–6844 |  |
| `premortem_list` | L6846–6855 |  |
| `premortem_resolve` | L6857–6865 |  |
| `decision_log` | L6868–6883 |  |
| `decision_list` | L6885–6918 |  |
| `close` | L6921–6925 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L48–89 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v49_seed_config_runtime_from_env` | L92–183 | R4 — seed config_runtime_value from running os.environ for every |
| `_migration_v48_llm_embedding_ledger` | L186–237 | Phase 9.2 C7 — per-call ledger for the embedding pipeline. |
| `_migration_v47_llm_shadow_invocation` | L240–277 | Phase 9.2 C4 — dedicated table for SHADOW_DUAL parallel invocations. |
| `_migration_v45b_config_runtime_value` | L280–291 | Phase 9.1 — runtime override values for declarative config keys. |
| `_migration_v45_config_change_log` | L294–338 | Phase 9.1 (Foundation) — unified audit ledger for every analyst-driven |
| `_migration_v42_llm_routing_overrides` | L341–388 | Phase 8 (LLM survey v10) — analyst-driven routing overrides. |
| `_migration_v41_llm_routing_v10` | L391–428 | Phase 8 (LLM survey v10) — capture per-call thinking trace, the |
| `_migration_v37_decisions_revocation` | L431–494 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L497–538 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L541–553 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L556–619 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L622–649 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1540  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1545  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1558  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1570  (_CooperativeConn)
def executescript(self, sql: str)
# L1582  (_CooperativeConn)
def commit(self)
# L1590  (_CooperativeConn)
def rollback(self)
# L1598  (_CooperativeConn)
def close(self)
# L1605  (_CooperativeConn)
def writing(self)
# L1614  (_CooperativeConn)
def __getattr__(self, name)
# L1627  (RadarDB)
def __init__(self, db_path: str)
# L1639  (RadarDB)
def _check_integrity_or_recreate(self)
# L1671  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1695  (RadarDB)
def _ensure_schema(self)
# L1703  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1766  (RadarDB)
def schema_version(self) -> int
# L1775  (RadarDB)
def set_schema_version(self, ver: int)
# L2915  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2953  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2964  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2973  (RadarDB)
def baseline_all(self) -> dict
# L2984  (RadarDB)
def baseline_len(self) -> int
# L2989  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2996  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L3005  (RadarDB)
def airspace_all(self) -> dict
# L3011  (RadarDB)
def airspace_len(self) -> int
# L3016  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L3036  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L3056  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L3069  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L3078  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L3090  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L3099  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L3108  (RadarDB)
def hod_total_points(self, table: str) -> int
# L3114  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L3123  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L3139  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L3146  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L3155  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L3160  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L3174  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L3181  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L3188  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L3194  (RadarDB)
def ts_total_points(self) -> int
# L3202  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L3221  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L3233  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L3248  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L3256  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L3270  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L3277  (RadarDB)
def alert_count(self) -> int
# L3281  (RadarDB)
def alert_clear(self)
# L3289  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L3305  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L3313  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L3321  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L3329  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L3334  (RadarDB)
def seq_total(self) -> int
# L3338  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L3344  (RadarDB)
def seq_clear(self)
# L3350  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L3367  (RadarDB)
def scenario_history_start(self) -> float | None
# L3375  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L3427  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L3445  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L3463  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L3538  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3572  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L3614  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L3630  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3660  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L3726  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L3745  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L3768  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L3787  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3807  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3894  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3926  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L4016  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L4037  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L4048  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L4074  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L4095  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L4107  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L4119  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L4133  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L4157  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L4184  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L4237  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L4250  (RadarDB)
def scenario_list(self) -> list[dict]
# L4264  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L4308  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L4336  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L4354  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4358  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4386  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L4398  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L4415  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L4424  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L4455  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L4466  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L4488  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L4494  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L4502  (RadarDB)
def threat_clear(self)
# L4508  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L4526  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L4560  (RadarDB)
def zscore_stats_all(self) -> list
# L4577  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4589  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4619  (RadarDB)
def config_change_log_append(self, *, domain: str, config_key: str, old_value: 'object', new_value: 'object', changed_by: str, reason: 'str | None', request_id: 'str | None') -> int
# L4659  (RadarDB)
def config_change_log_recent(self, *, hours: int, domain: 'str | None', limit: int) -> list
# L4693  (RadarDB)
def config_change_log_domains(self, *, hours: int) -> list
# L4709  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str, use_case: 'str | None', shadow_model_choice: 'str | None', thinking_trace: 'str | None')
# L4746  (RadarDB)
def llm_shadow_invocation_append(self, *, primary_call_id: int, model: str, duration_ms: int, outcome: str, response_text: 'str | None', confidence: 'float | None', prompt_sha256: 'str | None', error: 'str | None') -> int
# L4780  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4808  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4820  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4840  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4865  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4963  (RadarDB)
def llm_routing_stats(self, hours: int) -> dict
# L5205  (RadarDB)
def llm_embedding_stats(self, hours: int) -> dict
# L5263  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L5279  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L5302  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L5318  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L5330  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L5357  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L5382  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L5390  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L5399  (RadarDB)
def sensor_cache_all(self) -> dict
# L5408  (RadarDB)
def sensor_cache_count(self) -> int
# L5413  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L5426  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L5432  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L5451  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L5461  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L5474  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L5493  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L5510  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L5529  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L5572  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L5590  (RadarDB)
def startup_cleanup(self)
# L5595  (RadarDB)
def shutdown(self)
# L5614  (RadarDB)
def wal_checkpoint(self)
# L5624  (RadarDB)
def periodic_cleanup(self)
# L5630  (RadarDB)
def _prune_stale_rows(self) -> dict
# L5826  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L5852  (RadarDB)
def climate_events_save(self, events: list[dict])
# L5875  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L5892  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L5899  (RadarDB)
def intel_upsert(self, item: dict)
# L5923  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L5934  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L5944  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L5972  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L5985  (RadarDB)
def intel_recent_by_source(self, source_type: str, since_ts: float, limit: int) -> list[dict]
# L6000  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L6029  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L6054  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L6066  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L6079  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L6096  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L6108  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L6119  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L6130  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L6149  (RadarDB)
def intel_source_list(self) -> list[dict]
# L6158  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L6177  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L6244  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L6293  (RadarDB)
def user_count(self) -> int
# L6297  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L6305  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L6311  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L6323  (RadarDB)
def user_exists(self, username: str) -> bool
# L6329  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L6334  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L6339  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L6348  (RadarDB)
def user_delete(self, user_id: int)
# L6354  (RadarDB)
def user_list(self) -> list[dict]
# L6360  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L6368  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L6395  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L6413  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L6421  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L6427  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L6438  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L6454  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L6486  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L6507  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L6518  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L6533  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L6543  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L6554  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L6565  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L6574  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L6606  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L6619  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L6636  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L6653  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L6665  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6675  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L6687  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L6709  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L6723  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L6735  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L6741  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L6767  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L6792  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L6807  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L6829  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L6846  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6857  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L6868  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L6885  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L6921  (RadarDB)
def close(self)
# L48
def _migration_v40_schema_version_singleton(conn) -> None
# L92
def _migration_v49_seed_config_runtime_from_env(conn) -> None
# L186
def _migration_v48_llm_embedding_ledger(conn) -> None
# L240
def _migration_v47_llm_shadow_invocation(conn) -> None
# L280
def _migration_v45b_config_runtime_value(conn) -> None
# L294
def _migration_v45_config_change_log(conn) -> None
# L341
def _migration_v42_llm_routing_overrides(conn) -> None
# L391
def _migration_v41_llm_routing_v10(conn) -> None
# L431
def _migration_v37_decisions_revocation(conn) -> None
# L497
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L541
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L556
def _migration_v36_bg_observer(conn) -> None
# L622
def _migration_v24_generated_country(conn) -> None
```
