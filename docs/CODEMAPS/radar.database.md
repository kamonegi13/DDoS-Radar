# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 4982
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L750–838)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L763–766 |  |
| `execute` | L768–779 |  |
| `executemany` | L781–791 |  |
| `executescript` | L793–803 |  |
| `commit` | L805–811 |  |
| `rollback` | L813–819 |  |
| `close` | L821–825 |  |
| `writing` | L828–835 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L837–838 |  |

### `RadarDB` (L841–4966)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L850–856 |  |
| `_check_integrity_or_recreate` | L858–888 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L890–900 |  |
| `_ensure_schema` | L902–908 |  |
| `_post_baseline_indexes` | L910–931 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L933–940 |  |
| `set_schema_version` | L942–948 |  |
| `_run_migrations` | L1739–1774 | Apply any pending schema migrations in order. |
| `baseline_get` | L1777–1786 |  |
| `baseline_set` | L1788–1795 |  |
| `baseline_all` | L1797–1806 |  |
| `baseline_len` | L1808–1810 |  |
| `airspace_get` | L1813–1818 |  |
| `airspace_set` | L1820–1827 |  |
| `airspace_all` | L1829–1833 |  |
| `airspace_len` | L1835–1837 |  |
| `hod_record` | L1840–1858 |  |
| `hod_record_many` | L1860–1878 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L1880–1891 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L1893–1900 |  |
| `hod_all_entries` | L1902–1912 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L1914–1921 |  |
| `hod_distinct_hours` | L1923–1930 |  |
| `hod_total_points` | L1932–1936 |  |
| `hod_total_points_theater` | L1938–1944 |  |
| `gdelt_dow_record` | L1947–1961 |  |
| `gdelt_dow_last_bucket` | L1963–1968 |  |
| `gdelt_dow_same_weekday` | L1970–1977 |  |
| `gdelt_dow_total_points` | L1979–1981 |  |
| `ts_append` | L1984–1996 |  |
| `ts_get` | L1998–2003 |  |
| `ts_get_since` | L2005–2010 |  |
| `ts_distinct_theaters` | L2012–2016 |  |
| `ts_total_points` | L2018–2020 |  |
| `sensor_obs_record` | L2026–2043 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L2045–2054 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L2057–2070 |  |
| `series_get` | L2072–2077 |  |
| `alert_append` | L2080–2092 |  |
| `alert_list` | L2094–2099 |  |
| `alert_count` | L2101–2103 |  |
| `alert_clear` | L2105–2108 |  |
| `seq_append` | L2113–2127 |  |
| `seq_exists_since` | L2129–2135 |  |
| `seq_events_since` | L2137–2143 |  |
| `seq_all_events` | L2145–2151 |  |
| `seq_cleanup` | L2153–2156 |  |
| `seq_total` | L2158–2160 |  |
| `seq_distinct_theaters` | L2162–2166 |  |
| `seq_clear` | L2168–2171 |  |
| `tl_observation_append` | L2174–2189 |  |
| `scenario_history_start` | L2191–2196 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L2199–2218 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L2220–2236 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L2238–2254 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L2256–2304 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `focus_switch_append` | L2307–2321 |  |
| `focus_switch_stats` | L2323–2351 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L2353–2416 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L2419–2436 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L2438–2459 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L2461–2478 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L2480–2498 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L2500–2584 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L2587–2617 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L2619–2707 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L2709–2728 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L2730–2739 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L2741–2765 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L2767–2786 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L2788–2798 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L2800–2810 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L2812–2824 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L2826–2848 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L2850–2875 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L2877–2926 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L2930–2941 |  |
| `scenario_list` | L2943–2955 |  |
| `scenario_upsert` | L2957–2991 |  |
| `scenario_update_state` | L3001–3027 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L3029–3045 |  |
| `scenario_delete` | L3047–3049 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L3051–3077 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L3079–3089 |  |
| `scenario_change_log` | L3091–3096 |  |
| `threat_append` | L3099–3110 |  |
| `threat_list` | L3112–3116 |  |
| `threat_last` | L3118–3122 |  |
| `threat_clear` | L3124–3127 |  |
| `zscore_stats_get` | L3130–3146 |  |
| `zscore_stats_update` | L3148–3180 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L3182–3196 |  |
| `fetch_log_append` | L3199–3209 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L3211–3238 | Per-sensor reliability aggregates over the given time window. |
| `llm_call_log_append` | L3241–3260 | Persist a single LLM call attempt and its downstream queue verdict. |
| `auto_judge_decision_log` | L3262–3288 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L3290–3300 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L3302–3320 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L3322–3345 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L3347–3412 | Aggregate stats per caller and overall verdict counts. |
| `ct_log_known_cas` | L3415–3429 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L3431–3452 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L3454–3468 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L3470–3480 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L3482–3507 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L3509–3531 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L3534–3540 |  |
| `sensor_cache_get` | L3542–3549 |  |
| `sensor_cache_all` | L3551–3558 |  |
| `sensor_cache_count` | L3560–3562 |  |
| `noise_excl_add` | L3565–3576 |  |
| `noise_excl_remove` | L3578–3582 |  |
| `noise_excl_list` | L3584–3601 |  |
| `noise_excl_match` | L3603–3610 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L3613–3624 |  |
| `confirmed_threat_list` | L3626–3642 |  |
| `daily_summary_upsert` | L3645–3660 |  |
| `daily_summary_get` | L3662–3675 |  |
| `forecast_log_add` | L3678–3687 |  |
| `forecast_log_resolve` | L3689–3696 |  |
| `forecast_log_get` | L3698–3714 |  |
| `forecast_accuracy_summary` | L3716–3738 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L3741–3782 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L3784–3799 |  |
| `startup_cleanup` | L3802–3805 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L3807–3824 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L3826–3834 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L3836–3840 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L3842–3946 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `climate_events_save` | L3949–3970 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L3972–3987 | Load climate events since a given timestamp. |
| `climate_events_prune` | L3989–3993 | Remove climate events older than given timestamp. |
| `intel_upsert` | L3996–4018 | Insert or replace an LLM intel item. |
| `intel_update_status` | L4020–4029 |  |
| `intel_get` | L4031–4039 |  |
| `intel_list` | L4041–4067 |  |
| `intel_find_by_raw_url` | L4069–4080 | Find intel items with matching raw_url across all source_types. |
| `intel_status_counts` | L4082–4087 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L4089–4112 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L4114–4124 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L4126–4137 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L4139–4154 |  |
| `intel_source_get` | L4156–4166 |  |
| `intel_source_upsert` | L4168–4177 |  |
| `intel_source_update_credibility` | L4179–4188 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L4190–4207 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L4209–4216 |  |
| `intel_source_reset_credibility` | L4218–4235 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L4237–4302 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L4304–4349 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L4353–4355 |  |
| `user_get` | L4357–4363 |  |
| `user_get_role` | L4365–4369 |  |
| `user_create` | L4371–4381 |  |
| `user_exists` | L4383–4387 |  |
| `user_update_last_login` | L4389–4392 |  |
| `user_update_role` | L4394–4397 |  |
| `user_update_password` | L4399–4406 |  |
| `user_delete` | L4408–4412 |  |
| `user_list` | L4414–4418 |  |
| `user_settings_get` | L4420–4426 |  |
| `user_settings_create` | L4428–4437 |  |
| `user_settings_update` | L4442–4452 |  |
| `token_revoke` | L4454–4460 |  |
| `token_is_revoked` | L4462–4466 |  |
| `user_get_invalidate_ts` | L4468–4476 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L4479–4493 |  |
| `hidden_signal_list` | L4495–4524 |  |
| `coverage_upsert` | L4527–4546 |  |
| `coverage_list` | L4548–4556 |  |
| `disconf_add` | L4559–4572 |  |
| `disconf_list` | L4574–4582 |  |
| `disconf_retract` | L4584–4592 |  |
| `ach_matrix_create` | L4595–4604 |  |
| `ach_matrix_list` | L4606–4613 |  |
| `ach_matrix_get` | L4615–4645 |  |
| `ach_hypothesis_add` | L4647–4658 |  |
| `ach_evidence_add` | L4660–4675 |  |
| `ach_score_set` | L4677–4691 |  |
| `dissent_add` | L4694–4704 |  |
| `dissent_list` | L4706–4714 |  |
| `dissent_resolve` | L4716–4725 |  |
| `assumption_add` | L4728–4748 |  |
| `_assumption_log` | L4750–4762 |  |
| `assumption_list` | L4764–4774 |  |
| `assumption_get` | L4776–4780 |  |
| `assumption_update` | L4782–4806 |  |
| `assumption_lock` | L4808–4831 |  |
| `assumption_invalidate` | L4833–4846 |  |
| `assumption_change_log` | L4848–4867 |  |
| `premortem_add` | L4870–4885 |  |
| `premortem_list` | L4887–4896 |  |
| `premortem_resolve` | L4898–4906 |  |
| `decision_log` | L4909–4924 |  |
| `decision_list` | L4926–4959 |  |
| `close` | L4962–4966 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v24_generated_country` | L49–76 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L763  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L768  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L781  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L793  (_CooperativeConn)
def executescript(self, sql: str)
# L805  (_CooperativeConn)
def commit(self)
# L813  (_CooperativeConn)
def rollback(self)
# L821  (_CooperativeConn)
def close(self)
# L828  (_CooperativeConn)
def writing(self)
# L837  (_CooperativeConn)
def __getattr__(self, name)
# L850  (RadarDB)
def __init__(self, db_path: str)
# L858  (RadarDB)
def _check_integrity_or_recreate(self)
# L890  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L902  (RadarDB)
def _ensure_schema(self)
# L910  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L933  (RadarDB)
def schema_version(self) -> int
# L942  (RadarDB)
def set_schema_version(self, ver: int)
# L1739  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L1777  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L1788  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L1797  (RadarDB)
def baseline_all(self) -> dict
# L1808  (RadarDB)
def baseline_len(self) -> int
# L1813  (RadarDB)
def airspace_get(self, code: str) -> dict
# L1820  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L1829  (RadarDB)
def airspace_all(self) -> dict
# L1835  (RadarDB)
def airspace_len(self) -> int
# L1840  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L1860  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L1880  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L1893  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L1902  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L1914  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L1923  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L1932  (RadarDB)
def hod_total_points(self, table: str) -> int
# L1938  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L1947  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L1963  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L1970  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L1979  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L1984  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L1998  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L2005  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L2012  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L2018  (RadarDB)
def ts_total_points(self) -> int
# L2026  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L2045  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L2057  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L2072  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L2080  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L2094  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L2101  (RadarDB)
def alert_count(self) -> int
# L2105  (RadarDB)
def alert_clear(self)
# L2113  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L2129  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L2137  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L2145  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L2153  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L2158  (RadarDB)
def seq_total(self) -> int
# L2162  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L2168  (RadarDB)
def seq_clear(self)
# L2174  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L2191  (RadarDB)
def scenario_history_start(self) -> float | None
# L2199  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float) -> None
# L2220  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L2238  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L2256  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L2307  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L2323  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L2353  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L2419  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L2438  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L2461  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L2480  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L2500  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L2587  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L2619  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L2709  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L2730  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L2741  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L2767  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L2788  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L2800  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L2812  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L2826  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L2850  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L2877  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L2930  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L2943  (RadarDB)
def scenario_list(self) -> list[dict]
# L2957  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L3001  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L3029  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L3047  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3051  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3079  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L3091  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L3099  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L3112  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L3118  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L3124  (RadarDB)
def threat_clear(self)
# L3130  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L3148  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L3182  (RadarDB)
def zscore_stats_all(self) -> list
# L3199  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L3211  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L3241  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str)
# L3262  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L3290  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L3302  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L3322  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L3347  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L3415  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L3431  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L3454  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L3470  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L3482  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L3509  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L3534  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L3542  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L3551  (RadarDB)
def sensor_cache_all(self) -> dict
# L3560  (RadarDB)
def sensor_cache_count(self) -> int
# L3565  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L3578  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L3584  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L3603  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L3613  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L3626  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L3645  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L3662  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L3678  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L3689  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L3698  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L3716  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L3741  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L3784  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L3802  (RadarDB)
def startup_cleanup(self)
# L3807  (RadarDB)
def shutdown(self)
# L3826  (RadarDB)
def wal_checkpoint(self)
# L3836  (RadarDB)
def periodic_cleanup(self)
# L3842  (RadarDB)
def _prune_stale_rows(self) -> dict
# L3949  (RadarDB)
def climate_events_save(self, events: list[dict])
# L3972  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L3989  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L3996  (RadarDB)
def intel_upsert(self, item: dict)
# L4020  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L4031  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L4041  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L4069  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L4082  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L4089  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L4114  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L4126  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L4139  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L4156  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L4168  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L4179  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L4190  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L4209  (RadarDB)
def intel_source_list(self) -> list[dict]
# L4218  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L4237  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L4304  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L4353  (RadarDB)
def user_count(self) -> int
# L4357  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L4365  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L4371  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L4383  (RadarDB)
def user_exists(self, username: str) -> bool
# L4389  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L4394  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L4399  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L4408  (RadarDB)
def user_delete(self, user_id: int)
# L4414  (RadarDB)
def user_list(self) -> list[dict]
# L4420  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L4428  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L4442  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L4454  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L4462  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L4468  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L4479  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L4495  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L4527  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L4548  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L4559  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L4574  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L4584  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L4595  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L4606  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L4615  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L4647  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L4660  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L4677  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L4694  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L4706  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L4716  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L4728  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L4750  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L4764  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L4776  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L4782  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L4808  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L4833  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L4848  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L4870  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L4887  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L4898  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L4909  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L4926  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L4962  (RadarDB)
def close(self)
# L49
def _migration_v24_generated_country(conn) -> None
```
