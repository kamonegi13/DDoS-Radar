# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 6942
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1617–1705)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1630–1633 |  |
| `execute` | L1635–1646 |  |
| `executemany` | L1648–1658 |  |
| `executescript` | L1660–1670 |  |
| `commit` | L1672–1678 |  |
| `rollback` | L1680–1686 |  |
| `close` | L1688–1692 |  |
| `writing` | L1695–1702 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1704–1705 |  |

### `RadarDB` (L1708–6934)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1717–1727 |  |
| `_check_integrity_or_recreate` | L1729–1759 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1761–1783 |  |
| `_ensure_schema` | L1785–1791 |  |
| `_post_baseline_indexes` | L1793–1854 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1856–1863 |  |
| `set_schema_version` | L1865–1888 |  |
| `_run_migrations` | L3018–3053 | Apply any pending schema migrations in order. |
| `baseline_get` | L3056–3065 |  |
| `baseline_set` | L3067–3074 |  |
| `baseline_all` | L3076–3085 |  |
| `baseline_len` | L3087–3089 |  |
| `airspace_get` | L3092–3097 |  |
| `airspace_set` | L3099–3106 |  |
| `airspace_all` | L3108–3112 |  |
| `airspace_len` | L3114–3116 |  |
| `hod_record` | L3119–3137 |  |
| `hod_record_many` | L3139–3157 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L3159–3170 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L3172–3179 |  |
| `hod_all_entries` | L3181–3191 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L3193–3200 |  |
| `hod_distinct_hours` | L3202–3209 |  |
| `hod_total_points` | L3211–3215 |  |
| `hod_total_points_theater` | L3217–3223 |  |
| `gdelt_dow_record` | L3226–3240 |  |
| `gdelt_dow_last_bucket` | L3242–3247 |  |
| `gdelt_dow_same_weekday` | L3249–3256 |  |
| `gdelt_dow_total_points` | L3258–3260 |  |
| `ts_append` | L3263–3275 |  |
| `ts_get` | L3277–3282 |  |
| `ts_get_since` | L3284–3289 |  |
| `ts_distinct_theaters` | L3291–3295 |  |
| `ts_total_points` | L3297–3299 |  |
| `sensor_obs_record` | L3308–3325 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L3327–3336 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `flag_state_upsert` | L3339–3353 | Record one evaluation of one detection flag. |
| `flag_record_evaluation` | L3373–3401 | Persist one sensor's whole flag evaluation in ONE transaction. |
| `flag_state_all` | L3403–3409 | Every (sensor, flag) state row. |
| `flag_state_for` | L3411–3418 | State rows for a single sensor (cheap path for detection_health). |
| `flag_fire_append` | L3420–3438 | Append one fire event and opportunistically prune > ttl_sec. |
| `flag_fire_counts_since` | L3440–3447 | {(sensor, flag_id): fire count} since `since_ts`, in one query. |
| `l5_job_get` | L3449–3454 |  |
| `l5_job_set` | L3456–3467 |  |
| `l5_check_append` | L3469–3492 | Append one verification result (S5-VERIF-016). Never updates. |
| `l5_check_latest` | L3494–3501 |  |
| `l5_check_last_for_target` | L3503–3510 | Most recent row for one target — backs first_detected_at carry-forward. |
| `series_append` | L3513–3526 |  |
| `series_get` | L3528–3533 |  |
| `alert_append` | L3536–3548 |  |
| `alert_list` | L3550–3555 |  |
| `alert_count` | L3557–3559 |  |
| `alert_clear` | L3561–3564 |  |
| `seq_append` | L3569–3583 |  |
| `seq_exists_since` | L3585–3591 |  |
| `seq_events_since` | L3593–3599 |  |
| `seq_all_events` | L3601–3607 |  |
| `seq_cleanup` | L3609–3612 |  |
| `seq_total` | L3614–3616 |  |
| `seq_distinct_theaters` | L3618–3622 |  |
| `seq_clear` | L3624–3627 |  |
| `tl_observation_append` | L3630–3645 |  |
| `scenario_history_start` | L3647–3652 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L3655–3705 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L3707–3723 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L3725–3741 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L3743–3815 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L3818–3850 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3852–3891 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L3894–3908 |  |
| `focus_switch_stats` | L3910–3938 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3940–4003 | Extended focus_switch_stats with delta distribution and per-scenario |
| `intel_count_24h_by_scenario` | L4010–4029 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L4031–4040 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L4042–4066 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L4068–4087 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L4089–4099 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L4101–4111 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L4113–4125 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L4127–4149 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L4151–4176 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L4178–4227 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L4231–4242 |  |
| `scenario_list` | L4244–4256 |  |
| `scenario_upsert` | L4258–4292 |  |
| `scenario_update_state` | L4302–4328 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L4330–4346 |  |
| `scenario_delete` | L4348–4350 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L4352–4378 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L4380–4390 |  |
| `scenario_change_log` | L4392–4397 |  |
| `threat_append` | L4409–4416 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L4418–4447 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L4449–4458 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L4460–4480 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L4482–4486 |  |
| `threat_last_scoped` | L4488–4494 |  |
| `threat_clear` | L4496–4499 |  |
| `zscore_stats_get` | L4502–4518 |  |
| `zscore_stats_update` | L4520–4552 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L4554–4568 |  |
| `fetch_log_append` | L4571–4581 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4583–4610 | Per-sensor reliability aggregates over the given time window. |
| `config_change_log_append` | L4613–4651 | Append a row to the unified config audit ledger. Returns the new |
| `config_change_log_recent` | L4653–4685 | Return recent config change rows for the Audit UI. Optional domain |
| `config_change_log_domains` | L4687–4700 | Return distinct domains observed in the audit ledger over the |
| `llm_call_log_append` | L4703–4738 | Persist a single LLM call attempt and its downstream queue verdict. |
| `llm_shadow_invocation_append` | L4740–4772 | Phase 9.2 C4 — record a SHADOW_DUAL parallel invocation. Linked |
| `auto_judge_decision_log` | L4774–4800 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4802–4812 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4814–4832 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4834–4857 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4859–4955 | Aggregate stats per caller and overall verdict counts. |
| `llm_routing_stats` | L4957–5197 | Phase 8 (LLM survey v10) — per-model + per-use_case rollups. |
| `llm_embedding_stats` | L5199–5254 | Phase 9.2 C9 — embedding pipeline rollups for the LLM Console |
| `ct_log_known_cas` | L5257–5271 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L5273–5294 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L5296–5310 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L5312–5322 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L5324–5349 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L5351–5373 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L5376–5382 |  |
| `sensor_cache_get` | L5384–5391 |  |
| `sensor_cache_all` | L5393–5400 |  |
| `sensor_cache_count` | L5402–5404 |  |
| `noise_excl_add` | L5407–5418 |  |
| `noise_excl_remove` | L5420–5424 |  |
| `noise_excl_list` | L5426–5443 |  |
| `noise_excl_match` | L5445–5452 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L5455–5466 |  |
| `confirmed_threat_list` | L5468–5484 |  |
| `daily_summary_upsert` | L5487–5502 |  |
| `daily_summary_get` | L5504–5517 |  |
| `cooccurrence_update` | L5523–5564 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L5566–5581 |  |
| `startup_cleanup` | L5584–5587 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L5589–5606 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L5608–5616 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L5618–5622 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L5624–5833 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L5835–5858 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L5861–5882 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L5884–5899 | Load climate events since a given timestamp. |
| `climate_events_prune` | L5901–5905 | Remove climate events older than given timestamp. |
| `intel_upsert` | L5908–5930 | Insert or replace an LLM intel item. |
| `intel_update_status` | L5932–5941 |  |
| `intel_get` | L5943–5951 |  |
| `intel_list` | L5953–5979 |  |
| `intel_find_by_raw_url` | L5981–5992 | Find intel items with matching raw_url across all source_types. |
| `intel_recent_by_source` | L5994–6007 | Phase 9.2 C8 — recent intel rows for the same source_type, used |
| `intel_status_counts` | L6009–6036 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L6038–6061 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L6063–6073 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L6075–6086 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L6088–6103 |  |
| `intel_source_get` | L6105–6115 |  |
| `intel_source_upsert` | L6117–6126 |  |
| `intel_source_update_credibility` | L6128–6137 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L6139–6156 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L6158–6165 |  |
| `intel_source_reset_credibility` | L6167–6184 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L6186–6251 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L6253–6298 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L6302–6304 |  |
| `user_get` | L6306–6312 |  |
| `user_get_role` | L6314–6318 |  |
| `user_create` | L6320–6330 |  |
| `user_exists` | L6332–6336 |  |
| `user_update_last_login` | L6338–6341 |  |
| `user_update_role` | L6343–6346 |  |
| `user_update_password` | L6348–6355 |  |
| `user_delete` | L6357–6361 |  |
| `user_list` | L6363–6367 |  |
| `user_settings_get` | L6369–6375 |  |
| `user_settings_create` | L6377–6386 |  |
| `user_settings_update` | L6404–6420 |  |
| `token_revoke` | L6422–6428 |  |
| `token_is_revoked` | L6430–6434 |  |
| `user_get_invalidate_ts` | L6436–6444 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L6447–6461 |  |
| `hidden_signal_list` | L6463–6492 |  |
| `coverage_upsert` | L6495–6514 |  |
| `coverage_list` | L6516–6524 |  |
| `disconf_add` | L6527–6540 |  |
| `disconf_list` | L6542–6550 |  |
| `disconf_retract` | L6552–6560 |  |
| `ach_matrix_create` | L6563–6572 |  |
| `ach_matrix_list` | L6574–6581 |  |
| `ach_matrix_get` | L6583–6613 |  |
| `ach_hypothesis_add` | L6615–6626 |  |
| `ach_evidence_add` | L6628–6643 |  |
| `ach_score_set` | L6645–6659 |  |
| `dissent_add` | L6662–6672 |  |
| `dissent_list` | L6674–6682 |  |
| `dissent_resolve` | L6684–6693 |  |
| `assumption_add` | L6696–6716 |  |
| `_assumption_log` | L6718–6730 |  |
| `assumption_list` | L6732–6742 |  |
| `assumption_get` | L6744–6748 |  |
| `assumption_update` | L6750–6774 |  |
| `assumption_lock` | L6776–6799 |  |
| `assumption_invalidate` | L6801–6814 |  |
| `assumption_change_log` | L6816–6835 |  |
| `premortem_add` | L6838–6853 |  |
| `premortem_list` | L6855–6864 |  |
| `premortem_resolve` | L6866–6874 |  |
| `decision_log` | L6877–6892 |  |
| `decision_list` | L6894–6927 |  |
| `close` | L6930–6934 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L48–89 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v49_seed_config_runtime_from_env` | L92–183 | R4 — seed config_runtime_value from running os.environ for every |
| `_migration_v55_firing_liveness` | L186–253 | WP-1.1 (v3 Phase 1) — L5 firing-liveness monitoring tables. |
| `_migration_v48_llm_embedding_ledger` | L256–307 | Phase 9.2 C7 — per-call ledger for the embedding pipeline. |
| `_migration_v47_llm_shadow_invocation` | L310–347 | Phase 9.2 C4 — dedicated table for SHADOW_DUAL parallel invocations. |
| `_migration_v45b_config_runtime_value` | L350–361 | Phase 9.1 — runtime override values for declarative config keys. |
| `_migration_v45_config_change_log` | L364–408 | Phase 9.1 (Foundation) — unified audit ledger for every analyst-driven |
| `_migration_v42_llm_routing_overrides` | L411–458 | Phase 8 (LLM survey v10) — analyst-driven routing overrides. |
| `_migration_v41_llm_routing_v10` | L461–498 | Phase 8 (LLM survey v10) — capture per-call thinking trace, the |
| `_migration_v37_decisions_revocation` | L501–564 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L567–608 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L611–623 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L626–689 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L692–719 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1630  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1635  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1648  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1660  (_CooperativeConn)
def executescript(self, sql: str)
# L1672  (_CooperativeConn)
def commit(self)
# L1680  (_CooperativeConn)
def rollback(self)
# L1688  (_CooperativeConn)
def close(self)
# L1695  (_CooperativeConn)
def writing(self)
# L1704  (_CooperativeConn)
def __getattr__(self, name)
# L1717  (RadarDB)
def __init__(self, db_path: str)
# L1729  (RadarDB)
def _check_integrity_or_recreate(self)
# L1761  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1785  (RadarDB)
def _ensure_schema(self)
# L1793  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1856  (RadarDB)
def schema_version(self) -> int
# L1865  (RadarDB)
def set_schema_version(self, ver: int)
# L3018  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L3056  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L3067  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L3076  (RadarDB)
def baseline_all(self) -> dict
# L3087  (RadarDB)
def baseline_len(self) -> int
# L3092  (RadarDB)
def airspace_get(self, code: str) -> dict
# L3099  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L3108  (RadarDB)
def airspace_all(self) -> dict
# L3114  (RadarDB)
def airspace_len(self) -> int
# L3119  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L3139  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L3159  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L3172  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L3181  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L3193  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L3202  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L3211  (RadarDB)
def hod_total_points(self, table: str) -> int
# L3217  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L3226  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L3242  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L3249  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L3258  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L3263  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L3277  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L3284  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L3291  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L3297  (RadarDB)
def ts_total_points(self) -> int
# L3308  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, ttl_sec: float, baseline: float | None, status: str | None) -> None
# L3327  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L3339  (RadarDB)
def flag_state_upsert(self, sensor: str, flag_id: str, ts: float, fired: bool) -> None
# L3373  (RadarDB)
def flag_record_evaluation(self, sensor: str, fired: dict[str, bool], ts: float, ttl_sec: float) -> None
# L3403  (RadarDB)
def flag_state_all(self) -> list[dict]
# L3411  (RadarDB)
def flag_state_for(self, sensor: str) -> list[dict]
# L3420  (RadarDB)
def flag_fire_append(self, sensor: str, flag_id: str, ts: float, ttl_sec: float) -> None
# L3440  (RadarDB)
def flag_fire_counts_since(self, since_ts: float) -> dict[tuple, int]
# L3449  (RadarDB)
def l5_job_get(self, job_id: str) -> dict | None
# L3456  (RadarDB)
def l5_job_set(self, job_id: str, last_run_at: float, next_run_at: float) -> None
# L3469  (RadarDB)
def l5_check_append(self, ts: float, check_id: str, target: str, verdict: str, measured_json: str | None, expected_json: str | None, first_detected_at: float | None, ttl_sec: float | None) -> None
# L3494  (RadarDB)
def l5_check_latest(self, check_id: str, limit: int) -> list[dict]
# L3503  (RadarDB)
def l5_check_last_for_target(self, check_id: str, target: str) -> dict | None
# L3513  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L3528  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L3536  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L3550  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L3557  (RadarDB)
def alert_count(self) -> int
# L3561  (RadarDB)
def alert_clear(self)
# L3569  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L3585  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L3593  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L3601  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L3609  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L3614  (RadarDB)
def seq_total(self) -> int
# L3618  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L3624  (RadarDB)
def seq_clear(self)
# L3630  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L3647  (RadarDB)
def scenario_history_start(self) -> float | None
# L3655  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L3707  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L3725  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L3743  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L3818  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3852  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L3894  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L3910  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3940  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L4010  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L4031  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L4042  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L4068  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L4089  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L4101  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L4113  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L4127  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L4151  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L4178  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L4231  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L4244  (RadarDB)
def scenario_list(self) -> list[dict]
# L4258  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L4302  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L4330  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L4348  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4352  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4380  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L4392  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L4409  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L4418  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L4449  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L4460  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L4482  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L4488  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L4496  (RadarDB)
def threat_clear(self)
# L4502  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L4520  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L4554  (RadarDB)
def zscore_stats_all(self) -> list
# L4571  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4583  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4613  (RadarDB)
def config_change_log_append(self, *, domain: str, config_key: str, old_value: 'object', new_value: 'object', changed_by: str, reason: 'str | None', request_id: 'str | None') -> int
# L4653  (RadarDB)
def config_change_log_recent(self, *, hours: int, domain: 'str | None', limit: int) -> list
# L4687  (RadarDB)
def config_change_log_domains(self, *, hours: int) -> list
# L4703  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str, use_case: 'str | None', shadow_model_choice: 'str | None', thinking_trace: 'str | None')
# L4740  (RadarDB)
def llm_shadow_invocation_append(self, *, primary_call_id: int, model: str, duration_ms: int, outcome: str, response_text: 'str | None', confidence: 'float | None', prompt_sha256: 'str | None', error: 'str | None') -> int
# L4774  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4802  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4814  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4834  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4859  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4957  (RadarDB)
def llm_routing_stats(self, hours: int) -> dict
# L5199  (RadarDB)
def llm_embedding_stats(self, hours: int) -> dict
# L5257  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L5273  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L5296  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L5312  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L5324  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L5351  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L5376  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L5384  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L5393  (RadarDB)
def sensor_cache_all(self) -> dict
# L5402  (RadarDB)
def sensor_cache_count(self) -> int
# L5407  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L5420  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L5426  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L5445  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L5455  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L5468  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L5487  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L5504  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L5523  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L5566  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L5584  (RadarDB)
def startup_cleanup(self)
# L5589  (RadarDB)
def shutdown(self)
# L5608  (RadarDB)
def wal_checkpoint(self)
# L5618  (RadarDB)
def periodic_cleanup(self)
# L5624  (RadarDB)
def _prune_stale_rows(self) -> dict
# L5835  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L5861  (RadarDB)
def climate_events_save(self, events: list[dict])
# L5884  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L5901  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L5908  (RadarDB)
def intel_upsert(self, item: dict)
# L5932  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L5943  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L5953  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L5981  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L5994  (RadarDB)
def intel_recent_by_source(self, source_type: str, since_ts: float, limit: int) -> list[dict]
# L6009  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L6038  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L6063  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L6075  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L6088  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L6105  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L6117  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L6128  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L6139  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L6158  (RadarDB)
def intel_source_list(self) -> list[dict]
# L6167  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L6186  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L6253  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L6302  (RadarDB)
def user_count(self) -> int
# L6306  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L6314  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L6320  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L6332  (RadarDB)
def user_exists(self, username: str) -> bool
# L6338  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L6343  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L6348  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L6357  (RadarDB)
def user_delete(self, user_id: int)
# L6363  (RadarDB)
def user_list(self) -> list[dict]
# L6369  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L6377  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L6404  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L6422  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L6430  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L6436  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L6447  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L6463  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L6495  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L6516  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L6527  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L6542  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L6552  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L6563  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L6574  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L6583  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L6615  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L6628  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L6645  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L6662  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L6674  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6684  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L6696  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L6718  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L6732  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L6744  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L6750  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L6776  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L6801  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L6816  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L6838  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L6855  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6866  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L6877  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L6894  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L6930  (RadarDB)
def close(self)
# L48
def _migration_v40_schema_version_singleton(conn) -> None
# L92
def _migration_v49_seed_config_runtime_from_env(conn) -> None
# L186
def _migration_v55_firing_liveness(conn) -> None
# L256
def _migration_v48_llm_embedding_ledger(conn) -> None
# L310
def _migration_v47_llm_shadow_invocation(conn) -> None
# L350
def _migration_v45b_config_runtime_value(conn) -> None
# L364
def _migration_v45_config_change_log(conn) -> None
# L411
def _migration_v42_llm_routing_overrides(conn) -> None
# L461
def _migration_v41_llm_routing_v10(conn) -> None
# L501
def _migration_v37_decisions_revocation(conn) -> None
# L567
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L611
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L626
def _migration_v36_bg_observer(conn) -> None
# L692
def _migration_v24_generated_country(conn) -> None
```
