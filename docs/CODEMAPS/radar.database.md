# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 5907
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1036–1124)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1049–1052 |  |
| `execute` | L1054–1065 |  |
| `executemany` | L1067–1077 |  |
| `executescript` | L1079–1089 |  |
| `commit` | L1091–1097 |  |
| `rollback` | L1099–1105 |  |
| `close` | L1107–1111 |  |
| `writing` | L1114–1121 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1123–1124 |  |

### `RadarDB` (L1127–5891)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1136–1146 |  |
| `_check_integrity_or_recreate` | L1148–1178 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1180–1202 |  |
| `_ensure_schema` | L1204–1210 |  |
| `_post_baseline_indexes` | L1212–1256 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1258–1265 |  |
| `set_schema_version` | L1267–1273 |  |
| `_run_migrations` | L2274–2309 | Apply any pending schema migrations in order. |
| `baseline_get` | L2312–2321 |  |
| `baseline_set` | L2323–2330 |  |
| `baseline_all` | L2332–2341 |  |
| `baseline_len` | L2343–2345 |  |
| `airspace_get` | L2348–2353 |  |
| `airspace_set` | L2355–2362 |  |
| `airspace_all` | L2364–2368 |  |
| `airspace_len` | L2370–2372 |  |
| `hod_record` | L2375–2393 |  |
| `hod_record_many` | L2395–2413 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2415–2426 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L2428–2435 |  |
| `hod_all_entries` | L2437–2447 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L2449–2456 |  |
| `hod_distinct_hours` | L2458–2465 |  |
| `hod_total_points` | L2467–2471 |  |
| `hod_total_points_theater` | L2473–2479 |  |
| `gdelt_dow_record` | L2482–2496 |  |
| `gdelt_dow_last_bucket` | L2498–2503 |  |
| `gdelt_dow_same_weekday` | L2505–2512 |  |
| `gdelt_dow_total_points` | L2514–2516 |  |
| `ts_append` | L2519–2531 |  |
| `ts_get` | L2533–2538 |  |
| `ts_get_since` | L2540–2545 |  |
| `ts_distinct_theaters` | L2547–2551 |  |
| `ts_total_points` | L2553–2555 |  |
| `sensor_obs_record` | L2561–2578 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L2580–2589 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L2592–2605 |  |
| `series_get` | L2607–2612 |  |
| `alert_append` | L2615–2627 |  |
| `alert_list` | L2629–2634 |  |
| `alert_count` | L2636–2638 |  |
| `alert_clear` | L2640–2643 |  |
| `seq_append` | L2648–2662 |  |
| `seq_exists_since` | L2664–2670 |  |
| `seq_events_since` | L2672–2678 |  |
| `seq_all_events` | L2680–2686 |  |
| `seq_cleanup` | L2688–2691 |  |
| `seq_total` | L2693–2695 |  |
| `seq_distinct_theaters` | L2697–2701 |  |
| `seq_clear` | L2703–2706 |  |
| `tl_observation_append` | L2709–2724 |  |
| `scenario_history_start` | L2726–2731 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L2734–2784 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L2786–2802 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L2804–2820 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L2822–2894 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L2897–2929 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L2931–2970 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L2973–2987 |  |
| `focus_switch_stats` | L2989–3017 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3019–3082 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L3085–3102 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L3104–3125 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L3127–3144 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L3146–3164 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3166–3250 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3253–3283 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3285–3373 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3375–3394 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3396–3405 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3407–3431 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L3433–3452 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L3454–3464 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L3466–3476 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L3478–3490 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L3492–3514 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L3516–3541 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L3543–3592 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L3596–3607 |  |
| `scenario_list` | L3609–3621 |  |
| `scenario_upsert` | L3623–3657 |  |
| `scenario_update_state` | L3667–3693 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L3695–3711 |  |
| `scenario_delete` | L3713–3715 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L3717–3743 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L3745–3755 |  |
| `scenario_change_log` | L3757–3762 |  |
| `threat_append` | L3774–3781 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L3783–3812 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L3814–3823 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L3825–3845 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L3847–3851 |  |
| `threat_last_scoped` | L3853–3859 |  |
| `threat_clear` | L3861–3864 |  |
| `zscore_stats_get` | L3867–3883 |  |
| `zscore_stats_update` | L3885–3917 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L3919–3933 |  |
| `fetch_log_append` | L3936–3946 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L3948–3975 | Per-sensor reliability aggregates over the given time window. |
| `llm_call_log_append` | L3978–3997 | Persist a single LLM call attempt and its downstream queue verdict. |
| `auto_judge_decision_log` | L3999–4025 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4027–4037 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4039–4057 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4059–4082 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4084–4180 | Aggregate stats per caller and overall verdict counts. |
| `ct_log_known_cas` | L4183–4197 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L4199–4220 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L4222–4236 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L4238–4248 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L4250–4275 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L4277–4299 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L4302–4308 |  |
| `sensor_cache_get` | L4310–4317 |  |
| `sensor_cache_all` | L4319–4326 |  |
| `sensor_cache_count` | L4328–4330 |  |
| `noise_excl_add` | L4333–4344 |  |
| `noise_excl_remove` | L4346–4350 |  |
| `noise_excl_list` | L4352–4369 |  |
| `noise_excl_match` | L4371–4378 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L4381–4392 |  |
| `confirmed_threat_list` | L4394–4410 |  |
| `daily_summary_upsert` | L4413–4428 |  |
| `daily_summary_get` | L4430–4443 |  |
| `forecast_log_add` | L4446–4455 |  |
| `forecast_log_resolve` | L4457–4464 |  |
| `forecast_log_get` | L4466–4482 |  |
| `forecast_accuracy_summary` | L4484–4506 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L4509–4550 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L4552–4567 |  |
| `startup_cleanup` | L4570–4573 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L4575–4592 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L4594–4602 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L4604–4608 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L4610–4805 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L4807–4830 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L4833–4854 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L4856–4871 | Load climate events since a given timestamp. |
| `climate_events_prune` | L4873–4877 | Remove climate events older than given timestamp. |
| `intel_upsert` | L4880–4902 | Insert or replace an LLM intel item. |
| `intel_update_status` | L4904–4913 |  |
| `intel_get` | L4915–4923 |  |
| `intel_list` | L4925–4951 |  |
| `intel_find_by_raw_url` | L4953–4964 | Find intel items with matching raw_url across all source_types. |
| `intel_status_counts` | L4966–4993 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L4995–5018 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L5020–5030 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L5032–5043 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L5045–5060 |  |
| `intel_source_get` | L5062–5072 |  |
| `intel_source_upsert` | L5074–5083 |  |
| `intel_source_update_credibility` | L5085–5094 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L5096–5113 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L5115–5122 |  |
| `intel_source_reset_credibility` | L5124–5141 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L5143–5208 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L5210–5255 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L5259–5261 |  |
| `user_get` | L5263–5269 |  |
| `user_get_role` | L5271–5275 |  |
| `user_create` | L5277–5287 |  |
| `user_exists` | L5289–5293 |  |
| `user_update_last_login` | L5295–5298 |  |
| `user_update_role` | L5300–5303 |  |
| `user_update_password` | L5305–5312 |  |
| `user_delete` | L5314–5318 |  |
| `user_list` | L5320–5324 |  |
| `user_settings_get` | L5326–5332 |  |
| `user_settings_create` | L5334–5343 |  |
| `user_settings_update` | L5361–5377 |  |
| `token_revoke` | L5379–5385 |  |
| `token_is_revoked` | L5387–5391 |  |
| `user_get_invalidate_ts` | L5393–5401 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L5404–5418 |  |
| `hidden_signal_list` | L5420–5449 |  |
| `coverage_upsert` | L5452–5471 |  |
| `coverage_list` | L5473–5481 |  |
| `disconf_add` | L5484–5497 |  |
| `disconf_list` | L5499–5507 |  |
| `disconf_retract` | L5509–5517 |  |
| `ach_matrix_create` | L5520–5529 |  |
| `ach_matrix_list` | L5531–5538 |  |
| `ach_matrix_get` | L5540–5570 |  |
| `ach_hypothesis_add` | L5572–5583 |  |
| `ach_evidence_add` | L5585–5600 |  |
| `ach_score_set` | L5602–5616 |  |
| `dissent_add` | L5619–5629 |  |
| `dissent_list` | L5631–5639 |  |
| `dissent_resolve` | L5641–5650 |  |
| `assumption_add` | L5653–5673 |  |
| `_assumption_log` | L5675–5687 |  |
| `assumption_list` | L5689–5699 |  |
| `assumption_get` | L5701–5705 |  |
| `assumption_update` | L5707–5731 |  |
| `assumption_lock` | L5733–5756 |  |
| `assumption_invalidate` | L5758–5771 |  |
| `assumption_change_log` | L5773–5792 |  |
| `premortem_add` | L5795–5810 |  |
| `premortem_list` | L5812–5821 |  |
| `premortem_resolve` | L5823–5831 |  |
| `decision_log` | L5834–5849 |  |
| `decision_list` | L5851–5884 |  |
| `close` | L5887–5891 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v37_decisions_revocation` | L49–112 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L115–156 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L159–171 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L174–237 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L240–267 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1049  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1054  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1067  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1079  (_CooperativeConn)
def executescript(self, sql: str)
# L1091  (_CooperativeConn)
def commit(self)
# L1099  (_CooperativeConn)
def rollback(self)
# L1107  (_CooperativeConn)
def close(self)
# L1114  (_CooperativeConn)
def writing(self)
# L1123  (_CooperativeConn)
def __getattr__(self, name)
# L1136  (RadarDB)
def __init__(self, db_path: str)
# L1148  (RadarDB)
def _check_integrity_or_recreate(self)
# L1180  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1204  (RadarDB)
def _ensure_schema(self)
# L1212  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1258  (RadarDB)
def schema_version(self) -> int
# L1267  (RadarDB)
def set_schema_version(self, ver: int)
# L2274  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2312  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2323  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2332  (RadarDB)
def baseline_all(self) -> dict
# L2343  (RadarDB)
def baseline_len(self) -> int
# L2348  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2355  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2364  (RadarDB)
def airspace_all(self) -> dict
# L2370  (RadarDB)
def airspace_len(self) -> int
# L2375  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2395  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2415  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L2428  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L2437  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L2449  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L2458  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L2467  (RadarDB)
def hod_total_points(self, table: str) -> int
# L2473  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L2482  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L2498  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L2505  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L2514  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L2519  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L2533  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L2540  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L2547  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L2553  (RadarDB)
def ts_total_points(self) -> int
# L2561  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L2580  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L2592  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L2607  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L2615  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L2629  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L2636  (RadarDB)
def alert_count(self) -> int
# L2640  (RadarDB)
def alert_clear(self)
# L2648  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L2664  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L2672  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L2680  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L2688  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L2693  (RadarDB)
def seq_total(self) -> int
# L2697  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L2703  (RadarDB)
def seq_clear(self)
# L2709  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L2726  (RadarDB)
def scenario_history_start(self) -> float | None
# L2734  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L2786  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L2804  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L2822  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L2897  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L2931  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L2973  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L2989  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3019  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L3085  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L3104  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L3127  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L3146  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3166  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3253  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3285  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3375  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3396  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3407  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L3433  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L3454  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L3466  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L3478  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L3492  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L3516  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L3543  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L3596  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L3609  (RadarDB)
def scenario_list(self) -> list[dict]
# L3623  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L3667  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L3695  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L3713  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3717  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3745  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L3757  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L3774  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L3783  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L3814  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L3825  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L3847  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L3853  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L3861  (RadarDB)
def threat_clear(self)
# L3867  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L3885  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L3919  (RadarDB)
def zscore_stats_all(self) -> list
# L3936  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L3948  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L3978  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str)
# L3999  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4027  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4039  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4059  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4084  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4183  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L4199  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L4222  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L4238  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L4250  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L4277  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L4302  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L4310  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L4319  (RadarDB)
def sensor_cache_all(self) -> dict
# L4328  (RadarDB)
def sensor_cache_count(self) -> int
# L4333  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L4346  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L4352  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L4371  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L4381  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L4394  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L4413  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L4430  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L4446  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L4457  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L4466  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L4484  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L4509  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L4552  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L4570  (RadarDB)
def startup_cleanup(self)
# L4575  (RadarDB)
def shutdown(self)
# L4594  (RadarDB)
def wal_checkpoint(self)
# L4604  (RadarDB)
def periodic_cleanup(self)
# L4610  (RadarDB)
def _prune_stale_rows(self) -> dict
# L4807  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L4833  (RadarDB)
def climate_events_save(self, events: list[dict])
# L4856  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L4873  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L4880  (RadarDB)
def intel_upsert(self, item: dict)
# L4904  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L4915  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L4925  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L4953  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L4966  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L4995  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L5020  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L5032  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L5045  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L5062  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L5074  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L5085  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L5096  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L5115  (RadarDB)
def intel_source_list(self) -> list[dict]
# L5124  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L5143  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L5210  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L5259  (RadarDB)
def user_count(self) -> int
# L5263  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L5271  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L5277  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L5289  (RadarDB)
def user_exists(self, username: str) -> bool
# L5295  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L5300  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L5305  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L5314  (RadarDB)
def user_delete(self, user_id: int)
# L5320  (RadarDB)
def user_list(self) -> list[dict]
# L5326  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L5334  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L5361  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L5379  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L5387  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L5393  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L5404  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L5420  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L5452  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L5473  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L5484  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L5499  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L5509  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L5520  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L5531  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L5540  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L5572  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L5585  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L5602  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L5619  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L5631  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5641  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L5653  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L5675  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L5689  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L5701  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L5707  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L5733  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L5758  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L5773  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L5795  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L5812  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5823  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L5834  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L5851  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L5887  (RadarDB)
def close(self)
# L49
def _migration_v37_decisions_revocation(conn) -> None
# L115
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L159
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L174
def _migration_v36_bg_observer(conn) -> None
# L240
def _migration_v24_generated_country(conn) -> None
```
