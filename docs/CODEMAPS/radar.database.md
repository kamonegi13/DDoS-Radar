# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 5524
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L853–941)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L866–869 |  |
| `execute` | L871–882 |  |
| `executemany` | L884–894 |  |
| `executescript` | L896–906 |  |
| `commit` | L908–914 |  |
| `rollback` | L916–922 |  |
| `close` | L924–928 |  |
| `writing` | L931–938 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L940–941 |  |

### `RadarDB` (L944–5508)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L953–963 |  |
| `_check_integrity_or_recreate` | L965–995 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L997–1007 |  |
| `_ensure_schema` | L1009–1015 |  |
| `_post_baseline_indexes` | L1017–1050 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1052–1059 |  |
| `set_schema_version` | L1061–1067 |  |
| `_run_migrations` | L2052–2087 | Apply any pending schema migrations in order. |
| `baseline_get` | L2090–2099 |  |
| `baseline_set` | L2101–2108 |  |
| `baseline_all` | L2110–2119 |  |
| `baseline_len` | L2121–2123 |  |
| `airspace_get` | L2126–2131 |  |
| `airspace_set` | L2133–2140 |  |
| `airspace_all` | L2142–2146 |  |
| `airspace_len` | L2148–2150 |  |
| `hod_record` | L2153–2171 |  |
| `hod_record_many` | L2173–2191 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2193–2204 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L2206–2213 |  |
| `hod_all_entries` | L2215–2225 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L2227–2234 |  |
| `hod_distinct_hours` | L2236–2243 |  |
| `hod_total_points` | L2245–2249 |  |
| `hod_total_points_theater` | L2251–2257 |  |
| `gdelt_dow_record` | L2260–2274 |  |
| `gdelt_dow_last_bucket` | L2276–2281 |  |
| `gdelt_dow_same_weekday` | L2283–2290 |  |
| `gdelt_dow_total_points` | L2292–2294 |  |
| `ts_append` | L2297–2309 |  |
| `ts_get` | L2311–2316 |  |
| `ts_get_since` | L2318–2323 |  |
| `ts_distinct_theaters` | L2325–2329 |  |
| `ts_total_points` | L2331–2333 |  |
| `sensor_obs_record` | L2339–2356 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L2358–2367 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L2370–2383 |  |
| `series_get` | L2385–2390 |  |
| `alert_append` | L2393–2405 |  |
| `alert_list` | L2407–2412 |  |
| `alert_count` | L2414–2416 |  |
| `alert_clear` | L2418–2421 |  |
| `seq_append` | L2426–2440 |  |
| `seq_exists_since` | L2442–2448 |  |
| `seq_events_since` | L2450–2456 |  |
| `seq_all_events` | L2458–2464 |  |
| `seq_cleanup` | L2466–2469 |  |
| `seq_total` | L2471–2473 |  |
| `seq_distinct_theaters` | L2475–2479 |  |
| `seq_clear` | L2481–2484 |  |
| `tl_observation_append` | L2487–2502 |  |
| `scenario_history_start` | L2504–2509 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L2512–2531 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L2533–2549 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L2551–2567 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L2569–2617 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `focus_switch_append` | L2620–2634 |  |
| `focus_switch_stats` | L2636–2664 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L2666–2729 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L2732–2749 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L2751–2772 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L2774–2791 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L2793–2811 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L2813–2897 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L2900–2930 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L2932–3020 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3022–3041 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3043–3052 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3054–3078 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L3080–3099 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L3101–3111 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L3113–3123 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L3125–3137 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L3139–3161 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L3163–3188 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L3190–3239 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L3243–3254 |  |
| `scenario_list` | L3256–3268 |  |
| `scenario_upsert` | L3270–3304 |  |
| `scenario_update_state` | L3314–3340 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L3342–3358 |  |
| `scenario_delete` | L3360–3362 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L3364–3390 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L3392–3402 |  |
| `scenario_change_log` | L3404–3409 |  |
| `threat_append` | L3421–3428 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L3430–3459 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L3461–3470 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L3472–3492 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L3494–3498 |  |
| `threat_last_scoped` | L3500–3506 |  |
| `threat_clear` | L3508–3511 |  |
| `zscore_stats_get` | L3514–3530 |  |
| `zscore_stats_update` | L3532–3564 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L3566–3580 |  |
| `fetch_log_append` | L3583–3593 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L3595–3622 | Per-sensor reliability aggregates over the given time window. |
| `llm_call_log_append` | L3625–3644 | Persist a single LLM call attempt and its downstream queue verdict. |
| `auto_judge_decision_log` | L3646–3672 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L3674–3684 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L3686–3704 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L3706–3729 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L3731–3827 | Aggregate stats per caller and overall verdict counts. |
| `ct_log_known_cas` | L3830–3844 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L3846–3867 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L3869–3883 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L3885–3895 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L3897–3922 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L3924–3946 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L3949–3955 |  |
| `sensor_cache_get` | L3957–3964 |  |
| `sensor_cache_all` | L3966–3973 |  |
| `sensor_cache_count` | L3975–3977 |  |
| `noise_excl_add` | L3980–3991 |  |
| `noise_excl_remove` | L3993–3997 |  |
| `noise_excl_list` | L3999–4016 |  |
| `noise_excl_match` | L4018–4025 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L4028–4039 |  |
| `confirmed_threat_list` | L4041–4057 |  |
| `daily_summary_upsert` | L4060–4075 |  |
| `daily_summary_get` | L4077–4090 |  |
| `forecast_log_add` | L4093–4102 |  |
| `forecast_log_resolve` | L4104–4111 |  |
| `forecast_log_get` | L4113–4129 |  |
| `forecast_accuracy_summary` | L4131–4153 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L4156–4197 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L4199–4214 |  |
| `startup_cleanup` | L4217–4220 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L4222–4239 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L4241–4249 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L4251–4255 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L4257–4441 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L4443–4466 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L4469–4490 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L4492–4507 | Load climate events since a given timestamp. |
| `climate_events_prune` | L4509–4513 | Remove climate events older than given timestamp. |
| `intel_upsert` | L4516–4538 | Insert or replace an LLM intel item. |
| `intel_update_status` | L4540–4549 |  |
| `intel_get` | L4551–4559 |  |
| `intel_list` | L4561–4587 |  |
| `intel_find_by_raw_url` | L4589–4600 | Find intel items with matching raw_url across all source_types. |
| `intel_status_counts` | L4602–4629 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L4631–4654 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L4656–4666 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L4668–4679 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L4681–4696 |  |
| `intel_source_get` | L4698–4708 |  |
| `intel_source_upsert` | L4710–4719 |  |
| `intel_source_update_credibility` | L4721–4730 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L4732–4749 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L4751–4758 |  |
| `intel_source_reset_credibility` | L4760–4777 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L4779–4844 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L4846–4891 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L4895–4897 |  |
| `user_get` | L4899–4905 |  |
| `user_get_role` | L4907–4911 |  |
| `user_create` | L4913–4923 |  |
| `user_exists` | L4925–4929 |  |
| `user_update_last_login` | L4931–4934 |  |
| `user_update_role` | L4936–4939 |  |
| `user_update_password` | L4941–4948 |  |
| `user_delete` | L4950–4954 |  |
| `user_list` | L4956–4960 |  |
| `user_settings_get` | L4962–4968 |  |
| `user_settings_create` | L4970–4979 |  |
| `user_settings_update` | L4984–4994 |  |
| `token_revoke` | L4996–5002 |  |
| `token_is_revoked` | L5004–5008 |  |
| `user_get_invalidate_ts` | L5010–5018 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L5021–5035 |  |
| `hidden_signal_list` | L5037–5066 |  |
| `coverage_upsert` | L5069–5088 |  |
| `coverage_list` | L5090–5098 |  |
| `disconf_add` | L5101–5114 |  |
| `disconf_list` | L5116–5124 |  |
| `disconf_retract` | L5126–5134 |  |
| `ach_matrix_create` | L5137–5146 |  |
| `ach_matrix_list` | L5148–5155 |  |
| `ach_matrix_get` | L5157–5187 |  |
| `ach_hypothesis_add` | L5189–5200 |  |
| `ach_evidence_add` | L5202–5217 |  |
| `ach_score_set` | L5219–5233 |  |
| `dissent_add` | L5236–5246 |  |
| `dissent_list` | L5248–5256 |  |
| `dissent_resolve` | L5258–5267 |  |
| `assumption_add` | L5270–5290 |  |
| `_assumption_log` | L5292–5304 |  |
| `assumption_list` | L5306–5316 |  |
| `assumption_get` | L5318–5322 |  |
| `assumption_update` | L5324–5348 |  |
| `assumption_lock` | L5350–5373 |  |
| `assumption_invalidate` | L5375–5388 |  |
| `assumption_change_log` | L5390–5409 |  |
| `premortem_add` | L5412–5427 |  |
| `premortem_list` | L5429–5438 |  |
| `premortem_resolve` | L5440–5448 |  |
| `decision_log` | L5451–5466 |  |
| `decision_list` | L5468–5501 |  |
| `close` | L5504–5508 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v24_generated_country` | L49–76 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L866  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L871  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L884  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L896  (_CooperativeConn)
def executescript(self, sql: str)
# L908  (_CooperativeConn)
def commit(self)
# L916  (_CooperativeConn)
def rollback(self)
# L924  (_CooperativeConn)
def close(self)
# L931  (_CooperativeConn)
def writing(self)
# L940  (_CooperativeConn)
def __getattr__(self, name)
# L953  (RadarDB)
def __init__(self, db_path: str)
# L965  (RadarDB)
def _check_integrity_or_recreate(self)
# L997  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1009  (RadarDB)
def _ensure_schema(self)
# L1017  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1052  (RadarDB)
def schema_version(self) -> int
# L1061  (RadarDB)
def set_schema_version(self, ver: int)
# L2052  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2090  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2101  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2110  (RadarDB)
def baseline_all(self) -> dict
# L2121  (RadarDB)
def baseline_len(self) -> int
# L2126  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2133  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2142  (RadarDB)
def airspace_all(self) -> dict
# L2148  (RadarDB)
def airspace_len(self) -> int
# L2153  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2173  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2193  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L2206  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L2215  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L2227  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L2236  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L2245  (RadarDB)
def hod_total_points(self, table: str) -> int
# L2251  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L2260  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L2276  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L2283  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L2292  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L2297  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L2311  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L2318  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L2325  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L2331  (RadarDB)
def ts_total_points(self) -> int
# L2339  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L2358  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L2370  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L2385  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L2393  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L2407  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L2414  (RadarDB)
def alert_count(self) -> int
# L2418  (RadarDB)
def alert_clear(self)
# L2426  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L2442  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L2450  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L2458  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L2466  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L2471  (RadarDB)
def seq_total(self) -> int
# L2475  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L2481  (RadarDB)
def seq_clear(self)
# L2487  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L2504  (RadarDB)
def scenario_history_start(self) -> float | None
# L2512  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float) -> None
# L2533  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L2551  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L2569  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L2620  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L2636  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L2666  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L2732  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L2751  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L2774  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L2793  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L2813  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L2900  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L2932  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3022  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3043  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3054  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L3080  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L3101  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L3113  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L3125  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L3139  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L3163  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L3190  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L3243  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L3256  (RadarDB)
def scenario_list(self) -> list[dict]
# L3270  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L3314  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L3342  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L3360  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3364  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3392  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L3404  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L3421  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L3430  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L3461  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L3472  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L3494  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L3500  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L3508  (RadarDB)
def threat_clear(self)
# L3514  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L3532  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L3566  (RadarDB)
def zscore_stats_all(self) -> list
# L3583  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L3595  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L3625  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str)
# L3646  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L3674  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L3686  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L3706  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L3731  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L3830  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L3846  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L3869  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L3885  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L3897  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L3924  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L3949  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L3957  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L3966  (RadarDB)
def sensor_cache_all(self) -> dict
# L3975  (RadarDB)
def sensor_cache_count(self) -> int
# L3980  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L3993  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L3999  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L4018  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L4028  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L4041  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L4060  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L4077  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L4093  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L4104  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L4113  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L4131  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L4156  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L4199  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L4217  (RadarDB)
def startup_cleanup(self)
# L4222  (RadarDB)
def shutdown(self)
# L4241  (RadarDB)
def wal_checkpoint(self)
# L4251  (RadarDB)
def periodic_cleanup(self)
# L4257  (RadarDB)
def _prune_stale_rows(self) -> dict
# L4443  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L4469  (RadarDB)
def climate_events_save(self, events: list[dict])
# L4492  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L4509  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L4516  (RadarDB)
def intel_upsert(self, item: dict)
# L4540  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L4551  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L4561  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L4589  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L4602  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L4631  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L4656  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L4668  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L4681  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L4698  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L4710  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L4721  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L4732  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L4751  (RadarDB)
def intel_source_list(self) -> list[dict]
# L4760  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L4779  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L4846  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L4895  (RadarDB)
def user_count(self) -> int
# L4899  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L4907  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L4913  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L4925  (RadarDB)
def user_exists(self, username: str) -> bool
# L4931  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L4936  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L4941  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L4950  (RadarDB)
def user_delete(self, user_id: int)
# L4956  (RadarDB)
def user_list(self) -> list[dict]
# L4962  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L4970  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L4984  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L4996  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L5004  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L5010  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L5021  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L5037  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L5069  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L5090  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L5101  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L5116  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L5126  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L5137  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L5148  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L5157  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L5189  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L5202  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L5219  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L5236  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L5248  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5258  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L5270  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L5292  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L5306  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L5318  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L5324  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L5350  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L5375  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L5390  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L5412  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L5429  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5440  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L5451  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L5468  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L5504  (RadarDB)
def close(self)
# L49
def _migration_v24_generated_country(conn) -> None
```
