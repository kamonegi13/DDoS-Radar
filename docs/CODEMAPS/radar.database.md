# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 7074
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1667–1755)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1680–1683 |  |
| `execute` | L1685–1696 |  |
| `executemany` | L1698–1708 |  |
| `executescript` | L1710–1720 |  |
| `commit` | L1722–1728 |  |
| `rollback` | L1730–1736 |  |
| `close` | L1738–1742 |  |
| `writing` | L1745–1752 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1754–1755 |  |

### `RadarDB` (L1758–7066)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1767–1777 |  |
| `_check_integrity_or_recreate` | L1779–1809 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1811–1833 |  |
| `_ensure_schema` | L1835–1841 |  |
| `_post_baseline_indexes` | L1843–1904 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1906–1913 |  |
| `set_schema_version` | L1915–1938 |  |
| `_run_migrations` | L3072–3107 | Apply any pending schema migrations in order. |
| `baseline_get` | L3110–3119 |  |
| `baseline_set` | L3121–3128 |  |
| `baseline_all` | L3130–3139 |  |
| `baseline_len` | L3141–3143 |  |
| `airspace_get` | L3146–3151 |  |
| `airspace_set` | L3153–3160 |  |
| `airspace_all` | L3162–3166 |  |
| `airspace_len` | L3168–3170 |  |
| `hod_record` | L3173–3191 |  |
| `hod_record_many` | L3193–3211 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L3213–3224 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L3226–3233 |  |
| `hod_all_entries` | L3235–3245 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L3247–3254 |  |
| `hod_distinct_hours` | L3256–3263 |  |
| `hod_total_points` | L3265–3269 |  |
| `hod_total_points_theater` | L3271–3277 |  |
| `gdelt_dow_record` | L3280–3294 |  |
| `gdelt_dow_last_bucket` | L3296–3301 |  |
| `gdelt_dow_same_weekday` | L3303–3310 |  |
| `gdelt_dow_total_points` | L3312–3314 |  |
| `ts_append` | L3317–3329 |  |
| `ts_get` | L3331–3336 |  |
| `ts_get_since` | L3338–3343 |  |
| `ts_distinct_theaters` | L3345–3349 |  |
| `ts_total_points` | L3351–3353 |  |
| `sensor_obs_record` | L3362–3379 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L3381–3390 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `flag_state_upsert` | L3393–3407 | Record one evaluation of one detection flag. |
| `flag_record_evaluation` | L3427–3455 | Persist one sensor's whole flag evaluation in ONE transaction. |
| `flag_state_all` | L3457–3463 | Every (sensor, flag) state row. |
| `flag_state_for` | L3465–3472 | State rows for a single sensor (cheap path for detection_health). |
| `flag_fire_append` | L3474–3492 | Append one fire event and opportunistically prune > ttl_sec. |
| `flag_fire_counts_since` | L3494–3501 | {(sensor, flag_id): fire count} since `since_ts`, in one query. |
| `l5_job_get` | L3503–3508 |  |
| `l5_job_set` | L3510–3521 |  |
| `l5_check_append` | L3523–3546 | Append one verification result (S5-VERIF-016). Never updates. |
| `l5_check_latest` | L3548–3555 |  |
| `l5_check_last_for_target` | L3557–3564 | Most recent row for one target — backs first_detected_at carry-forward. |
| `_read_stat_row` | L3567–3574 |  |
| `config_read_stats_merge` | L3576–3609 | Merge in-process config read counters into the persisted table. |
| `config_read_stats_note_considered` | L3611–3629 | Start the observation window for keys not yet tracked. |
| `config_read_stats_all` | L3631–3642 | {config key: {first_considered_at, first_read_at, last_read_at, |
| `series_append` | L3645–3658 |  |
| `series_get` | L3660–3665 |  |
| `alert_append` | L3668–3680 |  |
| `alert_list` | L3682–3687 |  |
| `alert_count` | L3689–3691 |  |
| `alert_clear` | L3693–3696 |  |
| `seq_append` | L3701–3715 |  |
| `seq_exists_since` | L3717–3723 |  |
| `seq_events_since` | L3725–3731 |  |
| `seq_all_events` | L3733–3739 |  |
| `seq_cleanup` | L3741–3744 |  |
| `seq_total` | L3746–3748 |  |
| `seq_distinct_theaters` | L3750–3754 |  |
| `seq_clear` | L3756–3759 |  |
| `tl_observation_append` | L3762–3777 |  |
| `scenario_history_start` | L3779–3784 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L3787–3837 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L3839–3855 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L3857–3873 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L3875–3947 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L3950–3982 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3984–4023 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L4026–4040 |  |
| `focus_switch_stats` | L4042–4070 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L4072–4135 | Extended focus_switch_stats with delta distribution and per-scenario |
| `intel_count_24h_by_scenario` | L4142–4161 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L4163–4172 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L4174–4198 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L4200–4219 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L4221–4231 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L4233–4243 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L4245–4257 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L4259–4281 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L4283–4308 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L4310–4359 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L4363–4374 |  |
| `scenario_list` | L4376–4388 |  |
| `scenario_upsert` | L4390–4424 |  |
| `scenario_update_state` | L4434–4460 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L4462–4478 |  |
| `scenario_delete` | L4480–4482 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L4484–4510 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L4512–4522 |  |
| `scenario_change_log` | L4524–4529 |  |
| `threat_append` | L4541–4548 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L4550–4579 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L4581–4590 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L4592–4612 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L4614–4618 |  |
| `threat_last_scoped` | L4620–4626 |  |
| `threat_clear` | L4628–4631 |  |
| `zscore_stats_get` | L4634–4650 |  |
| `zscore_stats_update` | L4652–4684 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L4686–4700 |  |
| `fetch_log_append` | L4703–4713 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4715–4742 | Per-sensor reliability aggregates over the given time window. |
| `config_change_log_append` | L4745–4783 | Append a row to the unified config audit ledger. Returns the new |
| `config_change_log_recent` | L4785–4817 | Return recent config change rows for the Audit UI. Optional domain |
| `config_change_log_domains` | L4819–4832 | Return distinct domains observed in the audit ledger over the |
| `llm_call_log_append` | L4835–4870 | Persist a single LLM call attempt and its downstream queue verdict. |
| `llm_shadow_invocation_append` | L4872–4904 | Phase 9.2 C4 — record a SHADOW_DUAL parallel invocation. Linked |
| `auto_judge_decision_log` | L4906–4932 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4934–4944 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4946–4964 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4966–4989 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4991–5087 | Aggregate stats per caller and overall verdict counts. |
| `llm_routing_stats` | L5089–5329 | Phase 8 (LLM survey v10) — per-model + per-use_case rollups. |
| `llm_embedding_stats` | L5331–5386 | Phase 9.2 C9 — embedding pipeline rollups for the LLM Console |
| `ct_log_known_cas` | L5389–5403 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L5405–5426 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L5428–5442 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L5444–5454 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L5456–5481 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L5483–5505 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L5508–5514 |  |
| `sensor_cache_get` | L5516–5523 |  |
| `sensor_cache_all` | L5525–5532 |  |
| `sensor_cache_count` | L5534–5536 |  |
| `noise_excl_add` | L5539–5550 |  |
| `noise_excl_remove` | L5552–5556 |  |
| `noise_excl_list` | L5558–5575 |  |
| `noise_excl_match` | L5577–5584 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L5587–5598 |  |
| `confirmed_threat_list` | L5600–5616 |  |
| `daily_summary_upsert` | L5619–5634 |  |
| `daily_summary_get` | L5636–5649 |  |
| `cooccurrence_update` | L5655–5696 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L5698–5713 |  |
| `startup_cleanup` | L5716–5719 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L5721–5738 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L5740–5748 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L5750–5754 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L5756–5965 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L5967–5990 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L5993–6014 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L6016–6031 | Load climate events since a given timestamp. |
| `climate_events_prune` | L6033–6037 | Remove climate events older than given timestamp. |
| `intel_upsert` | L6040–6062 | Insert or replace an LLM intel item. |
| `intel_update_status` | L6064–6073 |  |
| `intel_get` | L6075–6083 |  |
| `intel_list` | L6085–6111 |  |
| `intel_find_by_raw_url` | L6113–6124 | Find intel items with matching raw_url across all source_types. |
| `intel_recent_by_source` | L6126–6139 | Phase 9.2 C8 — recent intel rows for the same source_type, used |
| `intel_status_counts` | L6141–6168 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L6170–6193 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L6195–6205 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L6207–6218 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L6220–6235 |  |
| `intel_source_get` | L6237–6247 |  |
| `intel_source_upsert` | L6249–6258 |  |
| `intel_source_update_credibility` | L6260–6269 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L6271–6288 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L6290–6297 |  |
| `intel_source_reset_credibility` | L6299–6316 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L6318–6383 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L6385–6430 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L6434–6436 |  |
| `user_get` | L6438–6444 |  |
| `user_get_role` | L6446–6450 |  |
| `user_create` | L6452–6462 |  |
| `user_exists` | L6464–6468 |  |
| `user_update_last_login` | L6470–6473 |  |
| `user_update_role` | L6475–6478 |  |
| `user_update_password` | L6480–6487 |  |
| `user_delete` | L6489–6493 |  |
| `user_list` | L6495–6499 |  |
| `user_settings_get` | L6501–6507 |  |
| `user_settings_create` | L6509–6518 |  |
| `user_settings_update` | L6536–6552 |  |
| `token_revoke` | L6554–6560 |  |
| `token_is_revoked` | L6562–6566 |  |
| `user_get_invalidate_ts` | L6568–6576 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L6579–6593 |  |
| `hidden_signal_list` | L6595–6624 |  |
| `coverage_upsert` | L6627–6646 |  |
| `coverage_list` | L6648–6656 |  |
| `disconf_add` | L6659–6672 |  |
| `disconf_list` | L6674–6682 |  |
| `disconf_retract` | L6684–6692 |  |
| `ach_matrix_create` | L6695–6704 |  |
| `ach_matrix_list` | L6706–6713 |  |
| `ach_matrix_get` | L6715–6745 |  |
| `ach_hypothesis_add` | L6747–6758 |  |
| `ach_evidence_add` | L6760–6775 |  |
| `ach_score_set` | L6777–6791 |  |
| `dissent_add` | L6794–6804 |  |
| `dissent_list` | L6806–6814 |  |
| `dissent_resolve` | L6816–6825 |  |
| `assumption_add` | L6828–6848 |  |
| `_assumption_log` | L6850–6862 |  |
| `assumption_list` | L6864–6874 |  |
| `assumption_get` | L6876–6880 |  |
| `assumption_update` | L6882–6906 |  |
| `assumption_lock` | L6908–6931 |  |
| `assumption_invalidate` | L6933–6946 |  |
| `assumption_change_log` | L6948–6967 |  |
| `premortem_add` | L6970–6985 |  |
| `premortem_list` | L6987–6996 |  |
| `premortem_resolve` | L6998–7006 |  |
| `decision_log` | L7009–7024 |  |
| `decision_list` | L7026–7059 |  |
| `close` | L7062–7066 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L48–89 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v49_seed_config_runtime_from_env` | L92–183 | R4 — seed config_runtime_value from running os.environ for every |
| `_migration_v55_firing_liveness` | L186–253 | WP-1.1 (v3 Phase 1) — L5 firing-liveness monitoring tables. |
| `_migration_v56_config_read_stats` | L256–288 | WP-1.2 (v3 Phase 1) — persisted config read tracker. |
| `_migration_v48_llm_embedding_ledger` | L291–342 | Phase 9.2 C7 — per-call ledger for the embedding pipeline. |
| `_migration_v47_llm_shadow_invocation` | L345–382 | Phase 9.2 C4 — dedicated table for SHADOW_DUAL parallel invocations. |
| `_migration_v45b_config_runtime_value` | L385–396 | Phase 9.1 — runtime override values for declarative config keys. |
| `_migration_v45_config_change_log` | L399–443 | Phase 9.1 (Foundation) — unified audit ledger for every analyst-driven |
| `_migration_v42_llm_routing_overrides` | L446–493 | Phase 8 (LLM survey v10) — analyst-driven routing overrides. |
| `_migration_v41_llm_routing_v10` | L496–533 | Phase 8 (LLM survey v10) — capture per-call thinking trace, the |
| `_migration_v37_decisions_revocation` | L536–599 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L602–643 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L646–658 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L661–724 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L727–754 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1680  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1685  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1698  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1710  (_CooperativeConn)
def executescript(self, sql: str)
# L1722  (_CooperativeConn)
def commit(self)
# L1730  (_CooperativeConn)
def rollback(self)
# L1738  (_CooperativeConn)
def close(self)
# L1745  (_CooperativeConn)
def writing(self)
# L1754  (_CooperativeConn)
def __getattr__(self, name)
# L1767  (RadarDB)
def __init__(self, db_path: str)
# L1779  (RadarDB)
def _check_integrity_or_recreate(self)
# L1811  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1835  (RadarDB)
def _ensure_schema(self)
# L1843  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1906  (RadarDB)
def schema_version(self) -> int
# L1915  (RadarDB)
def set_schema_version(self, ver: int)
# L3072  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L3110  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L3121  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L3130  (RadarDB)
def baseline_all(self) -> dict
# L3141  (RadarDB)
def baseline_len(self) -> int
# L3146  (RadarDB)
def airspace_get(self, code: str) -> dict
# L3153  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L3162  (RadarDB)
def airspace_all(self) -> dict
# L3168  (RadarDB)
def airspace_len(self) -> int
# L3173  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L3193  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L3213  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L3226  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L3235  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L3247  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L3256  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L3265  (RadarDB)
def hod_total_points(self, table: str) -> int
# L3271  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L3280  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L3296  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L3303  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L3312  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L3317  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L3331  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L3338  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L3345  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L3351  (RadarDB)
def ts_total_points(self) -> int
# L3362  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, ttl_sec: float, baseline: float | None, status: str | None) -> None
# L3381  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L3393  (RadarDB)
def flag_state_upsert(self, sensor: str, flag_id: str, ts: float, fired: bool) -> None
# L3427  (RadarDB)
def flag_record_evaluation(self, sensor: str, fired: dict[str, bool], ts: float, ttl_sec: float) -> None
# L3457  (RadarDB)
def flag_state_all(self) -> list[dict]
# L3465  (RadarDB)
def flag_state_for(self, sensor: str) -> list[dict]
# L3474  (RadarDB)
def flag_fire_append(self, sensor: str, flag_id: str, ts: float, ttl_sec: float) -> None
# L3494  (RadarDB)
def flag_fire_counts_since(self, since_ts: float) -> dict[tuple, int]
# L3503  (RadarDB)
def l5_job_get(self, job_id: str) -> dict | None
# L3510  (RadarDB)
def l5_job_set(self, job_id: str, last_run_at: float, next_run_at: float) -> None
# L3523  (RadarDB)
def l5_check_append(self, ts: float, check_id: str, target: str, verdict: str, measured_json: str | None, expected_json: str | None, first_detected_at: float | None, ttl_sec: float | None) -> None
# L3548  (RadarDB)
def l5_check_latest(self, check_id: str, limit: int) -> list[dict]
# L3557  (RadarDB)
def l5_check_last_for_target(self, check_id: str, target: str) -> dict | None
# L3567  (RadarDB)
def _read_stat_row(key: str, entry: dict, ts: float) -> tuple
# L3576  (RadarDB)
def config_read_stats_merge(self, entries: dict, now: float | None) -> int
# L3611  (RadarDB)
def config_read_stats_note_considered(self, keys, now: float) -> int
# L3631  (RadarDB)
def config_read_stats_all(self) -> dict
# L3645  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L3660  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L3668  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L3682  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L3689  (RadarDB)
def alert_count(self) -> int
# L3693  (RadarDB)
def alert_clear(self)
# L3701  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L3717  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L3725  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L3733  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L3741  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L3746  (RadarDB)
def seq_total(self) -> int
# L3750  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L3756  (RadarDB)
def seq_clear(self)
# L3762  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L3779  (RadarDB)
def scenario_history_start(self) -> float | None
# L3787  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L3839  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L3857  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L3875  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L3950  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3984  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L4026  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L4042  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L4072  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L4142  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L4163  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L4174  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L4200  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L4221  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L4233  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L4245  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L4259  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L4283  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L4310  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L4363  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L4376  (RadarDB)
def scenario_list(self) -> list[dict]
# L4390  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L4434  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L4462  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L4480  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4484  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4512  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L4524  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L4541  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L4550  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L4581  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L4592  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L4614  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L4620  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L4628  (RadarDB)
def threat_clear(self)
# L4634  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L4652  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L4686  (RadarDB)
def zscore_stats_all(self) -> list
# L4703  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4715  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4745  (RadarDB)
def config_change_log_append(self, *, domain: str, config_key: str, old_value: 'object', new_value: 'object', changed_by: str, reason: 'str | None', request_id: 'str | None') -> int
# L4785  (RadarDB)
def config_change_log_recent(self, *, hours: int, domain: 'str | None', limit: int) -> list
# L4819  (RadarDB)
def config_change_log_domains(self, *, hours: int) -> list
# L4835  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str, use_case: 'str | None', shadow_model_choice: 'str | None', thinking_trace: 'str | None')
# L4872  (RadarDB)
def llm_shadow_invocation_append(self, *, primary_call_id: int, model: str, duration_ms: int, outcome: str, response_text: 'str | None', confidence: 'float | None', prompt_sha256: 'str | None', error: 'str | None') -> int
# L4906  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4934  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4946  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4966  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4991  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L5089  (RadarDB)
def llm_routing_stats(self, hours: int) -> dict
# L5331  (RadarDB)
def llm_embedding_stats(self, hours: int) -> dict
# L5389  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L5405  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L5428  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L5444  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L5456  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L5483  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L5508  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L5516  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L5525  (RadarDB)
def sensor_cache_all(self) -> dict
# L5534  (RadarDB)
def sensor_cache_count(self) -> int
# L5539  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L5552  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L5558  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L5577  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L5587  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L5600  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L5619  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L5636  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L5655  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L5698  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L5716  (RadarDB)
def startup_cleanup(self)
# L5721  (RadarDB)
def shutdown(self)
# L5740  (RadarDB)
def wal_checkpoint(self)
# L5750  (RadarDB)
def periodic_cleanup(self)
# L5756  (RadarDB)
def _prune_stale_rows(self) -> dict
# L5967  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L5993  (RadarDB)
def climate_events_save(self, events: list[dict])
# L6016  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L6033  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L6040  (RadarDB)
def intel_upsert(self, item: dict)
# L6064  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L6075  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L6085  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L6113  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L6126  (RadarDB)
def intel_recent_by_source(self, source_type: str, since_ts: float, limit: int) -> list[dict]
# L6141  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L6170  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L6195  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L6207  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L6220  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L6237  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L6249  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L6260  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L6271  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L6290  (RadarDB)
def intel_source_list(self) -> list[dict]
# L6299  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L6318  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L6385  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L6434  (RadarDB)
def user_count(self) -> int
# L6438  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L6446  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L6452  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L6464  (RadarDB)
def user_exists(self, username: str) -> bool
# L6470  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L6475  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L6480  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L6489  (RadarDB)
def user_delete(self, user_id: int)
# L6495  (RadarDB)
def user_list(self) -> list[dict]
# L6501  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L6509  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L6536  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L6554  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L6562  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L6568  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L6579  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L6595  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L6627  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L6648  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L6659  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L6674  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L6684  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L6695  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L6706  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L6715  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L6747  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L6760  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L6777  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L6794  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L6806  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6816  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L6828  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L6850  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L6864  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L6876  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L6882  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L6908  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L6933  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L6948  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L6970  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L6987  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6998  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L7009  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L7026  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L7062  (RadarDB)
def close(self)
# L48
def _migration_v40_schema_version_singleton(conn) -> None
# L92
def _migration_v49_seed_config_runtime_from_env(conn) -> None
# L186
def _migration_v55_firing_liveness(conn) -> None
# L256
def _migration_v56_config_read_stats(conn) -> None
# L291
def _migration_v48_llm_embedding_ledger(conn) -> None
# L345
def _migration_v47_llm_shadow_invocation(conn) -> None
# L385
def _migration_v45b_config_runtime_value(conn) -> None
# L399
def _migration_v45_config_change_log(conn) -> None
# L446
def _migration_v42_llm_routing_overrides(conn) -> None
# L496
def _migration_v41_llm_routing_v10(conn) -> None
# L536
def _migration_v37_decisions_revocation(conn) -> None
# L602
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L646
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L661
def _migration_v36_bg_observer(conn) -> None
# L727
def _migration_v24_generated_country(conn) -> None
```
