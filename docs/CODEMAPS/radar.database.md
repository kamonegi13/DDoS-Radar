# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 5737
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L919–1007)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L932–935 |  |
| `execute` | L937–948 |  |
| `executemany` | L950–960 |  |
| `executescript` | L962–972 |  |
| `commit` | L974–980 |  |
| `rollback` | L982–988 |  |
| `close` | L990–994 |  |
| `writing` | L997–1004 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1006–1007 |  |

### `RadarDB` (L1010–5721)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1019–1029 |  |
| `_check_integrity_or_recreate` | L1031–1061 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1063–1073 |  |
| `_ensure_schema` | L1075–1081 |  |
| `_post_baseline_indexes` | L1083–1127 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1129–1136 |  |
| `set_schema_version` | L1138–1144 |  |
| `_run_migrations` | L2132–2167 | Apply any pending schema migrations in order. |
| `baseline_get` | L2170–2179 |  |
| `baseline_set` | L2181–2188 |  |
| `baseline_all` | L2190–2199 |  |
| `baseline_len` | L2201–2203 |  |
| `airspace_get` | L2206–2211 |  |
| `airspace_set` | L2213–2220 |  |
| `airspace_all` | L2222–2226 |  |
| `airspace_len` | L2228–2230 |  |
| `hod_record` | L2233–2251 |  |
| `hod_record_many` | L2253–2271 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2273–2284 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L2286–2293 |  |
| `hod_all_entries` | L2295–2305 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L2307–2314 |  |
| `hod_distinct_hours` | L2316–2323 |  |
| `hod_total_points` | L2325–2329 |  |
| `hod_total_points_theater` | L2331–2337 |  |
| `gdelt_dow_record` | L2340–2354 |  |
| `gdelt_dow_last_bucket` | L2356–2361 |  |
| `gdelt_dow_same_weekday` | L2363–2370 |  |
| `gdelt_dow_total_points` | L2372–2374 |  |
| `ts_append` | L2377–2389 |  |
| `ts_get` | L2391–2396 |  |
| `ts_get_since` | L2398–2403 |  |
| `ts_distinct_theaters` | L2405–2409 |  |
| `ts_total_points` | L2411–2413 |  |
| `sensor_obs_record` | L2419–2436 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L2438–2447 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L2450–2463 |  |
| `series_get` | L2465–2470 |  |
| `alert_append` | L2473–2485 |  |
| `alert_list` | L2487–2492 |  |
| `alert_count` | L2494–2496 |  |
| `alert_clear` | L2498–2501 |  |
| `seq_append` | L2506–2520 |  |
| `seq_exists_since` | L2522–2528 |  |
| `seq_events_since` | L2530–2536 |  |
| `seq_all_events` | L2538–2544 |  |
| `seq_cleanup` | L2546–2549 |  |
| `seq_total` | L2551–2553 |  |
| `seq_distinct_theaters` | L2555–2559 |  |
| `seq_clear` | L2561–2564 |  |
| `tl_observation_append` | L2567–2582 |  |
| `scenario_history_start` | L2584–2589 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L2592–2633 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L2635–2651 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L2653–2669 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L2671–2743 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L2746–2778 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L2780–2819 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L2822–2836 |  |
| `focus_switch_stats` | L2838–2866 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L2868–2931 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L2934–2951 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L2953–2974 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L2976–2993 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L2995–3013 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3015–3099 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3102–3132 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3134–3222 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3224–3243 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3245–3254 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3256–3280 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L3282–3301 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L3303–3313 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L3315–3325 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L3327–3339 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L3341–3363 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L3365–3390 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L3392–3441 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L3445–3456 |  |
| `scenario_list` | L3458–3470 |  |
| `scenario_upsert` | L3472–3506 |  |
| `scenario_update_state` | L3516–3542 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L3544–3560 |  |
| `scenario_delete` | L3562–3564 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L3566–3592 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L3594–3604 |  |
| `scenario_change_log` | L3606–3611 |  |
| `threat_append` | L3623–3630 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L3632–3661 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L3663–3672 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L3674–3694 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L3696–3700 |  |
| `threat_last_scoped` | L3702–3708 |  |
| `threat_clear` | L3710–3713 |  |
| `zscore_stats_get` | L3716–3732 |  |
| `zscore_stats_update` | L3734–3766 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L3768–3782 |  |
| `fetch_log_append` | L3785–3795 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L3797–3824 | Per-sensor reliability aggregates over the given time window. |
| `llm_call_log_append` | L3827–3846 | Persist a single LLM call attempt and its downstream queue verdict. |
| `auto_judge_decision_log` | L3848–3874 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L3876–3886 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L3888–3906 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L3908–3931 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L3933–4029 | Aggregate stats per caller and overall verdict counts. |
| `ct_log_known_cas` | L4032–4046 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L4048–4069 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L4071–4085 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L4087–4097 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L4099–4124 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L4126–4148 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L4151–4157 |  |
| `sensor_cache_get` | L4159–4166 |  |
| `sensor_cache_all` | L4168–4175 |  |
| `sensor_cache_count` | L4177–4179 |  |
| `noise_excl_add` | L4182–4193 |  |
| `noise_excl_remove` | L4195–4199 |  |
| `noise_excl_list` | L4201–4218 |  |
| `noise_excl_match` | L4220–4227 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L4230–4241 |  |
| `confirmed_threat_list` | L4243–4259 |  |
| `daily_summary_upsert` | L4262–4277 |  |
| `daily_summary_get` | L4279–4292 |  |
| `forecast_log_add` | L4295–4304 |  |
| `forecast_log_resolve` | L4306–4313 |  |
| `forecast_log_get` | L4315–4331 |  |
| `forecast_accuracy_summary` | L4333–4355 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L4358–4399 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L4401–4416 |  |
| `startup_cleanup` | L4419–4422 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L4424–4441 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L4443–4451 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L4453–4457 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L4459–4654 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L4656–4679 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L4682–4703 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L4705–4720 | Load climate events since a given timestamp. |
| `climate_events_prune` | L4722–4726 | Remove climate events older than given timestamp. |
| `intel_upsert` | L4729–4751 | Insert or replace an LLM intel item. |
| `intel_update_status` | L4753–4762 |  |
| `intel_get` | L4764–4772 |  |
| `intel_list` | L4774–4800 |  |
| `intel_find_by_raw_url` | L4802–4813 | Find intel items with matching raw_url across all source_types. |
| `intel_status_counts` | L4815–4842 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L4844–4867 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L4869–4879 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L4881–4892 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L4894–4909 |  |
| `intel_source_get` | L4911–4921 |  |
| `intel_source_upsert` | L4923–4932 |  |
| `intel_source_update_credibility` | L4934–4943 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L4945–4962 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L4964–4971 |  |
| `intel_source_reset_credibility` | L4973–4990 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L4992–5057 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L5059–5104 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L5108–5110 |  |
| `user_get` | L5112–5118 |  |
| `user_get_role` | L5120–5124 |  |
| `user_create` | L5126–5136 |  |
| `user_exists` | L5138–5142 |  |
| `user_update_last_login` | L5144–5147 |  |
| `user_update_role` | L5149–5152 |  |
| `user_update_password` | L5154–5161 |  |
| `user_delete` | L5163–5167 |  |
| `user_list` | L5169–5173 |  |
| `user_settings_get` | L5175–5181 |  |
| `user_settings_create` | L5183–5192 |  |
| `user_settings_update` | L5197–5207 |  |
| `token_revoke` | L5209–5215 |  |
| `token_is_revoked` | L5217–5221 |  |
| `user_get_invalidate_ts` | L5223–5231 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L5234–5248 |  |
| `hidden_signal_list` | L5250–5279 |  |
| `coverage_upsert` | L5282–5301 |  |
| `coverage_list` | L5303–5311 |  |
| `disconf_add` | L5314–5327 |  |
| `disconf_list` | L5329–5337 |  |
| `disconf_retract` | L5339–5347 |  |
| `ach_matrix_create` | L5350–5359 |  |
| `ach_matrix_list` | L5361–5368 |  |
| `ach_matrix_get` | L5370–5400 |  |
| `ach_hypothesis_add` | L5402–5413 |  |
| `ach_evidence_add` | L5415–5430 |  |
| `ach_score_set` | L5432–5446 |  |
| `dissent_add` | L5449–5459 |  |
| `dissent_list` | L5461–5469 |  |
| `dissent_resolve` | L5471–5480 |  |
| `assumption_add` | L5483–5503 |  |
| `_assumption_log` | L5505–5517 |  |
| `assumption_list` | L5519–5529 |  |
| `assumption_get` | L5531–5535 |  |
| `assumption_update` | L5537–5561 |  |
| `assumption_lock` | L5563–5586 |  |
| `assumption_invalidate` | L5588–5601 |  |
| `assumption_change_log` | L5603–5622 |  |
| `premortem_add` | L5625–5640 |  |
| `premortem_list` | L5642–5651 |  |
| `premortem_resolve` | L5653–5661 |  |
| `decision_log` | L5664–5679 |  |
| `decision_list` | L5681–5714 |  |
| `close` | L5717–5721 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v36_bg_observer` | L49–112 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L115–142 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L932  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L937  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L950  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L962  (_CooperativeConn)
def executescript(self, sql: str)
# L974  (_CooperativeConn)
def commit(self)
# L982  (_CooperativeConn)
def rollback(self)
# L990  (_CooperativeConn)
def close(self)
# L997  (_CooperativeConn)
def writing(self)
# L1006  (_CooperativeConn)
def __getattr__(self, name)
# L1019  (RadarDB)
def __init__(self, db_path: str)
# L1031  (RadarDB)
def _check_integrity_or_recreate(self)
# L1063  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1075  (RadarDB)
def _ensure_schema(self)
# L1083  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1129  (RadarDB)
def schema_version(self) -> int
# L1138  (RadarDB)
def set_schema_version(self, ver: int)
# L2132  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2170  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2181  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2190  (RadarDB)
def baseline_all(self) -> dict
# L2201  (RadarDB)
def baseline_len(self) -> int
# L2206  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2213  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2222  (RadarDB)
def airspace_all(self) -> dict
# L2228  (RadarDB)
def airspace_len(self) -> int
# L2233  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2253  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2273  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L2286  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L2295  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L2307  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L2316  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L2325  (RadarDB)
def hod_total_points(self, table: str) -> int
# L2331  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L2340  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L2356  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L2363  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L2372  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L2377  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L2391  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L2398  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L2405  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L2411  (RadarDB)
def ts_total_points(self) -> int
# L2419  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L2438  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L2450  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L2465  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L2473  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L2487  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L2494  (RadarDB)
def alert_count(self) -> int
# L2498  (RadarDB)
def alert_clear(self)
# L2506  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L2522  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L2530  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L2538  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L2546  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L2551  (RadarDB)
def seq_total(self) -> int
# L2555  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L2561  (RadarDB)
def seq_clear(self)
# L2567  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L2584  (RadarDB)
def scenario_history_start(self) -> float | None
# L2592  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L2635  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L2653  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L2671  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L2746  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L2780  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L2822  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L2838  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L2868  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L2934  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L2953  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L2976  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L2995  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3015  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3102  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3134  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3224  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3245  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3256  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L3282  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L3303  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L3315  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L3327  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L3341  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L3365  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L3392  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L3445  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L3458  (RadarDB)
def scenario_list(self) -> list[dict]
# L3472  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L3516  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L3544  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L3562  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3566  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L3594  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L3606  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L3623  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L3632  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L3663  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L3674  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L3696  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L3702  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L3710  (RadarDB)
def threat_clear(self)
# L3716  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L3734  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L3768  (RadarDB)
def zscore_stats_all(self) -> list
# L3785  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L3797  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L3827  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str)
# L3848  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L3876  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L3888  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L3908  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L3933  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4032  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L4048  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L4071  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L4087  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L4099  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L4126  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L4151  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L4159  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L4168  (RadarDB)
def sensor_cache_all(self) -> dict
# L4177  (RadarDB)
def sensor_cache_count(self) -> int
# L4182  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L4195  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L4201  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L4220  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L4230  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L4243  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L4262  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L4279  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L4295  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L4306  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L4315  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L4333  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L4358  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L4401  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L4419  (RadarDB)
def startup_cleanup(self)
# L4424  (RadarDB)
def shutdown(self)
# L4443  (RadarDB)
def wal_checkpoint(self)
# L4453  (RadarDB)
def periodic_cleanup(self)
# L4459  (RadarDB)
def _prune_stale_rows(self) -> dict
# L4656  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L4682  (RadarDB)
def climate_events_save(self, events: list[dict])
# L4705  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L4722  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L4729  (RadarDB)
def intel_upsert(self, item: dict)
# L4753  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L4764  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L4774  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L4802  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L4815  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L4844  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L4869  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L4881  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L4894  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L4911  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L4923  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L4934  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L4945  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L4964  (RadarDB)
def intel_source_list(self) -> list[dict]
# L4973  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L4992  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L5059  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L5108  (RadarDB)
def user_count(self) -> int
# L5112  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L5120  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L5126  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L5138  (RadarDB)
def user_exists(self, username: str) -> bool
# L5144  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L5149  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L5154  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L5163  (RadarDB)
def user_delete(self, user_id: int)
# L5169  (RadarDB)
def user_list(self) -> list[dict]
# L5175  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L5183  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L5197  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L5209  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L5217  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L5223  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L5234  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L5250  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L5282  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L5303  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L5314  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L5329  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L5339  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L5350  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L5361  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L5370  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L5402  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L5415  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L5432  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L5449  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L5461  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5471  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L5483  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L5505  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L5519  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L5531  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L5537  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L5563  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L5588  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L5603  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L5625  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L5642  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L5653  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L5664  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L5681  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L5717  (RadarDB)
def close(self)
# L49
def _migration_v36_bg_observer(conn) -> None
# L115
def _migration_v24_generated_country(conn) -> None
```
