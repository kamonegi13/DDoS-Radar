# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 6953
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1553–1641)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1566–1569 |  |
| `execute` | L1571–1582 |  |
| `executemany` | L1584–1594 |  |
| `executescript` | L1596–1606 |  |
| `commit` | L1608–1614 |  |
| `rollback` | L1616–1622 |  |
| `close` | L1624–1628 |  |
| `writing` | L1631–1638 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1640–1641 |  |

### `RadarDB` (L1644–6937)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1653–1663 |  |
| `_check_integrity_or_recreate` | L1665–1695 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1697–1719 |  |
| `_ensure_schema` | L1721–1727 |  |
| `_post_baseline_indexes` | L1729–1790 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1792–1799 |  |
| `set_schema_version` | L1801–1824 |  |
| `_run_migrations` | L2849–2884 | Apply any pending schema migrations in order. |
| `baseline_get` | L2887–2896 |  |
| `baseline_set` | L2898–2905 |  |
| `baseline_all` | L2907–2916 |  |
| `baseline_len` | L2918–2920 |  |
| `airspace_get` | L2923–2928 |  |
| `airspace_set` | L2930–2937 |  |
| `airspace_all` | L2939–2943 |  |
| `airspace_len` | L2945–2947 |  |
| `hod_record` | L2950–2968 |  |
| `hod_record_many` | L2970–2988 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2990–3001 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L3003–3010 |  |
| `hod_all_entries` | L3012–3022 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L3024–3031 |  |
| `hod_distinct_hours` | L3033–3040 |  |
| `hod_total_points` | L3042–3046 |  |
| `hod_total_points_theater` | L3048–3054 |  |
| `gdelt_dow_record` | L3057–3071 |  |
| `gdelt_dow_last_bucket` | L3073–3078 |  |
| `gdelt_dow_same_weekday` | L3080–3087 |  |
| `gdelt_dow_total_points` | L3089–3091 |  |
| `ts_append` | L3094–3106 |  |
| `ts_get` | L3108–3113 |  |
| `ts_get_since` | L3115–3120 |  |
| `ts_distinct_theaters` | L3122–3126 |  |
| `ts_total_points` | L3128–3130 |  |
| `sensor_obs_record` | L3136–3153 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L3155–3164 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L3167–3180 |  |
| `series_get` | L3182–3187 |  |
| `alert_append` | L3190–3202 |  |
| `alert_list` | L3204–3209 |  |
| `alert_count` | L3211–3213 |  |
| `alert_clear` | L3215–3218 |  |
| `seq_append` | L3223–3237 |  |
| `seq_exists_since` | L3239–3245 |  |
| `seq_events_since` | L3247–3253 |  |
| `seq_all_events` | L3255–3261 |  |
| `seq_cleanup` | L3263–3266 |  |
| `seq_total` | L3268–3270 |  |
| `seq_distinct_theaters` | L3272–3276 |  |
| `seq_clear` | L3278–3281 |  |
| `tl_observation_append` | L3284–3299 |  |
| `scenario_history_start` | L3301–3306 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L3309–3359 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L3361–3377 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L3379–3395 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L3397–3469 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L3472–3504 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3506–3545 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L3548–3562 |  |
| `focus_switch_stats` | L3564–3592 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3594–3657 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L3660–3677 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L3679–3700 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L3702–3719 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L3721–3739 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3741–3825 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3828–3858 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3860–3948 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3950–3969 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3971–3980 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3982–4006 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L4008–4027 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L4029–4039 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L4041–4051 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L4053–4065 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L4067–4089 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L4091–4116 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L4118–4167 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L4171–4182 |  |
| `scenario_list` | L4184–4196 |  |
| `scenario_upsert` | L4198–4232 |  |
| `scenario_update_state` | L4242–4268 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L4270–4286 |  |
| `scenario_delete` | L4288–4290 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L4292–4318 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L4320–4330 |  |
| `scenario_change_log` | L4332–4337 |  |
| `threat_append` | L4349–4356 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L4358–4387 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L4389–4398 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L4400–4420 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L4422–4426 |  |
| `threat_last_scoped` | L4428–4434 |  |
| `threat_clear` | L4436–4439 |  |
| `zscore_stats_get` | L4442–4458 |  |
| `zscore_stats_update` | L4460–4492 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L4494–4508 |  |
| `fetch_log_append` | L4511–4521 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4523–4550 | Per-sensor reliability aggregates over the given time window. |
| `config_change_log_append` | L4553–4591 | Append a row to the unified config audit ledger. Returns the new |
| `config_change_log_recent` | L4593–4625 | Return recent config change rows for the Audit UI. Optional domain |
| `config_change_log_domains` | L4627–4640 | Return distinct domains observed in the audit ledger over the |
| `llm_call_log_append` | L4643–4678 | Persist a single LLM call attempt and its downstream queue verdict. |
| `llm_shadow_invocation_append` | L4680–4712 | Phase 9.2 C4 — record a SHADOW_DUAL parallel invocation. Linked |
| `auto_judge_decision_log` | L4714–4740 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4742–4752 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4754–4772 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4774–4797 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4799–4895 | Aggregate stats per caller and overall verdict counts. |
| `llm_routing_stats` | L4897–5137 | Phase 8 (LLM survey v10) — per-model + per-use_case rollups. |
| `llm_embedding_stats` | L5139–5194 | Phase 9.2 C9 — embedding pipeline rollups for the LLM Console |
| `ct_log_known_cas` | L5197–5211 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L5213–5234 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L5236–5250 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L5252–5262 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L5264–5289 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L5291–5313 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L5316–5322 |  |
| `sensor_cache_get` | L5324–5331 |  |
| `sensor_cache_all` | L5333–5340 |  |
| `sensor_cache_count` | L5342–5344 |  |
| `noise_excl_add` | L5347–5358 |  |
| `noise_excl_remove` | L5360–5364 |  |
| `noise_excl_list` | L5366–5383 |  |
| `noise_excl_match` | L5385–5392 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L5395–5406 |  |
| `confirmed_threat_list` | L5408–5424 |  |
| `daily_summary_upsert` | L5427–5442 |  |
| `daily_summary_get` | L5444–5457 |  |
| `forecast_log_add` | L5460–5469 |  |
| `forecast_log_resolve` | L5471–5478 |  |
| `forecast_log_get` | L5480–5496 |  |
| `forecast_accuracy_summary` | L5498–5520 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L5523–5564 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L5566–5581 |  |
| `startup_cleanup` | L5584–5587 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L5589–5606 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L5608–5616 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L5618–5622 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L5624–5836 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L5838–5861 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L5864–5885 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L5887–5902 | Load climate events since a given timestamp. |
| `climate_events_prune` | L5904–5908 | Remove climate events older than given timestamp. |
| `intel_upsert` | L5911–5933 | Insert or replace an LLM intel item. |
| `intel_update_status` | L5935–5944 |  |
| `intel_get` | L5946–5954 |  |
| `intel_list` | L5956–5982 |  |
| `intel_find_by_raw_url` | L5984–5995 | Find intel items with matching raw_url across all source_types. |
| `intel_recent_by_source` | L5997–6010 | Phase 9.2 C8 — recent intel rows for the same source_type, used |
| `intel_status_counts` | L6012–6039 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L6041–6064 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L6066–6076 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L6078–6089 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L6091–6106 |  |
| `intel_source_get` | L6108–6118 |  |
| `intel_source_upsert` | L6120–6129 |  |
| `intel_source_update_credibility` | L6131–6140 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L6142–6159 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L6161–6168 |  |
| `intel_source_reset_credibility` | L6170–6187 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L6189–6254 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L6256–6301 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L6305–6307 |  |
| `user_get` | L6309–6315 |  |
| `user_get_role` | L6317–6321 |  |
| `user_create` | L6323–6333 |  |
| `user_exists` | L6335–6339 |  |
| `user_update_last_login` | L6341–6344 |  |
| `user_update_role` | L6346–6349 |  |
| `user_update_password` | L6351–6358 |  |
| `user_delete` | L6360–6364 |  |
| `user_list` | L6366–6370 |  |
| `user_settings_get` | L6372–6378 |  |
| `user_settings_create` | L6380–6389 |  |
| `user_settings_update` | L6407–6423 |  |
| `token_revoke` | L6425–6431 |  |
| `token_is_revoked` | L6433–6437 |  |
| `user_get_invalidate_ts` | L6439–6447 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L6450–6464 |  |
| `hidden_signal_list` | L6466–6495 |  |
| `coverage_upsert` | L6498–6517 |  |
| `coverage_list` | L6519–6527 |  |
| `disconf_add` | L6530–6543 |  |
| `disconf_list` | L6545–6553 |  |
| `disconf_retract` | L6555–6563 |  |
| `ach_matrix_create` | L6566–6575 |  |
| `ach_matrix_list` | L6577–6584 |  |
| `ach_matrix_get` | L6586–6616 |  |
| `ach_hypothesis_add` | L6618–6629 |  |
| `ach_evidence_add` | L6631–6646 |  |
| `ach_score_set` | L6648–6662 |  |
| `dissent_add` | L6665–6675 |  |
| `dissent_list` | L6677–6685 |  |
| `dissent_resolve` | L6687–6696 |  |
| `assumption_add` | L6699–6719 |  |
| `_assumption_log` | L6721–6733 |  |
| `assumption_list` | L6735–6745 |  |
| `assumption_get` | L6747–6751 |  |
| `assumption_update` | L6753–6777 |  |
| `assumption_lock` | L6779–6802 |  |
| `assumption_invalidate` | L6804–6817 |  |
| `assumption_change_log` | L6819–6838 |  |
| `premortem_add` | L6841–6856 |  |
| `premortem_list` | L6858–6867 |  |
| `premortem_resolve` | L6869–6877 |  |
| `decision_log` | L6880–6895 |  |
| `decision_list` | L6897–6930 |  |
| `close` | L6933–6937 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L49–90 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v49_seed_config_runtime_from_env` | L93–184 | R4 — seed config_runtime_value from running os.environ for every |
| `_migration_v48_llm_embedding_ledger` | L187–238 | Phase 9.2 C7 — per-call ledger for the embedding pipeline. |
| `_migration_v47_llm_shadow_invocation` | L241–278 | Phase 9.2 C4 — dedicated table for SHADOW_DUAL parallel invocations. |
| `_migration_v45b_config_runtime_value` | L281–292 | Phase 9.1 — runtime override values for declarative config keys. |
| `_migration_v45_config_change_log` | L295–339 | Phase 9.1 (Foundation) — unified audit ledger for every analyst-driven |
| `_migration_v42_llm_routing_overrides` | L342–389 | Phase 8 (LLM survey v10) — analyst-driven routing overrides. |
| `_migration_v41_llm_routing_v10` | L392–429 | Phase 8 (LLM survey v10) — capture per-call thinking trace, the |
| `_migration_v37_decisions_revocation` | L432–495 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L498–539 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L542–554 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L557–620 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L623–650 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1566  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1571  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1584  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1596  (_CooperativeConn)
def executescript(self, sql: str)
# L1608  (_CooperativeConn)
def commit(self)
# L1616  (_CooperativeConn)
def rollback(self)
# L1624  (_CooperativeConn)
def close(self)
# L1631  (_CooperativeConn)
def writing(self)
# L1640  (_CooperativeConn)
def __getattr__(self, name)
# L1653  (RadarDB)
def __init__(self, db_path: str)
# L1665  (RadarDB)
def _check_integrity_or_recreate(self)
# L1697  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1721  (RadarDB)
def _ensure_schema(self)
# L1729  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1792  (RadarDB)
def schema_version(self) -> int
# L1801  (RadarDB)
def set_schema_version(self, ver: int)
# L2849  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2887  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2898  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2907  (RadarDB)
def baseline_all(self) -> dict
# L2918  (RadarDB)
def baseline_len(self) -> int
# L2923  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2930  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2939  (RadarDB)
def airspace_all(self) -> dict
# L2945  (RadarDB)
def airspace_len(self) -> int
# L2950  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2970  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2990  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L3003  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L3012  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L3024  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L3033  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L3042  (RadarDB)
def hod_total_points(self, table: str) -> int
# L3048  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L3057  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L3073  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L3080  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L3089  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L3094  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L3108  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L3115  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L3122  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L3128  (RadarDB)
def ts_total_points(self) -> int
# L3136  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L3155  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L3167  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L3182  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L3190  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L3204  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L3211  (RadarDB)
def alert_count(self) -> int
# L3215  (RadarDB)
def alert_clear(self)
# L3223  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L3239  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L3247  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L3255  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L3263  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L3268  (RadarDB)
def seq_total(self) -> int
# L3272  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L3278  (RadarDB)
def seq_clear(self)
# L3284  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L3301  (RadarDB)
def scenario_history_start(self) -> float | None
# L3309  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L3361  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L3379  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L3397  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L3472  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3506  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L3548  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L3564  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3594  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L3660  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L3679  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L3702  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L3721  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3741  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3828  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3860  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3950  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3971  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3982  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L4008  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L4029  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L4041  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L4053  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L4067  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L4091  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L4118  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L4171  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L4184  (RadarDB)
def scenario_list(self) -> list[dict]
# L4198  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L4242  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L4270  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L4288  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4292  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4320  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L4332  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L4349  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L4358  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L4389  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L4400  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L4422  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L4428  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L4436  (RadarDB)
def threat_clear(self)
# L4442  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L4460  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L4494  (RadarDB)
def zscore_stats_all(self) -> list
# L4511  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4523  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4553  (RadarDB)
def config_change_log_append(self, *, domain: str, config_key: str, old_value: 'object', new_value: 'object', changed_by: str, reason: 'str | None', request_id: 'str | None') -> int
# L4593  (RadarDB)
def config_change_log_recent(self, *, hours: int, domain: 'str | None', limit: int) -> list
# L4627  (RadarDB)
def config_change_log_domains(self, *, hours: int) -> list
# L4643  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str, use_case: 'str | None', shadow_model_choice: 'str | None', thinking_trace: 'str | None')
# L4680  (RadarDB)
def llm_shadow_invocation_append(self, *, primary_call_id: int, model: str, duration_ms: int, outcome: str, response_text: 'str | None', confidence: 'float | None', prompt_sha256: 'str | None', error: 'str | None') -> int
# L4714  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4742  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4754  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4774  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4799  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4897  (RadarDB)
def llm_routing_stats(self, hours: int) -> dict
# L5139  (RadarDB)
def llm_embedding_stats(self, hours: int) -> dict
# L5197  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L5213  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L5236  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L5252  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L5264  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L5291  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L5316  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L5324  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L5333  (RadarDB)
def sensor_cache_all(self) -> dict
# L5342  (RadarDB)
def sensor_cache_count(self) -> int
# L5347  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L5360  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L5366  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L5385  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L5395  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L5408  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L5427  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L5444  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L5460  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L5471  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L5480  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L5498  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L5523  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L5566  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L5584  (RadarDB)
def startup_cleanup(self)
# L5589  (RadarDB)
def shutdown(self)
# L5608  (RadarDB)
def wal_checkpoint(self)
# L5618  (RadarDB)
def periodic_cleanup(self)
# L5624  (RadarDB)
def _prune_stale_rows(self) -> dict
# L5838  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L5864  (RadarDB)
def climate_events_save(self, events: list[dict])
# L5887  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L5904  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L5911  (RadarDB)
def intel_upsert(self, item: dict)
# L5935  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L5946  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L5956  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L5984  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L5997  (RadarDB)
def intel_recent_by_source(self, source_type: str, since_ts: float, limit: int) -> list[dict]
# L6012  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L6041  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L6066  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L6078  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L6091  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L6108  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L6120  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L6131  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L6142  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L6161  (RadarDB)
def intel_source_list(self) -> list[dict]
# L6170  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L6189  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L6256  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L6305  (RadarDB)
def user_count(self) -> int
# L6309  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L6317  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L6323  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L6335  (RadarDB)
def user_exists(self, username: str) -> bool
# L6341  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L6346  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L6351  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L6360  (RadarDB)
def user_delete(self, user_id: int)
# L6366  (RadarDB)
def user_list(self) -> list[dict]
# L6372  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L6380  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L6407  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L6425  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L6433  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L6439  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L6450  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L6466  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L6498  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L6519  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L6530  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L6545  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L6555  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L6566  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L6577  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L6586  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L6618  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L6631  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L6648  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L6665  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L6677  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6687  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L6699  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L6721  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L6735  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L6747  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L6753  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L6779  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L6804  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L6819  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L6841  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L6858  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6869  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L6880  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L6897  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L6933  (RadarDB)
def close(self)
# L49
def _migration_v40_schema_version_singleton(conn) -> None
# L93
def _migration_v49_seed_config_runtime_from_env(conn) -> None
# L187
def _migration_v48_llm_embedding_ledger(conn) -> None
# L241
def _migration_v47_llm_shadow_invocation(conn) -> None
# L281
def _migration_v45b_config_runtime_value(conn) -> None
# L295
def _migration_v45_config_change_log(conn) -> None
# L342
def _migration_v42_llm_routing_overrides(conn) -> None
# L392
def _migration_v41_llm_routing_v10(conn) -> None
# L432
def _migration_v37_decisions_revocation(conn) -> None
# L498
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L542
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L557
def _migration_v36_bg_observer(conn) -> None
# L623
def _migration_v24_generated_country(conn) -> None
```
