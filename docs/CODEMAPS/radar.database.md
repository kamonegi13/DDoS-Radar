# Codemap: `radar/database.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 6856
- **Module purpose**: radar.database -- SQLite persistence layer.

## Top-level constants

- `_A4_THEATER_TABLES`
- `_SCHEMA_SQL`
- `_VALUE_COL`
- `_HOD_TABLES`
- `_WRITE_SQL_PREFIXES`
- `_DB_PATH`

## Classes

### `_CooperativeConn` (L1459–1547)
_Thin proxy around sqlite3.Connection that serializes write operations._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1472–1475 |  |
| `execute` | L1477–1488 |  |
| `executemany` | L1490–1500 |  |
| `executescript` | L1502–1512 |  |
| `commit` | L1514–1520 |  |
| `rollback` | L1522–1528 |  |
| `close` | L1530–1534 |  |
| `writing` | L1537–1544 | Context manager ensuring the write lock is released on exception. |
| `__getattr__` | L1546–1547 |  |

### `RadarDB` (L1550–6840)
_Thread-safe SQLite state store. One connection per greenlet/thread._

| Method | Lines | Summary |
|--------|-------|---------|
| `__init__` | L1559–1569 |  |
| `_check_integrity_or_recreate` | L1571–1601 | Run PRAGMA integrity_check on startup. If the DB is corrupted, |
| `_get_conn` | L1603–1625 |  |
| `_ensure_schema` | L1627–1633 |  |
| `_post_baseline_indexes` | L1635–1696 | Indexes and idempotent schema enhancements that must apply on |
| `schema_version` | L1698–1705 |  |
| `set_schema_version` | L1707–1730 |  |
| `_run_migrations` | L2752–2787 | Apply any pending schema migrations in order. |
| `baseline_get` | L2790–2799 |  |
| `baseline_set` | L2801–2808 |  |
| `baseline_all` | L2810–2819 |  |
| `baseline_len` | L2821–2823 |  |
| `airspace_get` | L2826–2831 |  |
| `airspace_set` | L2833–2840 |  |
| `airspace_all` | L2842–2846 |  |
| `airspace_len` | L2848–2850 |  |
| `hod_record` | L2853–2871 |  |
| `hod_record_many` | L2873–2891 | Bulk insert for migration. entries = [(hour_bucket, value), ...] |
| `hod_same_hour` | L2893–2904 | Values where hour-of-day matches and bucket < before_bucket. |
| `hod_last_bucket` | L2906–2913 |  |
| `hod_all_entries` | L2915–2925 | Return [(hour_bucket, value), ...] ordered by hour_bucket. |
| `hod_existing_buckets` | L2927–2934 |  |
| `hod_distinct_hours` | L2936–2943 |  |
| `hod_total_points` | L2945–2949 |  |
| `hod_total_points_theater` | L2951–2957 |  |
| `gdelt_dow_record` | L2960–2974 |  |
| `gdelt_dow_last_bucket` | L2976–2981 |  |
| `gdelt_dow_same_weekday` | L2983–2990 |  |
| `gdelt_dow_total_points` | L2992–2994 |  |
| `ts_append` | L2997–3009 |  |
| `ts_get` | L3011–3016 |  |
| `ts_get_since` | L3018–3023 |  |
| `ts_distinct_theaters` | L3025–3029 |  |
| `ts_total_points` | L3031–3033 |  |
| `sensor_obs_record` | L3039–3056 | Insert one observation point and opportunistically prune > ttl_sec. |
| `sensor_obs_recent` | L3058–3067 | Return [(ts, score, baseline, status), ...] for a sensor/scope since cutoff. |
| `series_append` | L3070–3083 |  |
| `series_get` | L3085–3090 |  |
| `alert_append` | L3093–3105 |  |
| `alert_list` | L3107–3112 |  |
| `alert_count` | L3114–3116 |  |
| `alert_clear` | L3118–3121 |  |
| `seq_append` | L3126–3140 |  |
| `seq_exists_since` | L3142–3148 |  |
| `seq_events_since` | L3150–3156 |  |
| `seq_all_events` | L3158–3164 |  |
| `seq_cleanup` | L3166–3169 |  |
| `seq_total` | L3171–3173 |  |
| `seq_distinct_theaters` | L3175–3179 |  |
| `seq_clear` | L3181–3184 |  |
| `tl_observation_append` | L3187–3202 |  |
| `scenario_history_start` | L3204–3209 | Return the earliest scenario_tl_observation timestamp, or None. |
| `scenario_contribution_append` | L3212–3262 | Persist this cycle's per-country contribution sums for a scenario. |
| `scenario_contributions_aggregate` | L3264–3280 | Sum per-country contributions for a scenario over a window. |
| `scenario_contributions_rows` | L3282–3298 | Raw (logged_at, country, contribution_sum) rows for time-bucketed |
| `scenario_signal_volume` | L3300–3372 | Per-scenario signal-volume report for the AP3 OBS chip. |
| `bg_observer_cycle_append` | L3375–3407 | Persist one bg_observer cycle audit row. |
| `bg_observer_cycle_summary` | L3409–3448 | Aggregate stats for the AP3 self-eval HUD chip. |
| `focus_switch_append` | L3451–3465 |  |
| `focus_switch_stats` | L3467–3495 | Return miss statistics for C-medium migration evaluation. |
| `focus_switch_detailed` | L3497–3560 | Extended focus_switch_stats with delta distribution and per-scenario |
| `shadow_sampler_state_get` | L3563–3580 | Return the most recent shadow sampler state for a scenario, or None. |
| `shadow_sampler_state_upsert` | L3582–3603 | Atomically upsert the shadow sampler state, incrementing sample_count. |
| `shadow_sampler_least_recent_among` | L3605–3622 | Return the scenario_id from the candidate list that has the oldest |
| `shadow_sampler_daily_count` | L3624–3642 | Return the number of shadow_sampler entries in focus_switch_log |
| `shadow_drift_stats` | L3644–3728 | Per-scenario lite-vs-full delta drift over the lookback window. |
| `shadow_eval_record` | L3731–3761 | Append a shadow evaluation sample. |
| `shadow_eval_summary` | L3763–3851 | Return aggregated shadow-eval statistics for the calibration endpoint. |
| `intel_count_24h_by_scenario` | L3853–3872 | Count LLM intel items (confirmed/auto_confirmed) in the last 24h per scenario. |
| `scenario_tl_last` | L3874–3883 | Return the most recent non-null TL observation for a scenario. |
| `scenario_tl_duration_sec` | L3885–3909 | Return seconds the scenario has been at current_tl, by walking |
| `intel_count_for_scenario_window` | L3911–3930 | Count confirmed/auto_confirmed LLM intel touching any participant |
| `scenario_score_series` | L3932–3942 | Return recent (observed_at, score) pairs for velocity computation. |
| `scenario_score_at_or_before` | L3944–3954 | Return the most recent observed score at or before target_ts. |
| `scenario_prev_lite_score` | L3956–3968 | Return the most recent lite-mode score for a scenario strictly |
| `scenario_tl_timeseries` | L3970–3992 | Return TL observation timeseries for a scenario. |
| `scenario_country_timeseries` | L3994–4019 | Return TL observation timeseries filtered to entries where a specific |
| `tl_calibration_stats` | L4021–4070 | TL calibration monitoring data (Section 7.3.1). |
| `scenario_get` | L4074–4085 |  |
| `scenario_list` | L4087–4099 |  |
| `scenario_upsert` | L4101–4135 |  |
| `scenario_update_state` | L4145–4171 | Returns (success, error_message). Error is empty on success. |
| `scenario_set_enabled` | L4173–4189 |  |
| `scenario_delete` | L4191–4193 | ADR-011: delete = active -> paused (semantic deletion, data retained). |
| `scenario_purge` | L4195–4221 | ADR-011: purge = archived -> permanent delete. Reserves the scenario_id. |
| `scenario_reset` | L4223–4233 |  |
| `scenario_change_log` | L4235–4240 |  |
| `threat_append` | L4252–4259 | Legacy unscoped append. scenario_id stored as NULL. |
| `threat_append_scoped` | L4261–4290 | Append a (ts, level, scenario_id) row. |
| `threat_list` | L4292–4301 | Legacy unscoped list — returns ALL rows regardless of scenario. |
| `threat_list_scoped` | L4303–4323 | Return (ts, level) rows for a single scenario, optionally |
| `threat_last` | L4325–4329 |  |
| `threat_last_scoped` | L4331–4337 |  |
| `threat_clear` | L4339–4342 |  |
| `zscore_stats_get` | L4345–4361 |  |
| `zscore_stats_update` | L4363–4395 | Incrementally update running mean/variance using Welford's algorithm. |
| `zscore_stats_all` | L4397–4411 |  |
| `fetch_log_append` | L4414–4424 | Persist a sensor fetch result. Auto-prunes entries older than 7 days. |
| `fetch_log_reliability` | L4426–4453 | Per-sensor reliability aggregates over the given time window. |
| `config_change_log_append` | L4456–4494 | Append a row to the unified config audit ledger. Returns the new |
| `config_change_log_recent` | L4496–4528 | Return recent config change rows for the Audit UI. Optional domain |
| `config_change_log_domains` | L4530–4543 | Return distinct domains observed in the audit ledger over the |
| `llm_call_log_append` | L4546–4581 | Persist a single LLM call attempt and its downstream queue verdict. |
| `llm_shadow_invocation_append` | L4583–4615 | Phase 9.2 C4 — record a SHADOW_DUAL parallel invocation. Linked |
| `auto_judge_decision_log` | L4617–4643 | Append a row to auto_judge_decisions and return its rowid. |
| `auto_judge_decision_mark_applied` | L4645–4655 | Promote the most recent decision row for an item to applied=1. |
| `auto_judge_decision_mark_overridden` | L4657–4675 | Mark the most recent applied decision for `item_id` as overridden |
| `auto_judge_decisions_recent` | L4677–4700 | Return decisions from the last `hours` for ECE calibration. |
| `llm_call_stats` | L4702–4798 | Aggregate stats per caller and overall verdict counts. |
| `llm_routing_stats` | L4800–5040 | Phase 8 (LLM survey v10) — per-model + per-use_case rollups. |
| `llm_embedding_stats` | L5042–5097 | Phase 9.2 C9 — embedding pipeline rollups for the LLM Console |
| `ct_log_known_cas` | L5100–5114 | Return the set of normalized CA names previously observed for a domain. |
| `ct_log_record_ca` | L5116–5137 | Insert or refresh the (domain, ca) observation row. |
| `ct_log_first_observed` | L5139–5153 | Return the timestamp at which we first started monitoring this domain. |
| `ct_log_set_first_observed` | L5155–5165 | Mark a domain as monitored starting at `now`. No-op if already set. |
| `llm_call_patch_verdict` | L5167–5192 | Patch the most recent empty-verdict llm_call_log row for a caller set. |
| `llm_call_recent` | L5194–5216 | Most recent llm_call_log rows, optionally filtered by caller. |
| `sensor_cache_set` | L5219–5225 |  |
| `sensor_cache_get` | L5227–5234 |  |
| `sensor_cache_all` | L5236–5243 |  |
| `sensor_cache_count` | L5245–5247 |  |
| `noise_excl_add` | L5250–5261 |  |
| `noise_excl_remove` | L5263–5267 |  |
| `noise_excl_list` | L5269–5286 |  |
| `noise_excl_match` | L5288–5295 | Check if a signal matches any active noise exclusion rule. |
| `confirmed_threat_add` | L5298–5309 |  |
| `confirmed_threat_list` | L5311–5327 |  |
| `daily_summary_upsert` | L5330–5345 |  |
| `daily_summary_get` | L5347–5360 |  |
| `forecast_log_add` | L5363–5372 |  |
| `forecast_log_resolve` | L5374–5381 |  |
| `forecast_log_get` | L5383–5399 |  |
| `forecast_accuracy_summary` | L5401–5423 | Aggregate forecast accuracy for resolved forecasts. |
| `cooccurrence_update` | L5426–5467 | Update co-occurrence counts. Only increases sensitivity (per design principle). |
| `cooccurrence_get` | L5469–5484 |  |
| `startup_cleanup` | L5487–5490 | Prune stale data on startup. Called once during app init. |
| `shutdown` | L5492–5509 | Checkpoint WAL and close the current thread's connection. |
| `wal_checkpoint` | L5511–5519 | Flush WAL to main DB file. Called hourly by cleanup worker. |
| `periodic_cleanup` | L5521–5525 | Prune stale data during long-running operation. Called daily by the cleanup worker. |
| `_prune_stale_rows` | L5527–5739 | Execute all time-based DELETE statements. Returns counts of deleted rows. |
| `_revive_expired_snoozed_proposals` | L5741–5764 | Phase 1 fix (problem 48): scenario_proposals rows in |
| `climate_events_save` | L5767–5788 | Persist climate events. Replaces existing entries with same (indicator, theater, hour). |
| `climate_events_load` | L5790–5805 | Load climate events since a given timestamp. |
| `climate_events_prune` | L5807–5811 | Remove climate events older than given timestamp. |
| `intel_upsert` | L5814–5836 | Insert or replace an LLM intel item. |
| `intel_update_status` | L5838–5847 |  |
| `intel_get` | L5849–5857 |  |
| `intel_list` | L5859–5885 |  |
| `intel_find_by_raw_url` | L5887–5898 | Find intel items with matching raw_url across all source_types. |
| `intel_recent_by_source` | L5900–5913 | Phase 9.2 C8 — recent intel rows for the same source_type, used |
| `intel_status_counts` | L5915–5942 | Return {status: count} for all intel items using GROUP BY. |
| `intel_update_llm_fields` | L5944–5967 | Merge new key/value pairs into an existing item's llm_fields JSON blob. |
| `intel_update_core_fields` | L5969–5979 | Update headline, confidence, score_delta, raw_text, raw_url for an existing item. |
| `intel_active_in_window` | L5981–5992 | Items that were auto_confirmed or confirmed in the given time window (for CLASSIFY THREAT linking). |
| `_intel_row_to_dict` | L5994–6009 |  |
| `intel_source_get` | L6011–6021 |  |
| `intel_source_upsert` | L6023–6032 |  |
| `intel_source_update_credibility` | L6034–6043 | Adjust credibility weight by delta, clamped to [0.30, 0.95]. |
| `intel_source_record_outcome` | L6045–6062 | Increment confirmed_count or false_positive_count and adjust credibility. |
| `intel_source_list` | L6064–6071 |  |
| `intel_source_reset_credibility` | L6073–6090 | Reset credibility to an explicit value if the source has never been |
| `intel_confidence_distribution` | L6092–6157 | Confidence distribution of LLM intel items for pipeline tuning. |
| `intel_country_weight_aggregate` | L6159–6204 | Aggregate LLM-emitted country_weights across active intel items. |
| `user_count` | L6208–6210 |  |
| `user_get` | L6212–6218 |  |
| `user_get_role` | L6220–6224 |  |
| `user_create` | L6226–6236 |  |
| `user_exists` | L6238–6242 |  |
| `user_update_last_login` | L6244–6247 |  |
| `user_update_role` | L6249–6252 |  |
| `user_update_password` | L6254–6261 |  |
| `user_delete` | L6263–6267 |  |
| `user_list` | L6269–6273 |  |
| `user_settings_get` | L6275–6281 |  |
| `user_settings_create` | L6283–6292 |  |
| `user_settings_update` | L6310–6326 |  |
| `token_revoke` | L6328–6334 |  |
| `token_is_revoked` | L6336–6340 |  |
| `user_get_invalidate_ts` | L6342–6350 | Return the invalidate_tokens_before timestamp for a user, or None. |
| `hidden_signal_log` | L6353–6367 |  |
| `hidden_signal_list` | L6369–6398 |  |
| `coverage_upsert` | L6401–6420 |  |
| `coverage_list` | L6422–6430 |  |
| `disconf_add` | L6433–6446 |  |
| `disconf_list` | L6448–6456 |  |
| `disconf_retract` | L6458–6466 |  |
| `ach_matrix_create` | L6469–6478 |  |
| `ach_matrix_list` | L6480–6487 |  |
| `ach_matrix_get` | L6489–6519 |  |
| `ach_hypothesis_add` | L6521–6532 |  |
| `ach_evidence_add` | L6534–6549 |  |
| `ach_score_set` | L6551–6565 |  |
| `dissent_add` | L6568–6578 |  |
| `dissent_list` | L6580–6588 |  |
| `dissent_resolve` | L6590–6599 |  |
| `assumption_add` | L6602–6622 |  |
| `_assumption_log` | L6624–6636 |  |
| `assumption_list` | L6638–6648 |  |
| `assumption_get` | L6650–6654 |  |
| `assumption_update` | L6656–6680 |  |
| `assumption_lock` | L6682–6705 |  |
| `assumption_invalidate` | L6707–6720 |  |
| `assumption_change_log` | L6722–6741 |  |
| `premortem_add` | L6744–6759 |  |
| `premortem_list` | L6761–6770 |  |
| `premortem_resolve` | L6772–6780 |  |
| `decision_log` | L6783–6798 |  |
| `decision_list` | L6800–6833 |  |
| `close` | L6836–6840 |  |

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_migration_v40_schema_version_singleton` | L49–90 | Phase 7.5h (audit DB L1) — collapse schema_version into a single |
| `_migration_v48_llm_embedding_ledger` | L93–144 | Phase 9.2 C7 — per-call ledger for the embedding pipeline. |
| `_migration_v47_llm_shadow_invocation` | L147–184 | Phase 9.2 C4 — dedicated table for SHADOW_DUAL parallel invocations. |
| `_migration_v45b_config_runtime_value` | L187–198 | Phase 9.1 — runtime override values for declarative config keys. |
| `_migration_v45_config_change_log` | L201–245 | Phase 9.1 (Foundation) — unified audit ledger for every analyst-driven |
| `_migration_v42_llm_routing_overrides` | L248–295 | Phase 8 (LLM survey v10) — analyst-driven routing overrides. |
| `_migration_v41_llm_routing_v10` | L298–335 | Phase 8 (LLM survey v10) — capture per-call thinking trace, the |
| `_migration_v37_decisions_revocation` | L338–401 | Phase 5 (audit) — split revocation out of `superseded_by` so the FK |
| `_migration_v38_sensor_observation_ts_pk` | L404–445 | Phase 5 (audit / DB H1) — give sensor_observation_ts a proper PK. |
| `_migration_v39_scenario_tl_obs_mode_index` | L448–460 | Phase 5 (audit / DB H3) — covering index for scenario_prev_lite_score. |
| `_migration_v36_bg_observer` | L463–526 | ADR-V2-015 Phase 1+5: per-sensor contribution log + bg_observer cycle log. |
| `_migration_v24_generated_country` | L529–556 | Add `country` GENERATED VIRTUAL column mirroring `theater` on each table. |

## Signatures

```python
# L1472  (_CooperativeConn)
def __init__(self, conn: sqlite3.Connection, write_lock: threading.Lock)
# L1477  (_CooperativeConn)
def execute(self, sql: str, parameters)
# L1490  (_CooperativeConn)
def executemany(self, sql: str, seq_of_parameters)
# L1502  (_CooperativeConn)
def executescript(self, sql: str)
# L1514  (_CooperativeConn)
def commit(self)
# L1522  (_CooperativeConn)
def rollback(self)
# L1530  (_CooperativeConn)
def close(self)
# L1537  (_CooperativeConn)
def writing(self)
# L1546  (_CooperativeConn)
def __getattr__(self, name)
# L1559  (RadarDB)
def __init__(self, db_path: str)
# L1571  (RadarDB)
def _check_integrity_or_recreate(self)
# L1603  (RadarDB)
def _get_conn(self) -> _CooperativeConn
# L1627  (RadarDB)
def _ensure_schema(self)
# L1635  (RadarDB)
def _post_baseline_indexes(self, conn: '_CooperativeConn')
# L1698  (RadarDB)
def schema_version(self) -> int
# L1707  (RadarDB)
def set_schema_version(self, ver: int)
# L2752  (RadarDB)
def _run_migrations(self, conn: '_CooperativeConn')
# L2790  (RadarDB)
def baseline_get(self, theater: str) -> dict
# L2801  (RadarDB)
def baseline_set(self, theater: str, data: dict, ts: float)
# L2810  (RadarDB)
def baseline_all(self) -> dict
# L2821  (RadarDB)
def baseline_len(self) -> int
# L2826  (RadarDB)
def airspace_get(self, code: str) -> dict
# L2833  (RadarDB)
def airspace_set(self, code: str, data: dict)
# L2842  (RadarDB)
def airspace_all(self) -> dict
# L2848  (RadarDB)
def airspace_len(self) -> int
# L2853  (RadarDB)
def hod_record(self, table: str, theater: str, hour_bucket: int, value: float, max_entries: int)
# L2873  (RadarDB)
def hod_record_many(self, table: str, theater: str, entries: list[tuple[int, float]], max_entries: int)
# L2893  (RadarDB)
def hod_same_hour(self, table: str, theater: str, target_hod: int, before_bucket: int) -> list[float]
# L2906  (RadarDB)
def hod_last_bucket(self, table: str, theater: str) -> Optional[int]
# L2915  (RadarDB)
def hod_all_entries(self, table: str, theater: str) -> list[tuple[int, float]]
# L2927  (RadarDB)
def hod_existing_buckets(self, table: str, theater: str) -> set[int]
# L2936  (RadarDB)
def hod_distinct_hours(self, table: str, theater: str) -> int
# L2945  (RadarDB)
def hod_total_points(self, table: str) -> int
# L2951  (RadarDB)
def hod_total_points_theater(self, table: str, theater: str) -> int
# L2960  (RadarDB)
def gdelt_dow_record(self, theater: str, day_bucket: int, weekday: int, tone: float, max_entries: int)
# L2976  (RadarDB)
def gdelt_dow_last_bucket(self, theater: str) -> Optional[int]
# L2983  (RadarDB)
def gdelt_dow_same_weekday(self, theater: str, weekday: int, before_bucket: int) -> list[float]
# L2992  (RadarDB)
def gdelt_dow_total_points(self) -> int
# L2997  (RadarDB)
def ts_append(self, theater: str, ts: float, value: float, max_entries: int)
# L3011  (RadarDB)
def ts_get(self, theater: str) -> list[tuple[float, float]]
# L3018  (RadarDB)
def ts_get_since(self, theater: str, cutoff: float) -> list[tuple[float, float]]
# L3025  (RadarDB)
def ts_distinct_theaters(self) -> list[str]
# L3031  (RadarDB)
def ts_total_points(self) -> int
# L3039  (RadarDB)
def sensor_obs_record(self, sensor: str, scope: str, ts: float, score: float, baseline: float | None, status: str | None, ttl_sec: float) -> None
# L3058  (RadarDB)
def sensor_obs_recent(self, sensor: str, scope: str, since_ts: float) -> list[tuple[float, float, float | None, str | None]]
# L3070  (RadarDB)
def series_append(self, theater: str, series_type: str, value: float, max_entries: int)
# L3085  (RadarDB)
def series_get(self, theater: str, series_type: str) -> list[float]
# L3093  (RadarDB)
def alert_append(self, alert_dict: dict, max_entries: int)
# L3107  (RadarDB)
def alert_list(self, limit: int) -> list[dict]
# L3114  (RadarDB)
def alert_count(self) -> int
# L3118  (RadarDB)
def alert_clear(self)
# L3126  (RadarDB)
def seq_append(self, theater: str, ts: float, event_type: str, meta: dict, scenario_id: str | None)
# L3142  (RadarDB)
def seq_exists_since(self, theater: str, event_type: str, since: float) -> bool
# L3150  (RadarDB)
def seq_events_since(self, theater: str, cutoff: float) -> list[dict]
# L3158  (RadarDB)
def seq_all_events(self, theater: str) -> list[dict]
# L3166  (RadarDB)
def seq_cleanup(self, cutoff: float)
# L3171  (RadarDB)
def seq_total(self) -> int
# L3175  (RadarDB)
def seq_distinct_theaters(self) -> list[str]
# L3181  (RadarDB)
def seq_clear(self)
# L3187  (RadarDB)
def tl_observation_append(self, scenario_id: str, observed_at: float, score: float, tl: int | None, cyber: float, physical: float, info: float, convergence_bonus: float, scoring_mode: str, active_countries: list[str])
# L3204  (RadarDB)
def scenario_history_start(self) -> float | None
# L3212  (RadarDB)
def scenario_contribution_append(self, scenario_id: str, contributions_by_country: dict[str, float], logged_at: float, sensor: Optional[str], contributions_by_country_sensor: Optional[dict[tuple[str, str], float]]) -> None
# L3264  (RadarDB)
def scenario_contributions_aggregate(self, scenario_id: str, hours: int) -> dict[str, float]
# L3282  (RadarDB)
def scenario_contributions_rows(self, scenario_id: str, hours: int) -> list[tuple[float, str, float]]
# L3300  (RadarDB)
def scenario_signal_volume(self, scenario_id: str, hours: int) -> dict
# L3375  (RadarDB)
def bg_observer_cycle_append(self, row: dict) -> None
# L3409  (RadarDB)
def bg_observer_cycle_summary(self, hours: int) -> dict
# L3451  (RadarDB)
def focus_switch_append(self, scenario_id: str, switched_at: float, lite_score: float, full_score: float, delta: float, is_miss: bool, source: str, shadow_score_kind: str | None)
# L3467  (RadarDB)
def focus_switch_stats(self, days: int, source: str | None) -> dict
# L3497  (RadarDB)
def focus_switch_detailed(self, days: int, source: str | None) -> dict
# L3563  (RadarDB)
def shadow_sampler_state_get(self, scenario_id: str) -> dict | None
# L3582  (RadarDB)
def shadow_sampler_state_upsert(self, scenario_id: str, last_sampled_at: float, lite_score: float, full_score: float, delta: float) -> None
# L3605  (RadarDB)
def shadow_sampler_least_recent_among(self, scenario_ids: list[str]) -> str | None
# L3624  (RadarDB)
def shadow_sampler_daily_count(self, scenario_id: str | None) -> int
# L3644  (RadarDB)
def shadow_drift_stats(self, days: int, noise_band_pct: float) -> dict
# L3731  (RadarDB)
def shadow_eval_record(self, scenario_id: str, sampled_at: float, shadow_variant: str, actual_score: float, actual_tl: int | None, shadow_score: float, shadow_tl: int | None, active_countries: list[str], notes: str | None) -> None
# L3763  (RadarDB)
def shadow_eval_summary(self, days: int, shadow_variant: str | None) -> dict
# L3853  (RadarDB)
def intel_count_24h_by_scenario(self, scenario_participants: dict[str, set[str]]) -> dict[str, int]
# L3874  (RadarDB)
def scenario_tl_last(self, scenario_id: str) -> int | None
# L3885  (RadarDB)
def scenario_tl_duration_sec(self, scenario_id: str, current_tl: int | None) -> float | None
# L3911  (RadarDB)
def intel_count_for_scenario_window(self, participants: set[str], since_ts: float) -> int
# L3932  (RadarDB)
def scenario_score_series(self, scenario_id: str, limit: int) -> list[tuple[float, float]]
# L3944  (RadarDB)
def scenario_score_at_or_before(self, scenario_id: str, target_ts: float) -> float | None
# L3956  (RadarDB)
def scenario_prev_lite_score(self, scenario_id: str, before_ts: float) -> float | None
# L3970  (RadarDB)
def scenario_tl_timeseries(self, scenario_id: str, hours: int, limit: int) -> list[dict]
# L3994  (RadarDB)
def scenario_country_timeseries(self, scenario_id: str, country: str, hours: int) -> list[dict]
# L4021  (RadarDB)
def tl_calibration_stats(self, scenario_id: str | None, hours: int) -> dict
# L4074  (RadarDB)
def scenario_get(self, scenario_id: str) -> dict | None
# L4087  (RadarDB)
def scenario_list(self) -> list[dict]
# L4101  (RadarDB)
def scenario_upsert(self, scenario_id: str, data: dict, changed_by: str) -> None
# L4145  (RadarDB)
def scenario_update_state(self, scenario_id: str, state: str, changed_by: str) -> tuple[bool, str]
# L4173  (RadarDB)
def scenario_set_enabled(self, scenario_id: str, enabled: bool, changed_by: str) -> bool
# L4191  (RadarDB)
def scenario_delete(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4195  (RadarDB)
def scenario_purge(self, scenario_id: str, changed_by: str) -> tuple[bool, str]
# L4223  (RadarDB)
def scenario_reset(self, scenario_id: str, changed_by: str) -> bool
# L4235  (RadarDB)
def scenario_change_log(self, scenario_id: str, limit: int) -> list[dict]
# L4252  (RadarDB)
def threat_append(self, ts: float, level: int, max_entries: int)
# L4261  (RadarDB)
def threat_append_scoped(self, ts: float, level: int, scenario_id: Optional[str], max_entries: int)
# L4292  (RadarDB)
def threat_list(self) -> list[tuple[float, int]]
# L4303  (RadarDB)
def threat_list_scoped(self, scenario_id: str, since_ts: Optional[float], limit: int) -> list[tuple[float, int]]
# L4325  (RadarDB)
def threat_last(self) -> Optional[tuple[float, int]]
# L4331  (RadarDB)
def threat_last_scoped(self, scenario_id: str) -> Optional[tuple[float, int]]
# L4339  (RadarDB)
def threat_clear(self)
# L4345  (RadarDB)
def zscore_stats_get(self, sensor: str, theater: str) -> Optional[dict]
# L4363  (RadarDB)
def zscore_stats_update(self, sensor: str, theater: str, new_value: float)
# L4397  (RadarDB)
def zscore_stats_all(self) -> list
# L4414  (RadarDB)
def fetch_log_append(self, sensor_name: str, ts: float, success: bool, duration_ms: int, http_status: int, records: int, error: str)
# L4426  (RadarDB)
def fetch_log_reliability(self, sensor_name: str, hours: int) -> list
# L4456  (RadarDB)
def config_change_log_append(self, *, domain: str, config_key: str, old_value: 'object', new_value: 'object', changed_by: str, reason: 'str | None', request_id: 'str | None') -> int
# L4496  (RadarDB)
def config_change_log_recent(self, *, hours: int, domain: 'str | None', limit: int) -> list
# L4530  (RadarDB)
def config_change_log_domains(self, *, hours: int) -> list
# L4546  (RadarDB)
def llm_call_log_append(self, caller: str, model: str, duration_ms: int, outcome: str, verdict: str, confidence: float, headline: str, error: str, prompt_sha256: str, use_case: 'str | None', shadow_model_choice: 'str | None', thinking_trace: 'str | None')
# L4583  (RadarDB)
def llm_shadow_invocation_append(self, *, primary_call_id: int, model: str, duration_ms: int, outcome: str, response_text: 'str | None', confidence: 'float | None', prompt_sha256: 'str | None', error: 'str | None') -> int
# L4617  (RadarDB)
def auto_judge_decision_log(self, item_id: str, action_proposed: str, confidence: float, reason: str, layer1_corroborators: int, layer1_satisfied: bool, applied: bool) -> int
# L4645  (RadarDB)
def auto_judge_decision_mark_applied(self, item_id: str) -> None
# L4657  (RadarDB)
def auto_judge_decision_mark_overridden(self, item_id: str, override_action: str, analyst_id: str) -> None
# L4677  (RadarDB)
def auto_judge_decisions_recent(self, hours: int) -> list[dict]
# L4702  (RadarDB)
def llm_call_stats(self, hours: int) -> dict
# L4800  (RadarDB)
def llm_routing_stats(self, hours: int) -> dict
# L5042  (RadarDB)
def llm_embedding_stats(self, hours: int) -> dict
# L5100  (RadarDB)
def ct_log_known_cas(self, domain: str) -> set[str]
# L5116  (RadarDB)
def ct_log_record_ca(self, domain: str, ca_normalized: str, ca_raw: str, now: float) -> None
# L5139  (RadarDB)
def ct_log_first_observed(self, domain: str) -> float | None
# L5155  (RadarDB)
def ct_log_set_first_observed(self, domain: str, now: float) -> None
# L5167  (RadarDB)
def llm_call_patch_verdict(self, callers: tuple, verdict: str, window_sec: int) -> bool
# L5194  (RadarDB)
def llm_call_recent(self, caller: str, limit: int) -> list
# L5219  (RadarDB)
def sensor_cache_set(self, sensor_name: str, cache_time: float, cache_data: dict)
# L5227  (RadarDB)
def sensor_cache_get(self, sensor_name: str) -> Optional[dict]
# L5236  (RadarDB)
def sensor_cache_all(self) -> dict
# L5245  (RadarDB)
def sensor_cache_count(self) -> int
# L5250  (RadarDB)
def noise_excl_add(self, sensor: str, theater: str, pattern: str, reason: str, created_by: str, expires_at: float) -> int
# L5263  (RadarDB)
def noise_excl_remove(self, rule_id: int) -> bool
# L5269  (RadarDB)
def noise_excl_list(self, sensor: str, theater: str) -> list[dict]
# L5288  (RadarDB)
def noise_excl_match(self, sensor: str, theater: str, value: str) -> Optional[dict]
# L5298  (RadarDB)
def confirmed_threat_add(self, theater: str, ts: float, classification: str, sensors_active: list, threat_level: int, notes: str, created_by: str) -> int
# L5311  (RadarDB)
def confirmed_threat_list(self, theater: str, limit: int) -> list[dict]
# L5330  (RadarDB)
def daily_summary_upsert(self, theater: str, day_bucket: int, avg_score: float, max_score: float, min_tl: int, max_tl: int, fired_sensors: list, domain_scores: dict, context_alignment: dict, summary: dict)
# L5347  (RadarDB)
def daily_summary_get(self, theater: str, days: int) -> list[dict]
# L5363  (RadarDB)
def forecast_log_add(self, theater: str, ts: float, forecast_type: str, predicted: str) -> int
# L5374  (RadarDB)
def forecast_log_resolve(self, forecast_id: int, actual: str, accuracy: float)
# L5383  (RadarDB)
def forecast_log_get(self, theater: str, limit: int) -> list[dict]
# L5401  (RadarDB)
def forecast_accuracy_summary(self, theater: str) -> dict
# L5426  (RadarDB)
def cooccurrence_update(self, sensor_a: str, sensor_b: str, theater: str, both_fired: bool)
# L5469  (RadarDB)
def cooccurrence_get(self, theater: str) -> list[dict]
# L5487  (RadarDB)
def startup_cleanup(self)
# L5492  (RadarDB)
def shutdown(self)
# L5511  (RadarDB)
def wal_checkpoint(self)
# L5521  (RadarDB)
def periodic_cleanup(self)
# L5527  (RadarDB)
def _prune_stale_rows(self) -> dict
# L5741  (RadarDB)
def _revive_expired_snoozed_proposals(self, *, now: float) -> int
# L5767  (RadarDB)
def climate_events_save(self, events: list[dict])
# L5790  (RadarDB)
def climate_events_load(self, since_ts: float) -> list[dict]
# L5807  (RadarDB)
def climate_events_prune(self, before_ts: float)
# L5814  (RadarDB)
def intel_upsert(self, item: dict)
# L5838  (RadarDB)
def intel_update_status(self, item_id: str, status: str, confirmed_by: str, confirmed_at: float, override_at: float)
# L5849  (RadarDB)
def intel_get(self, item_id: str) -> Optional[dict]
# L5859  (RadarDB)
def intel_list(self, source_type: str, status: str, theater: str, limit: int, since_ts: float, before_ts: float) -> list[dict]
# L5887  (RadarDB)
def intel_find_by_raw_url(self, raw_url: str, since_ts: float, limit: int) -> list[dict]
# L5900  (RadarDB)
def intel_recent_by_source(self, source_type: str, since_ts: float, limit: int) -> list[dict]
# L5915  (RadarDB)
def intel_status_counts(self) -> dict[str, int]
# L5944  (RadarDB)
def intel_update_llm_fields(self, item_id: str, llm_fields: dict)
# L5969  (RadarDB)
def intel_update_core_fields(self, item_id: str, headline: str, confidence: float, score_delta: float, raw_text: str, raw_url: str)
# L5981  (RadarDB)
def intel_active_in_window(self, since_ts: float) -> list[dict]
# L5994  (RadarDB)
def _intel_row_to_dict(self, r) -> dict
# L6011  (RadarDB)
def intel_source_get(self, source_id: str) -> Optional[dict]
# L6023  (RadarDB)
def intel_source_upsert(self, source_id: str, source_type: str, credibility_weight: float)
# L6034  (RadarDB)
def intel_source_update_credibility(self, source_id: str, delta: float)
# L6045  (RadarDB)
def intel_source_record_outcome(self, source_id: str, confirmed: bool)
# L6064  (RadarDB)
def intel_source_list(self) -> list[dict]
# L6073  (RadarDB)
def intel_source_reset_credibility(self, source_id: str, credibility_weight: float) -> bool
# L6092  (RadarDB)
def intel_confidence_distribution(self, hours: int) -> dict
# L6159  (RadarDB)
def intel_country_weight_aggregate(self, hours: int) -> dict
# L6208  (RadarDB)
def user_count(self) -> int
# L6212  (RadarDB)
def user_get(self, username: str) -> Optional[dict]
# L6220  (RadarDB)
def user_get_role(self, username: str) -> Optional[str]
# L6226  (RadarDB)
def user_create(self, username: str, password_hash: str, salt: str, role: str, created_at: float) -> int
# L6238  (RadarDB)
def user_exists(self, username: str) -> bool
# L6244  (RadarDB)
def user_update_last_login(self, user_id: int, ts: float)
# L6249  (RadarDB)
def user_update_role(self, user_id: int, role: str)
# L6254  (RadarDB)
def user_update_password(self, user_id: int, password_hash: str, salt: str)
# L6263  (RadarDB)
def user_delete(self, user_id: int)
# L6269  (RadarDB)
def user_list(self) -> list[dict]
# L6275  (RadarDB)
def user_settings_get(self, username: str) -> Optional[dict]
# L6283  (RadarDB)
def user_settings_create(self, user_id: int, focused_scenario: Optional[str], muted: str, lang: str, updated_at: float)
# L6310  (RadarDB)
def user_settings_update(self, user_id: int, updates: dict)
# L6328  (RadarDB)
def token_revoke(self, jti: str, revoked_at: float)
# L6336  (RadarDB)
def token_is_revoked(self, jti: str) -> bool
# L6342  (RadarDB)
def user_get_invalidate_ts(self, username: str) -> Optional[float]
# L6353  (RadarDB)
def hidden_signal_log(self, scenario_id: Optional[str], country: Optional[str], sensor: str, domain: str, hide_reason: str, detail: Optional[dict])
# L6369  (RadarDB)
def hidden_signal_list(self, scenario_id: Optional[str], since: Optional[float], limit: int) -> list[dict]
# L6401  (RadarDB)
def coverage_upsert(self, scenario_id: str, sensor: str, domain: str, last_success: Optional[float], last_attempt: Optional[float], state: str, consecutive_failures: int)
# L6422  (RadarDB)
def coverage_list(self, scenario_id: str) -> list[dict]
# L6433  (RadarDB)
def disconf_add(self, scenario_id: str, tagged_by: str, source_kind: str, source_ref: Optional[str], summary: str, strength: int) -> int
# L6448  (RadarDB)
def disconf_list(self, scenario_id: str, include_retracted: bool) -> list[dict]
# L6458  (RadarDB)
def disconf_retract(self, item_id: int, retracted_by: str) -> bool
# L6469  (RadarDB)
def ach_matrix_create(self, scenario_id: str, title: str, created_by: str) -> int
# L6480  (RadarDB)
def ach_matrix_list(self, scenario_id: str, include_archived: bool) -> list[dict]
# L6489  (RadarDB)
def ach_matrix_get(self, matrix_id: int) -> Optional[dict]
# L6521  (RadarDB)
def ach_hypothesis_add(self, matrix_id: int, text: str, is_null: bool, ord_: int) -> int
# L6534  (RadarDB)
def ach_evidence_add(self, matrix_id: int, text: str, source_ref: Optional[str], credibility: int, relevance: int, ord_: int) -> int
# L6551  (RadarDB)
def ach_score_set(self, matrix_id: int, hypothesis_id: int, evidence_id: int, consistency: int, note: Optional[str])
# L6568  (RadarDB)
def dissent_add(self, scenario_id: str, created_by: str, title: str, body: str, argues_for_tl: Optional[int]) -> int
# L6580  (RadarDB)
def dissent_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6590  (RadarDB)
def dissent_resolve(self, view_id: int, resolved_by: str, resolution_note: Optional[str]) -> bool
# L6602  (RadarDB)
def assumption_add(self, scenario_id: str, created_by: str, statement: str, rationale: Optional[str], confidence: str) -> int
# L6624  (RadarDB)
def _assumption_log(self, conn, assumption_id: int, changed_by: str, change_type: str, before: Optional[dict], after: Optional[dict], note: Optional[str])
# L6638  (RadarDB)
def assumption_list(self, scenario_id: str, include_invalidated: bool) -> list[dict]
# L6650  (RadarDB)
def assumption_get(self, assumption_id: int) -> Optional[dict]
# L6656  (RadarDB)
def assumption_update(self, assumption_id: int, changed_by: str, fields: dict, is_admin: bool) -> bool
# L6682  (RadarDB)
def assumption_lock(self, assumption_id: int, locked_by: str, is_admin: bool, lock: bool) -> bool
# L6707  (RadarDB)
def assumption_invalidate(self, assumption_id: int, by: str, note: Optional[str]) -> bool
# L6722  (RadarDB)
def assumption_change_log(self, assumption_id: int) -> list[dict]
# L6744  (RadarDB)
def premortem_add(self, scenario_id: str, created_by: str, failure_mode: str, imagined_outcome: str, root_cause: str, early_warning: Optional[str], mitigation: Optional[str]) -> int
# L6761  (RadarDB)
def premortem_list(self, scenario_id: str, include_resolved: bool) -> list[dict]
# L6772  (RadarDB)
def premortem_resolve(self, entry_id: int, resolved_by: str) -> bool
# L6783  (RadarDB)
def decision_log(self, user_id: Optional[int], username: str, session_id: str, scenario_id: Optional[str], decision_type: str, summary: str, detail: Optional[dict]) -> int
# L6800  (RadarDB)
def decision_list(self, session_id: Optional[str], scenario_id: Optional[str], user_id: Optional[int], since: Optional[float], limit: int) -> list[dict]
# L6836  (RadarDB)
def close(self)
# L49
def _migration_v40_schema_version_singleton(conn) -> None
# L93
def _migration_v48_llm_embedding_ledger(conn) -> None
# L147
def _migration_v47_llm_shadow_invocation(conn) -> None
# L187
def _migration_v45b_config_runtime_value(conn) -> None
# L201
def _migration_v45_config_change_log(conn) -> None
# L248
def _migration_v42_llm_routing_overrides(conn) -> None
# L298
def _migration_v41_llm_routing_v10(conn) -> None
# L338
def _migration_v37_decisions_revocation(conn) -> None
# L404
def _migration_v38_sensor_observation_ts_pk(conn) -> None
# L448
def _migration_v39_scenario_tl_obs_mode_index(conn) -> None
# L463
def _migration_v36_bg_observer(conn) -> None
# L529
def _migration_v24_generated_country(conn) -> None
```
