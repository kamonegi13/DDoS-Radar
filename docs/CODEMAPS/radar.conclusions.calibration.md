# Codemap: `radar/conclusions/calibration.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 207
- **Module purpose**: Per-Conclusion calibration status — closes NP5+8 in v2.0.

## Top-level constants

- `_MIN_RECALL_SAMPLES`
- `_DEGRADED_RECALL_THRESHOLD`
- `_WINDOW_DAYS`
- `_CACHE_TTL_SEC`
- `_INSUFFICIENT`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `calibration_status_for` | L98–122 | Build the per-Conclusion calibration_status dict for `scenario_id`. |
| `invalidate_cache` | L125–132 | Drop memoised snapshots. Call after an ETL pass writes fresh labels |
| `_compute_snapshot` | L135–191 | Query the deduped confusion matrix for one scenario and classify. |
| `_classify_status` | L194–207 | OK / DEGRADED / INSUFFICIENT_DATA from the recall denominator. |

## Signatures

```python
# L98
def calibration_status_for(db: 'RadarDB', scenario_id: str) -> dict
# L125
def invalidate_cache(scenario_id: Optional[str]) -> None
# L135
def _compute_snapshot(db: 'RadarDB', scenario_id: str, *, now: float) -> dict
# L194
def _classify_status(*, recall_denom: int, recall: Optional[float]) -> str
```
