# Codemap: `radar/routes/intel.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 345
- **Module purpose**: radar.routes.intel -- LLM Intelligence API endpoints.

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_safe_float_env` | L13–23 | Read float env var; return default on missing or non-numeric value. |
| `intel_list` | L27–83 | List LLM intel items with optional filters. |
| `intel_pending_triage` | L87–143 | Return pending intel items ranked by triage priority. |
| `intel_stats` | L147–180 | Return LLM intel queue statistics and LLM status. |
| `_invalidate_scoring_cache` | L183–188 | Reset scoring cache timestamp so the next /api/threat_data re-scores. |
| `intel_confirm` | L193–202 | Manually confirm a PENDING intel item. |
| `intel_reject` | L207–226 | Reject a PENDING intel item. |
| `intel_revert` | L231–240 | Revert a confirmed or rejected item back to pending for re-review. |
| `intel_override` | L245–254 | Override an AUTO-CONFIRMED item within the override window. |
| `intel_sources` | L258–261 | List LLM source credibility data. |
| `intel_llm_call_stats` | L265–283 | Aggregate LLM call observability over the given window. |
| `llm_models` | L287–345 | Proxy: fetch available models from Ollama at the given host. |

## Signatures

```python
# L13
def _safe_float_env(name: str, default: float) -> float
# L27
def intel_list()
# L87
def intel_pending_triage()
# L147
def intel_stats()
# L183
def _invalidate_scoring_cache()
# L193
def intel_confirm(item_id: str)
# L207
def intel_reject(item_id: str)
# L231
def intel_revert(item_id: str)
# L245
def intel_override(item_id: str)
# L258
def intel_sources()
# L265
def intel_llm_call_stats()
# L287
def llm_models()
```
