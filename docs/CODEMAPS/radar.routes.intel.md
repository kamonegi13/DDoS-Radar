# Codemap: `radar/routes/intel.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 344
- **Module purpose**: radar.routes.intel -- LLM Intelligence API endpoints.

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_safe_float_env` | L12–22 | Read float env var; return default on missing or non-numeric value. |
| `intel_list` | L26–82 | List LLM intel items with optional filters. |
| `intel_pending_triage` | L86–142 | Return pending intel items ranked by triage priority. |
| `intel_stats` | L146–179 | Return LLM intel queue statistics and LLM status. |
| `_invalidate_scoring_cache` | L182–187 | Reset scoring cache timestamp so the next /api/threat_data re-scores. |
| `intel_confirm` | L192–201 | Manually confirm a PENDING intel item. |
| `intel_reject` | L206–225 | Reject a PENDING intel item. |
| `intel_revert` | L230–239 | Revert a confirmed or rejected item back to pending for re-review. |
| `intel_override` | L244–253 | Override an AUTO-CONFIRMED item within the override window. |
| `intel_sources` | L257–260 | List LLM source credibility data. |
| `intel_llm_call_stats` | L264–282 | Aggregate LLM call observability over the given window. |
| `llm_models` | L286–344 | Proxy: fetch available models from Ollama at the given host. |

## Signatures

```python
# L12
def _safe_float_env(name: str, default: float) -> float
# L26
def intel_list()
# L86
def intel_pending_triage()
# L146
def intel_stats()
# L182
def _invalidate_scoring_cache()
# L192
def intel_confirm(item_id: str)
# L206
def intel_reject(item_id: str)
# L230
def intel_revert(item_id: str)
# L244
def intel_override(item_id: str)
# L257
def intel_sources()
# L264
def intel_llm_call_stats()
# L286
def llm_models()
```
