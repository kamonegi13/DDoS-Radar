# Codemap: `radar/routes/intel.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 405
- **Module purpose**: radar.routes.intel -- LLM Intelligence API endpoints.

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_safe_float_env` | L13–23 | Read float env var; return default on missing or non-numeric value. |
| `_expose_country` | L32–44 | Display-layer rename: expose the intel item's primary country as |
| `intel_list` | L49–105 | List LLM intel items with optional filters. |
| `intel_pending_triage` | L110–166 | Return pending intel items ranked by triage priority. |
| `intel_stats` | L171–238 | Return LLM intel queue statistics and LLM status. |
| `_invalidate_scoring_cache` | L241–246 | Reset scoring cache timestamp so the next /api/threat_data re-scores. |
| `intel_confirm` | L251–260 | Manually confirm a PENDING intel item. |
| `intel_reject` | L265–284 | Reject a PENDING intel item. |
| `intel_revert` | L289–298 | Revert a confirmed or rejected item back to pending for re-review. |
| `intel_override` | L303–312 | Override an AUTO-CONFIRMED item within the override window. |
| `intel_sources` | L317–320 | List LLM source credibility data. |
| `intel_llm_call_stats` | L325–343 | Aggregate LLM call observability over the given window. |
| `llm_models` | L347–405 | Proxy: fetch available models from Ollama at the given host. |

## Signatures

```python
# L13
def _safe_float_env(name: str, default: float) -> float
# L32
def _expose_country(items: list[dict]) -> list[dict]
# L49
def intel_list()
# L110
def intel_pending_triage()
# L171
def intel_stats()
# L241
def _invalidate_scoring_cache()
# L251
def intel_confirm(item_id: str)
# L265
def intel_reject(item_id: str)
# L289
def intel_revert(item_id: str)
# L303
def intel_override(item_id: str)
# L317
def intel_sources()
# L325
def intel_llm_call_stats()
# L347
def llm_models()
```
