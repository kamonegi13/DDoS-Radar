# Codemap: `radar/routes/intel.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 389
- **Module purpose**: radar.routes.intel -- LLM Intelligence API endpoints.

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_safe_float_env` | L13–23 | Read float env var; return default on missing or non-numeric value. |
| `intel_list` | L33–89 | List LLM intel items with optional filters. |
| `intel_pending_triage` | L94–150 | Return pending intel items ranked by triage priority. |
| `intel_stats` | L155–222 | Return LLM intel queue statistics and LLM status. |
| `_invalidate_scoring_cache` | L225–230 | Reset scoring cache timestamp so the next /api/threat_data re-scores. |
| `intel_confirm` | L235–244 | Manually confirm a PENDING intel item. |
| `intel_reject` | L249–268 | Reject a PENDING intel item. |
| `intel_revert` | L273–282 | Revert a confirmed or rejected item back to pending for re-review. |
| `intel_override` | L287–296 | Override an AUTO-CONFIRMED item within the override window. |
| `intel_sources` | L301–304 | List LLM source credibility data. |
| `intel_llm_call_stats` | L309–327 | Aggregate LLM call observability over the given window. |
| `llm_models` | L331–389 | Proxy: fetch available models from Ollama at the given host. |

## Signatures

```python
# L13
def _safe_float_env(name: str, default: float) -> float
# L33
def intel_list()
# L94
def intel_pending_triage()
# L155
def intel_stats()
# L225
def _invalidate_scoring_cache()
# L235
def intel_confirm(item_id: str)
# L249
def intel_reject(item_id: str)
# L273
def intel_revert(item_id: str)
# L287
def intel_override(item_id: str)
# L301
def intel_sources()
# L309
def intel_llm_call_stats()
# L331
def llm_models()
```
