# Codemap: `radar/diagnostics.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 144
- **Module purpose**: radar.diagnostics — Weekly observability runner.

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_interval_seconds` | L37–39 |  |
| `_import_scripts` | L42–52 | Import the two CLI diagnostic scripts as modules. They live in |
| `_diversity_signal` | L55–67 | Return a one-line signal summarising whether the radar's |
| `run_once` | L70–109 | Run both diagnostics once unconditionally and log the reports. |
| `maybe_run` | L112–133 | Cadence-gated entry point — invoked by the cleanup_worker on every |
| `_force_reset_for_tests` | L136–141 | Test-only helper: reset the cadence gate so a test can re-trigger |

## Signatures

```python
# L37
def _interval_seconds() -> float
# L42
def _import_scripts() -> tuple[Any, Any]
# L55
def _diversity_signal(audit_result: dict, backtest_result: dict) -> str
# L70
def run_once(db_path: str | None) -> dict
# L112
def maybe_run() -> dict | None
# L136
def _force_reset_for_tests() -> None
```
