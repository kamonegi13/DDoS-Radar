# Codemap: `radar/routes/core.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 3139
- **Module purpose**: radar.routes.core -- Main API endpoints: app_config and threat_data scoring loop.

## Top-level constants

- `_FORCE_SYNC_SENSORS`
- `_LATEST_SIGNALS_SNAPSHOT`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_spawn_force_sensors_fetch` | L69–97 | Background greenlet: fetch the allow-listed sensors without blocking |
| `app_config` | L123–133 |  |
| `list_scenarios` | L143–163 |  |
| `scenarios_compare` | L168–220 | F7 Scenario Comparison — side-by-side snapshot of N scenarios using |
| `scenario_whatif_weights` | L225–288 | F9 What-If Weight Slider — recompute scenario score with overlaid |
| `_compute_airspace_status` | L299–384 | Compute airspace anomaly status and noise filters. |
| `_read_sensor_caches` | L387–508 | Extract all sensor cache data into a flat dict. |
| `get_threat_data` | L513–3138 |  |

## Signatures

```python
# L69
def _spawn_force_sensors_fetch(sensors_iter, ctx) -> None
# L123
def app_config()
# L143
def list_scenarios()
# L168
def scenarios_compare()
# L225
def scenario_whatif_weights(scenario_id: str)
# L299
def _compute_airspace_status(airspace_data, weather_conditions, current_time, db, airspace_window, hod_baseline_days, hod_min_same_hour)
# L387
def _read_sensor_caches(registry)
# L513
def get_threat_data()
```
