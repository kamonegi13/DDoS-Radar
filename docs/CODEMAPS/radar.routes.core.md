# Codemap: `radar/routes/core.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 3070
- **Module purpose**: radar.routes.core -- Main API endpoints: app_config and threat_data scoring loop.

## Top-level constants

- `_FORCE_SYNC_SENSORS`
- `_LATEST_SIGNALS_SNAPSHOT`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_spawn_force_sensors_fetch` | L68–96 | Background greenlet: fetch the allow-listed sensors without blocking |
| `app_config` | L118–128 |  |
| `list_scenarios` | L131–151 |  |
| `scenarios_compare` | L155–207 | F7 Scenario Comparison — side-by-side snapshot of N scenarios using |
| `scenario_whatif_weights` | L211–274 | F9 What-If Weight Slider — recompute scenario score with overlaid |
| `_compute_airspace_status` | L285–370 | Compute airspace anomaly status and noise filters. |
| `_read_sensor_caches` | L373–494 | Extract all sensor cache data into a flat dict. |
| `get_threat_data` | L498–3069 |  |

## Signatures

```python
# L68
def _spawn_force_sensors_fetch(sensors_iter, ctx) -> None
# L118
def app_config()
# L131
def list_scenarios()
# L155
def scenarios_compare()
# L211
def scenario_whatif_weights(scenario_id: str)
# L285
def _compute_airspace_status(airspace_data, weather_conditions, current_time, db, airspace_window, hod_baseline_days, hod_min_same_hour)
# L373
def _read_sensor_caches(registry)
# L498
def get_threat_data()
```
