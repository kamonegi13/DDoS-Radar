# Codemap: `radar/routes/core.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 2869
- **Module purpose**: radar.routes.core -- Main API endpoints: app_config and threat_data scoring loop.

## Top-level constants

- `_LATEST_SIGNALS_SNAPSHOT`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `app_config` | L70–80 |  |
| `list_scenarios` | L83–92 |  |
| `scenarios_compare` | L96–148 | F7 Scenario Comparison — side-by-side snapshot of N scenarios using |
| `scenario_whatif_weights` | L152–215 | F9 What-If Weight Slider — recompute scenario score with overlaid |
| `scenario_breakdown` | L218–240 |  |
| `_compute_airspace_status` | L242–327 | Compute airspace anomaly status and noise filters. |
| `_read_sensor_caches` | L330–451 | Extract all sensor cache data into a flat dict. |
| `get_threat_data` | L455–2868 |  |

## Signatures

```python
# L70
def app_config()
# L83
def list_scenarios()
# L96
def scenarios_compare()
# L152
def scenario_whatif_weights(scenario_id: str)
# L218
def scenario_breakdown(scenario_id: str)
# L242
def _compute_airspace_status(airspace_data, weather_conditions, current_time, db, airspace_window, hod_baseline_days, hod_min_same_hour)
# L330
def _read_sensor_caches(registry)
# L455
def get_threat_data()
```
