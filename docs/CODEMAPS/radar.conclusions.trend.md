# Codemap: `radar/conclusions/trend.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 222
- **Module purpose**: Trend conclusion deriver — ADR-V2-003 (24h/7d/30d × 5 states).

## Top-level constants

- `FORMULA_REF`
- `_TL_SEVERITY`
- `WINDOWS`
- `RISING_DELTA`
- `ESCALATE_DELTA`
- `MIN_SAMPLES`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `derive_trend` | L62–134 | Compute the 3-window trend conclusion for one scenario. |
| `_eval_window` | L137–181 | Compute mean severity for the current and previous span; classify. |
| `_classify` | L184–193 |  |
| `_serialize_windows` | L196–205 | Deterministic serialization for state column. |
| `_confidence_from_samples` | L208–222 | Confidence scales with sample density. Caps at 0.95. |

## Signatures

```python
# L62
def derive_trend(db: 'RadarDB', scenario_id: str, *, now: Optional[float]) -> Conclusion
# L137
def _eval_window(db: 'RadarDB', scenario_id: str, span_sec: float, now: float) -> dict
# L184
def _classify(delta: float) -> str
# L196
def _serialize_windows(windows_state: dict[str, str]) -> str
# L208
def _confidence_from_samples(sample_counts: dict, any_unavailable: bool) -> float
```
