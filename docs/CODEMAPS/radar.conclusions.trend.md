# Codemap: `radar/conclusions/trend.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 234
- **Module purpose**: Trend conclusion deriver — ADR-V2-003 (24h/7d/30d × 5 states).

## Top-level constants

- `FORMULA_REF`
- `_TL_SEVERITY`
- `WINDOWS`
- `RISING_DELTA`
- `ESCALATE_DELTA`
- `MIN_SAMPLES`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `derive_trend` | L74–146 | Compute the 3-window trend conclusion for one scenario. |
| `_eval_window` | L149–193 | Compute mean severity for the current and previous span; classify. |
| `_classify` | L196–205 |  |
| `_serialize_windows` | L208–217 | Deterministic serialization for state column. |
| `_confidence_from_samples` | L220–234 | Confidence scales with sample density. Caps at 0.95. |

## Signatures

```python
# L74
def derive_trend(db: 'RadarDB', scenario_id: str, *, now: Optional[float]) -> Conclusion
# L149
def _eval_window(db: 'RadarDB', scenario_id: str, span_sec: float, now: float) -> dict
# L196
def _classify(delta: float) -> str
# L208
def _serialize_windows(windows_state: dict[str, str]) -> str
# L220
def _confidence_from_samples(sample_counts: dict, any_unavailable: bool) -> float
```
