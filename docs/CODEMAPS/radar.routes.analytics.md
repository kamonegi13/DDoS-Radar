# Codemap: `radar/routes/analytics.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 1160
- **Module purpose**: radar.routes.analytics -- Read-only analytics, report, and data-status endpoints.

## Top-level constants

- `_ANALYTICS_READ_ROLES`
- `WHATIF_SENSOR_CATALOG`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `_scenario_label` | L40–58 | Display name for a scenario, honouring an explicit `lang` request. |
| `data_status` | L63–84 |  |
| `sensor_reliability` | L88–92 | Per-sensor fetch reliability over a given time window (default 24h, max 168h). |
| `api_alert_timeline` | L96–98 |  |
| `api_sitrep` | L103–181 |  |
| `api_sequence_chain` | L189–213 | Return escalation chain status for all countries. |
| `api_deep_analytics` | L218–323 | Detailed endpoint for deep analysis results. |
| `api_salute_report` | L328–499 | Generate the current threat situation as a SALUTE format (Size/Activity/Location/Unit/Time/Equipment) |
| `api_weather_brief` | L504–618 | Convert current sensor data to an "Operational Weather Brief" format and return it. |
| `api_ip_check` | L623–660 | Look up IP address noise/classification info via GreyNoise Community API. |
| `api_score_breakdown` | L670–716 | Per-sensor score breakdown for the current threat assessment. |
| `whatif_catalog` | L750–752 | Return the sensor catalog for the What-If simulation UI. |
| `whatif_simulate` | L757–855 | Run a What-If simulation with user-injected virtual sensor events. |
| `spof_analysis` | L864–994 | Compute sensor SPOF (single point of failure) analysis. |
| `adaptive_zscore_status` | L1003–1021 | Return per-sensor/per-theater adaptive Z-score statistics. |
| `api_calibration_advisory` | L1035–1105 | Weight calibration advisory: analyzes scenario participant weight |
| `api_confidence_distribution` | L1110–1119 | LLM intel confidence distribution for pipeline tuning. |
| `api_scenario_phases` | L1124–1158 | Scenario implementation phase annotation for operational awareness. |

## Signatures

```python
# L40
def _scenario_label(scenario: 'Scenario', lang: str | None) -> str
# L63
def data_status()
# L88
def sensor_reliability()
# L96
def api_alert_timeline()
# L103
def api_sitrep()
# L189
def api_sequence_chain()
# L218
def api_deep_analytics()
# L328
def api_salute_report()
# L504
def api_weather_brief()
# L623
def api_ip_check()
# L670
def api_score_breakdown()
# L750
def whatif_catalog()
# L757
def whatif_simulate()
# L864
def spof_analysis()
# L1003
def adaptive_zscore_status()
# L1035
def api_calibration_advisory()
# L1110
def api_confidence_distribution()
# L1124
def api_scenario_phases()
```
