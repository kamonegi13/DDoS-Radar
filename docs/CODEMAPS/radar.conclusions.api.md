# Codemap: `radar/conclusions/api.py`

_Generated by `scripts/gen_codemap.py`. Do not hand-edit._

- **Lines**: 99
- **Module purpose**: v2 API envelope construction — keeps NP7 disclaimer enforcement and

## Top-level constants

- `API_VERSION`

## Top-level functions

| Function | Lines | Summary |
|----------|-------|---------|
| `build_envelope` | L23–43 | Wrap a list of Conclusions in the v2 API envelope. observed_at defaults |
| `build_error` | L46–56 | NP7-compliant error response. Every v2 error path goes through this so |
| `build_unavailable` | L59–99 | Envelope for a (scenario, type) pair the ledger has no row for yet. |

## Signatures

```python
# L23
def build_envelope(scenario_id: str, conclusions: Iterable[Conclusion], *, observed_at: Optional[float]) -> dict
# L46
def build_error(status: int, error: str, **extra) -> tuple[dict, int]
# L59
def build_unavailable(scenario_id: str, conclusion_type: ConclusionType, *, reason: ConclusionUnavailableReason, detail: str) -> dict
```
