# ACC グループ C — ACCIDENTAL 一次分類（較正・サービス・分析系）

**対象**: `S1-calibration.md §4`（A1-A15）/ `S1-services.md §4`（A1-A8）/ `S1-analytics-config.md §7`（A1-A20）= **全 43 件**
**物差し**: CLAUDE.md プロジェクト定義 4 文 + NP1-NP7 / AP1-AP4、[P4-processing-steps.md](../P4-processing-steps.md)（S9 較正 / S10 自己評価 / S11 記録 の責務と §5 判定基準）、[D2-defects.md](../D2-defects.md) §D/§E、[v3-rebuild.md](../../v3-rebuild.md) §3 ADR-V3-004〜009
**位置づけ**: 一次分類の草案。オーナー裁定の入力であり、確定 ADR ではない。

---

## 1. 件数サマリ

| 分類 | 件数 | 内訳（較正 / サービス / 分析・設定） |
|---|---|---|
| **KEEP** | 2 | 2 / 0 / 0 |
| **DROP** | 11 | 4 / 2 / 5 |
| **SIMPLIFY** | 19 | 8 / 4 / 7 |
| **OWNER** | 3 | 1 / 2 / 0 |
| **裁定不要（climate 凍結 / ADR-V3-008）** | 8 | 0 / 0 / 8 |
| **合計** | **43** | 15 / 8 / 20 |

**裁定対象は 35 件**（43 − climate 凍結 8）。

**この分類が simplification に効く構造**: 35 件のうち **24 件が 7 本の共通処方に合流する**（§6）。
個別に 35 通りの判断を実装するのではなく、7 本の修正を入れると 24 件が同時に消える。

---

## 2. 裁定不要 — climate 凍結分（ADR-V3-008、8 件）

`S1-analytics-config.md §7` の A1〜A8 は**すべて `radar/climate.py` の挙動**であり、
**ADR-V3-008（climate を v3 初期リリースに含めない = 凍結）で既に決着している**。
本書は分類対象から外す。**再裁定しない**。

| ID | 挙動 | 一次ソース | 扱い |
|---|---|---|---|
| ANLYT-A1 | S3（為替）のみ `record → get_baseline` の順で当該観測が自分のベースラインに混入する | climate.py:79-141 | 凍結対象のため裁定不要 |
| ANLYT-A2 | ベースライン剪定が「50 件ごと」だが剪定で長さが変わり以降の周期がずれる | climate.py:79-141 | 凍結対象のため裁定不要 |
| ANLYT-A3 | T2 の silence は severity 上限 1、surge は 2（沈黙のほうが軽い） | climate.py:240-283 | 凍結対象のため裁定不要 |
| ANLYT-A4 | S1 の theater 欄に ISO2 でなく空港コードが入る | climate.py:301-339 | 凍結対象のため裁定不要 |
| ANLYT-A5 | S2 と theater 未指定の CAL が空文字を共有し時単位 dedup で互いに潰し合う | climate.py:360-392, 808-825 | 凍結対象のため裁定不要 |
| ANLYT-A6 | 予定イベントを import 時に 1 度だけ読む（geo_data.json 更新に再起動が要る） | climate.py:147-220 | 凍結対象のため裁定不要 |
| ANLYT-A7 | Gauge の収斂写像に `economic` という第 4 ドメインが登場する | climate.py:922-992 | 凍結対象のため裁定不要 |
| ANLYT-A8 | 成熟度の母集団に S2 が入らず S2 用分岐が到達不能。宣言と実装も不一致 | climate.py:891-920 | 凍結対象のため裁定不要 |

**注**: `S1-analytics-config.md §10-3`（S3 の `economic` ドメインを 3 ドメイン体系にどう位置づけるか）も
ANLYT-A7 と同じ根であり、climate 再実装時まで持ち越す。**NP2 の収斂判定に波及するため、
climate を復活させる際は「Gauge を結論に接続するか参考情報と明示するか」の裁定が先行条件**（§10-2 と同じ）。

---

## 3. 較正系 — S1-calibration.md §4（15 件）

**この領域固有の前提**: 較正層は 3 回壊れた（#1 blanket-TP / #2 TL 反転 / #3 帰属汚染）。
修正で入ったガードは原則 KEEP。逆に「効いていないガード」「二重経路」「死んだメトリクス」は DROP（D2 E-03 / E-20）。

| ID | 挙動（平易に） | 分類 | 根拠（原則 / P4 ステップ責務） | 単純化の効果 | リスク |
|---|---|---|---|---|---|
| **CAL-A1** | TL 較正の入力集計に時間窓が無く全履歴を使う。他の recall 経路はすべて 30 日窓 | **SIMPLIFY** | **P4 S9**「ラベルと baseline は系列 ID を持ち、系列を跨いだ比較は機械的に拒否される」。全履歴集計は 2026-07-04 / 08-02 の系列断絶をまたぐ = インシデント #1〜#3 前後のラベルを混ぜて較正する。D2 D-01 の再発経路 | recall 計算の窓定義が **2 種（全履歴 / 30 日）→ 1 種**。系列エポック境界でのクリップも同じ 1 関数に入る（DP19 / DP23 と同一処方） | 窓を入れると sample_n が減り 30 件下限に届かず TL 較正が恒久無発火（null-zone）になりうる。**ただし条項 020 で較正値は現在採点に消費されていないため当面の実害はゼロ** |
| **CAL-A2** | degenerate guard の実効 precision 境界（0.30）が tighten 上限（0.30）と一致し、`tn==0 かつ fn==0` では厳格化に到達しない | **KEEP** | `tn==0 かつ fn==0` は**インシデント #1（blanket-TP、FN≡0・recall 1.0 固定）の署名そのもの**。退化データで閾値を厳格化しないのは **NP1（感度優先、recall > precision）**に一致する安全側の挙動。テスト済（test_tl_calibrator_guards.py degenerate 2 件） | 挙動は据置。ただし 2 つの定数を独立に持つのをやめ `tighten_precision_max = precision_min_floor + 0.20` と**導出関係で書く**（偶然の一致を構造的一致に変える） | 低。導出関係にすると片方を動かしたとき他方も動くため、変更時の影響が広がる（登録時に impact=high で警告） |
| **CAL-A3** | 較正ステップ ±5% が統治層の magnitude 予算 10% を常に下回るため、クランプが較正器に実効しない | **SIMPLIFY** | **P4 S9**「閾値の変更提案（適用は別途ゲート）」。相対%予算は反復緩和を止められない（DP22: 幾何級数的に 0 へ収束しうる）。二重防御の意図の有無を問うより、**効く 1 本に寄せる**のが正しい | 相対 magnitude 予算 + 絶対レンジの二層 → **band 既定値からの絶対レンジ 1 本**に統合。「効かないガードが 1 本ある」状態が消える | 絶対レンジの値が新たな未較正定数になる。緩和: band 既定値の ±50% 等、**既定値から導出**して新規定数を増やさない |
| **CAL-A4** | シナリオ列挙自体の失敗が空 dict を返し、「シナリオ 0 件」と区別できない | **DROP** | **P4 S6**「**データ欠測と真の平穏を区別できることが要件**」。**NP5+8**（結論不可の明示）。G-17（SALUTE が空キャッシュで ROUTINE）/ ANLYT-A15 / DP10 / DP11 と**完全に同型** | 沈黙フォールバックを捨て「結論不可」の単一表現に載せる。**§6 処方 ① に合流**し、個別の分岐が消える | 低。列挙失敗時に較正が走らなくなるが、それが正しい（NP3 のシナリオ間非伝播は条項 021 の**個別**失敗側で維持される） |
| **CAL-A5** | アナリスト FN の集計に国スコープが無く、1 件の FN が全世界の recall 削減提案を止める | **SIMPLIFY** | **NP1 的には安全側**であり**ガード自体は KEEP**。問題は抑止の範囲が証拠の範囲を超えていること（D2 **E-02 HIGH**、コード内で「保守的に過剰計上」と自認）。**P4 S9**「提案の生成・適用・却下は単一のガード評価器を通る」。DP26 で v3 規範は既に確定 | ガードを消さずスコープを `(scenario_id, country)` に**狭めるだけ**。グローバル集計と per-scenario 集計の**二重集計が 1 本に**なる（§7-5 の「別の集計を別の場所で持つ」問題の一部） | スコープを絞ると weight_too_high が通りやすくなる = **recall 削減方向のリスク**。緩和: 証拠強度 6 段ラダー・tier ゲート・role 保護再検査（条項 043 ③）が独立に効く多層防御は維持される |
| **CAL-A6** | 提案発行の dedup / cap のカウント失敗がフェイルオープン（提案を通す） | **DROP** | **ADR-V3-006（v3 は fail-closed を既定とする）で裁定済み**。dedup 対象の pending→pending\|applied 拡張は**「auto-apply 済み行の再発火で重みが発振した事故」由来のガード = ガード本体は KEEP**。捨てるのは例外時のフェイルオープン挙動のみ（ガードが最も必要な瞬間に消える） | 例外時の分岐が「通す」→「止める」の 1 択になり、DB 不調時の挙動が推論可能になる | DB 一時障害で提案が出なくなる。**較正は日次〜週次の遅い周期（P4 S9）なので次ティックで回復**し、実害は極小 |
| **CAL-A7** | 重み範囲の強制が適用層（参加者検証 0.0–1.0）にしかなく、提案 JSON を書き換えれば発行時 floor（0.10/0.95）を外れた値が通る | **KEEP** | **層の分離が正しい**: 発行時 floor は**提案の品質**制約、適用時 0.0–1.0 は**データ整合**制約。異なる問いに答えている（P4 §5 判定基準 2「隣接ステップの責務が重ならない」）。**P4 S9**「較正に影響する書き込みは権限を要求する」を満たし、`applied_by` は透過して変更ログに残る（NP6） | 変更なし。2 レイヤであることを**仕様として明記**する（現状は暗黙） | 低。権限を持つアナリストが意図的に 0.0 を置けるのは**機能**であり欠陥ではない |
| **CAL-A8** | 重み減算幅が改善器 0.15 / 構造提案器 0.20 と別値。どちらにも導出根拠が無い | **SIMPLIFY** | D1 の横断病理筆頭「**コピペドリフト**」の典型。**NP6**（式・閾値の導出開示）— 導出根拠の無い 2 値が並存する状態は開示できない。ADR-V3-004 が凍結するのは TL 導出式のみで本項は対象外 | 減算幅の定義が **2 箇所 → 1 registry キー**。「どちらが正か」の問いが消える | 統合で片方の刻み幅が変わる。**パリティ期間中は ADR-V3-006 の「予期された差分」として事前登録**して吸収 |
| **CAL-A9** | 提案の終端状態が 6 値しかなく、accept / auto-dismiss / expire の区別が `state_changed_by` のマーカー文字列にしか無い | **SIMPLIFY** | **AP4（判断履歴）/ P4 S11**「自動化判断を時系列で再生可能にする」。文字列パースに依存した再生は型で保証されず、条項 053 の 3 フック（マーカー 3 種）と E-06 の dry-run 汚染マーカーが同じ列に混在する | 状態 6 値は維持し、遷移理由を**型付き列（enum）**に分離。マーカー文字列のパースが**全経路から消える**。E-06（dry-run が台帳を applied に汚す）も型で区別可能になる | 既存行のマーカー → enum のバックフィルが要る（D4 移行対象表に 1 件追加）。未知マーカーは `unknown` に落とし fail-closed |
| **CAL-A10** | tier 遷移（auto-apply 権限段位の昇降格）が recall 劣化を直接の判定軸にしない。recall ゲートは提案単位にしか無い | **OWNER** | **NP1（感度優先）上は最大の穴**: 提案単位の recall ゲートは DP18 で**恒久 open**（存在しない関数を呼び例外握り潰し）と判明しており、**recall 劣化に反応する経路が 2 つとも死んでいる**。**P4 S10**「ゲート生存: 安全ゲート自体が機能しているか」が検査項目として要求する対象<br>**推奨**: 降格条件に「30 日窓 recall が baseline から Design W 閾値を超えて低下」を追加し、DP18 の修正（実在・テスト済・fail-closed）を前提条件にする | 効果は「単純化」ではなく**制御ループの追加**。OWNER に上げるのはこのため（複雑化解消というオーナーの目的と方向が逆） | 追加しない場合: 自動適用が recall を下げても tier が下がらず、劣化が加速しうる。追加する場合: recall 計測系自体のバグ（D2 **D-01「構造では直らない」**）が tier を誤って落とす経路が新設される。**D-01 は tier 降格を測定系に結ぶことの固有リスク** |
| **CAL-A11** | HIGH cooldown の発火判定が `impact_override` を考慮せず、env 不正値では cooldown が設定されないまま**無音で終了**する | **DROP** | **D2 の「沈黙した検知失敗」7 件と同じ構造**（UI は健全・判定だけが死ぬ）。**P4 S10** が検出対象とする類型そのもの。**ADR-V3-006** の fail-closed 既定にも該当 | `impact_override` の迂回経路を廃止し、impact 分類を**単一の宣言表**から解決（DP15 の fail-closed と同経路）。env 不正値は**起動時検証で落とす**（ANLYT-A17 と同じ処方） | override が実運用で使われていたか未確認。**使われていれば運用手段が 1 つ消える** → 台帳の `applied_by` / `impact` 実績を 1 クエリで確認してから削除する |
| **CAL-A12** | tier 上限（キルスイッチ）の不正値が 3（= 最大許可）にフォールバックする | **DROP** | **ADR-V3-006（fail-closed 既定）で裁定済み、再裁定不要**。不正値で**キルスイッチが最大許可に開く**のは fail-open の最悪形（キルスイッチの意味が消える） | フォールバック値が 0（最も安全）に一本化され、「不正値のとき何が起きるか」の推論が不要になる | 設定ミスで auto-apply が全停止する。**それが正しい**（キルスイッチの設計意図）。緩和: 起動時検証で不正値を早期に露出 |
| **CAL-A13** | magnitude 予算が `prior == 0` のとき適用されず、初出キーは上限チェックなしに verbatim コミットされる | **SIMPLIFY** | **NP6**（閾値変更の導出開示）— 「初回だけ無制限」は予算という概念を成立させない。条項 018 は基準値を「直近**採択値**（無ければ**既定値**）」と定義しており、**統治層だけが台帳のみを見る非対称**が原因 | prior 解決を「台帳の直近採択値 ∨ registry 既定値」の**単一関数**に統合。`prior == 0` の特例が消え、条項 018 と条項 063 の基準値が**同じ 1 関数**になる | registry 既定値が 0 のキーでは依然 `prior == 0` → **CAL-A3 の絶対レンジクランプが受け皿**になる（2 件が同時に閉じる） |
| **CAL-A14** | 自動 ETL の limit が ground truth 1000 / RSS 4000 と非対称。導出根拠が無い | **SIMPLIFY** | **D2 G-15**（98 キー中 96 キーが 3 層解決を迂回）に帰着。個別の値の妥当性より **「到達性が無いこと自体」が問題**。**NP6**: 値がどこで決まるか開示できない | 両 limit を registry 登録し 3 層解決経由に載せる。**値そのものは現行維持**（非対称の理由 = RSS のイベント密度が高い、を注記）。§6 処方 ⑤ に合流 | 低。registry 登録は静的定義の追加のみ。**値を変えないためパリティに差分が出ない** |
| **CAL-A15** | 較正系の閾値に DB override が 1 つも実装されていない（docstring は「DB-stored config」と主張） | **SIMPLIFY** | **G-15 の同型** + **DP1**（記述ドリフトが systemic）。**NP6**: 較正という「結論に影響する判断」の閾値が registry に不在で、**どの値が効くか開示できない**<br>**推奨**: registry には**載せる**が、較正ガード系キーは `immutable` フラグで DB override を禁止し **env + default の 2 層**に留める（到達性は得るが、ガードをアナリストが緩める経路は作らない） | 較正閾値の所在が**コード散在 → registry 1 箇所**。E-18（記述ドリフト）の発生源が 1 つ消え、docstring は仕様書参照のみになる | `immutable` にしないと**較正ガードを実行時に緩める経路**が新設される（3 インシデントの再発面）。緩和: impact=high + 理由必須 + 監査（S1-CONF-003 の既存機構をそのまま使う） |

---

## 4. サービス系 — S1-services.md §4（8 件）

| ID | 挙動（平易に） | 分類 | 根拠（原則 / P4 ステップ責務） | 単純化の効果 | リスク |
|---|---|---|---|---|---|
| **SVC-A1** | ログインレート制限がプロセス内メモリのみ（再起動で全解除、複数プロセスで実効上限が台数倍）。IP 表の退避が「最終試行が最も古い IP」なので、攻撃者は多数 IP を投げて正規利用者の記録を押し出せる | **OWNER** | P4 のステップ責務外（前向きの流れに属さないサービス層）。判断に**運用形態（gunicorn worker 数・将来の複数ノード）**の情報が要り、それはオーナーしか持たない<br>**推奨**: **単一プロセス運用を仕様として固定**し永続化しない（表と retention を増やさない = 複雑化を招かない）。退避規則のみ「最終試行が最も古い」→「**試行回数が最も少ない**」に変更すれば押し出し攻撃は塞がる | 永続化しない選択なら**新規テーブル 0・retention 方針 0**。退避規則の変更は 1 関数 | 単一プロセス前提を破る運用（水平スケール）に移ると**実効上限が黙って台数倍**になる。前提を仕様に書き、S10 の検査項目に「プロセス数 == 1」を入れることで沈黙を防ぐ |
| **SVC-A2** | ログアウトが access token の JTI のみ失効させ、refresh token の JTI は台帳に載らない（cookie 消去のみ） | **SIMPLIFY** | **DP7**「無効性判定は**単一述語を共有** MUST」と同じ処方。現状は失効機構が 2 種（台帳照会 + cookie 消去）あり、片方は**サーバ側に強制力が無い** | 失効判定が **2 経路 → 1 経路**。「失効しているか」の問いに答える場所が 1 箇所になる | refresh は長寿命のため台帳行数が増える。**G-11（移行 35 表のうち 17 表に retention が無い）の轍を踏まないよう、保持方針を同時に定義**する |
| **SVC-A3** | ユーザー削除で設定行の削除と本体の削除が単一トランザクションに入っていない（設定削除だけが書き込み文脈の外） | **SIMPLIFY** | **E-14 / DP16**（scenario_apply の部分失敗）と**完全に同型**で、v3 規範は既に「トランザクション境界で不可分 MUST」と確定済み。個別裁定ではなく既存規範の適用対象 | 「設定行だけ消えた孤児状態」という**状態空間が 1 つ消える**。§6 処方 ④ に合流 | ほぼ無し。書き込み文脈の統合のみ |
| **SVC-A4** | JWT 署名鍵が不在時に**設定ファイルへ自動追記される**（アプリがアプリの設定を書き換える） | **OWNER** | **S1-CONF-007（起動時に env を DB override へ複写）/ DP17 と同じ病理**「アプリが自分の設定を書き換え、実効層が追跡不能になる」。一方で CLAUDE.md 運用落とし穴 1 が**この自動生成を前提に運用手順（config.env バックアップ）を組んでいる**ため、変更は運用に波及する<br>**推奨**: 起動失敗にはせず、生成鍵を **config.env でなく専用 secret ファイル / DB bootstrap へ書き、config.env は読み取り専用**にする。「設定は外部から与える」原則を満たしつつセットアップの手間を増やさない | 設定ファイルへの書き込み経路が消え、**config.env が単方向（外→内）**になる。DP17 と同じ 1 本の処方で両方が閉じる | 起動失敗方式にすると**新規セットアップと復旧の手間が増える**（単一運用者ツールでは実害が大きい）。推奨案なら手間は不変だが、バックアップ対象ファイルが変わるため**運用手順書の更新が必須** |
| **SVC-A5** | drift 未確認通知の debounce キーに**件数と最古 ID**が入るため、件数が 1 増えるだけで debounce を貫通して再送される | **SIMPLIFY** | 他 4 事象のキーは「事象種別 + 対象識別子」で、**drift だけが非対称**（コピペドリフト型）。**NP1 上は再送が安全側**に見えるが、通知圧の上昇はアナリストの通知無視を招き**実効 recall を下げる**（NP7: 組織内ノードとしての価値は読まれることに依存） | alert_key 構成規則が **5 通り → 1 規則**（`{事象種別}:{対象}`）。条項 019 の表が 1 行で書ける | 件数の悪化（10 → 50 件）が通知されなくなる。緩和: **重大度が上がったときのみ貫通**する規則を 1 本だけ持つ（S1-SVC-018 の severity 軸を再利用し、新しい軸を作らない） |
| **SVC-A6** | 提案却下率が母数 10 件未満のとき 0.0 を返す（「観測不能」ではなく「良好」として振る舞う） | **DROP** | **NP5+8**（結論不可の明示）+ **DP23**「全経路で『測れない = None』に統一 MUST」。**同一ファイル内で自己矛盾**している: S1-SVC-024 は「失敗した収集器は 0 を書かず未設定のまま残す（観測できなかったと 0 だったを混同しない）」と規定しているのに、母数不足だけ 0.0 を書く | 0.0 フォールバックを捨てて None にすると、**ATTENTION の「観測不能」分岐が 1 種類**に揃う。§6 処方 ① に合流 | None を受ける UI 側の表示分岐が要る（小）。**AP3 の信頼性メトリクスとしてはむしろ改善**（「まだ測れていない」が可視になる） |
| **SVC-A7** | ATTENTION 一覧要求 1 回につきメトリクス収集が 2 回走る（5 秒ポーリング × 2 倍の DB 負荷、**ヒステリシス入替も 2 回**） | **DROP** | これは性能問題ではなく**正しさの問題**: ヒステリシスの進入/離脱カウンタが要求ごとに 2 段進むため、**発火判定がポーリング頻度に依存**する（P4 §5 判定基準 3「入力を与えれば出力が決まる」に反する）。S1-SVC-028 の v3 規範「1 要求につき収集 1 回 MUST / 派生ビューは同一評価結果から導出 MUST」で既に確定 | 二重収集を捨てるだけ。**純粋な削減**で、条項 023（ヒステリシス）の意味が要求頻度から独立する | 無し。将来 UI を増やしても収集回数が増えない構造になる |
| **SVC-A8** | 自動確認ゲートの定義（信頼度床 0.75 / 許可 ecosystem 3 種）が **triage 純関数側とインテルキュー本体で二重管理** | **SIMPLIFY** | **P4 §3 の中核病理そのもの**（S5 収斂の式重複 / S6 の TL 導出 4 複製と同型）。**DP6**「同一式の二重実装を禁止 MUST」。**ANLYT-A16（自動確定閾値 0.80 の DB 層ハードコード）と合わせると同一ゲートが 3 箇所**にある | ゲート定義が **3 箇所 → 1 registry キー + 1 実装**。フロント純関数はサーバ算出結果を受け取る（DP6 の attention_score と同じ処方）。**SVC-A8 と ANLYT-A16 が 1 本の修正で同時に閉じる** | `triage_score.js` の BEHAVIOR テスト 21 件（D5）が**サーバ側へ移植**になる。テスト資産は失われないが移植先の変更が要る |

---

## 5. 分析・設定系 — S1-analytics-config.md §7（A9-A20、12 件）

> A1〜A8 は climate 凍結（§2）。

| ID | 挙動（平易に） | 分類 | 根拠（原則 / P4 ステップ責務） | 単純化の効果 | リスク |
|---|---|---|---|---|---|
| **ANLYT-A9** | RSS の単一 country 検出が**許可リストの並び順に依存**する（同一記事でも呼び出し側の並びで結果が変わる） | **SIMPLIFY** | **P4 S2 正規化**「事実だけを記録する」+ **P4 §5 判定基準 3**「入力を与えれば出力が決まる」。呼び出し文脈で結果が変わる = **非決定的**であり、AP4 の再生（P4 S11）が原理的に成立しない | 単一検出版を廃し**多国版 1 実装**へ。単一が要る場所は「**本文中の出現位置が最も早い**」で決定的に選ぶ。検出関数 2 種 → 1 種 | 単一検出の下流（bg observer の findings 帰属）が複数国を受け取れるか要確認。**受け取れないなら選択規則だけ決定的にする**（実装は 1 種のまま） |
| **ANLYT-A10** | スコア内訳が cyber / physical / info **以外の domain を持つエントリをカウンタも残さず黙って捨てる**（LLM・補助ドメインの寄与が UI から消える） | **DROP** | **NP6（完全な導出開示）に真っ向から反する**。**P4 S5**「寄与内訳をこの時点で残す。**後から復元できない**」+ **P4 S7**「新しい判断をしない。既にある根拠を提示するだけ」— 提示層が根拠を間引いている | 黙って捨てる挙動を捨て、**捨てるなら件数と理由を残す**。「内訳の合計と総スコアが一致する」が検査可能になり、**P4 S10 の検査項目に昇格できる**（沈黙失敗の検出面が 1 つ増える） | 3 ドメイン外の domain 値が実際に何かを先に確定する必要（LLM / 補助）。**確定作業は 1 クエリ**（rationale の domain distinct） |
| **ANLYT-A11** | what-if の加点入力に上下限検査が無く、負値・巨大値・非数値を受け付ける（非数値は 500） | **SIMPLIFY** | **P4 S5**「純関数・冪等」+ **DP12**（反実仮想が confidence と signal_source を落とし本番より構造的に高く出る）。**反実仮想は本番と同一入力型であるべき**という 1 つの規範で両方が閉じる | what-if 専用の入力検査を**書かない**。本番の Indicator と同じ型・同じ値域で受ける。**DP12 と同時に解消**され、「what-if の結果は本番と比較できるか」の問いが消える | 極端値による探索ができなくなる。**上限は既にカタログ上限で切り詰めており実質変化なし**（下限のみ新設） |
| **ANLYT-A12** | SPOF の「代替手段あり」欄が**常に偽の固定値**（未実装のまま UI に出ている） | **DROP** | **D2 E-20（死んだメトリクス: 収集されるが判定に使われない）と同型**。本件はさらに悪く、**収集すらせず UI に出ている**。DP4「未使用状態を持たない MUST」 | API レスポンス 1 フィールドと UI 表示 1 箇所が消える。**アナリストが偽の情報を読まなくなる**（NP7: 出力は判断に使われる前提） | 無し。**将来必要になったら仕様を書いてから実装する**（現状は仕様も無い） |
| **ANLYT-A13** | SPOF はセンサーのエラー文字列を無加工で返す（データ状態 endpoint は 1 行目 100 文字に切詰）— 情報漏洩面が非対称 | **SIMPLIFY** | **DP14**「型付きエラー MUST」と同じ処方。エラー文字列には URL・認証情報が混ざりうる。CLAUDE.md「詳細なエラー文脈はサーバ側にログ」原則 | エラーの外部提示が **2 種 → 1 整形関数**。漏洩面が 1 箇所に集約され**監査可能**になる | 診断時に原文が見えなくなる。**原文はサーバログに残す**（提示と記録を分ける = P4 S7 と S11 の分離に一致） |
| **ANLYT-A14** | 適応 Z の Welford 分散が `n == 1` で 0.0 を返す（未定義ではなく） | **SIMPLIFY** | **NP5+8**「測れないことを明示する」+ **DP23**「全経路で『測れない = None』に統一 MUST」。「サンプル 1 件で分散 0」は統計的に誤りであり、下流が std_floor に救われるかは経路依存 | 統計量の「不在」表現が **0.0 と None の 2 種 → None 1 種**。§6 処方 ① に合流 | warmup 行の API 形状が `0.0 → null` に変わる（S2 契約の変更 1 件）。**adaptive 判定は sample_count ≥ 50 なので発火挙動は変わらない** |
| **ANLYT-A15** | calibration advisory の空リストが「健全」と「対象シナリオ無し」を区別しない | **DROP** | **G-17 / DP10 / DP11 / CAL-A4 と完全に同型**。**P4 S6**「データ欠測と真の平穏を区別できることが要件」。**NP5+8** | 空リスト = 健全という含意を捨て、結論不可の単一表現へ。**§6 処方 ① に合流し個別裁定が不要になる**（DP11 の「5 通りに分裂」の 1 通りが減る） | 低。UI 側で「対象なし」と「異常なし」の描き分けが要る（処方 ① で一括対応） |
| **ANLYT-A16** | 自動確定閾値 0.80 が DB 層にハードコードされ intel キューの実閾値と独立に動く。信頼度分布で空バケットも出力されない | **SIMPLIFY** | **SVC-A8 と同一ゲートの 3 箇所目**。**DP6**「同一式の二重実装を禁止 MUST」+ **G-15**（閾値の到達性）。乖離検知の仕組みが無い = **沈黙する drift**（P4 S10 の検査対象） | **SVC-A8 と 1 本の修正で同時に閉じる**（定義 3 箇所 → 1）。空バケットは固定 10 バケットを常に出し、**分布の形が入力に依存しない**（描画側の補完ロジックも消える） | 低。閾値を registry 化しても現行値を維持すればパリティ差分は出ない |
| **ANLYT-A17** | registry 登録全体が 1 つの例外捕捉に包まれ、**失敗時は部分 registry で起動する** | **DROP** | **ADR-V3-006（fail-closed 既定）に該当**。部分 registry = 一部キーが「未登録」扱いになり **env 生文字列経路（S1-CONF-002）に落ちる = 型変換も既定値も適用されない**。これは**設定面の沈黙失敗**そのもので、P4 S10「設定到達性」の検査対象 | 部分起動を捨て、**「registry が完全である」を起動時不変条件**にする。下流の「未登録なら env 生値」という特例分岐が消せる（S1-CONF-002 の MUST が 1 つ減る） | 起動失敗が増える。**registry は静的定義なので CI で事前検証可能**（起動前に落ちる = §6 処方 ⑦ の CI ゲートに含める） |
| **ANLYT-A18** | 読み出し経路に範囲検査が無く、範囲外の環境変数・registry 変更前に書かれた古い DB 行がそのまま通る | **SIMPLIFY** | **D2 G-15 に帰着**（到達性が無いこと自体が問題）。書き込み時のみ検査 = **書き込み以外の全経路が無防備**。**NP6**: 実際に効いている値が registry の宣言と一致する保証が無い | 解決時に registry 値域で検査し、**逸脱は拒否 → 既定値 + AP3 露出**。「書き込み時だけ検査」の非対称が消え、**DP18（孤児 override 行）も同じ検査で検出**できる | 既存の範囲外 DB 行が黙って既定値に戻る。緩和: **移行時に逸脱行の一覧を出しアナリストに提示**（S3 移行手順に 1 ステップ追加） |
| **ANLYT-A19** | キャッシュ一括 flush の外部経路が撤去済で **docstring だけが残っている** | **DROP** | **E-18（記述ドリフト群: docstring を仕様の一次ソースとして信用できない証拠）**。DP1「仕様書が正本。docstring は仕様書を参照するのみ MUST」 | docstring を捨てる。**30 秒 TTL で足りる**（世代付きキャッシュは登録時に全体無効化される = 条項 004）。E-18 の実例が 1 件減る | 無し |
| **ANLYT-A20** | 型別取得関数が無く呼び出し側が毎回キャストする（registry の型宣言が実行時に効いていない） | **SIMPLIFY** | **G-15 に帰着** + **P1 §12「直読み禁止を型で強制」の直接の適用対象**。registry が 21 属性で型を宣言しているのに `Any` 返しの単一関数しか無い = **宣言が装飾**（G-15 の縮図） | 型別アクセサ（`get_int` / `get_float` / `get_bool` / `get_list`）を registry の型宣言から導出し、`Any` 返しを非公開化。**呼び出し側のキャストが全消滅**し、型不一致が呼び出し地点ではなく registry 側で 1 度だけ検出される | 呼び出し箇所の一括置換が要る。**影響範囲は G-15 の「迂回②（import 時定数 38 キー）」と同一で、§10-4 が「P 段階で可否判断」と留保している未設計項目に依存**する |

---

## 6. 横断処方 — 35 件が 7 本に畳める

個別に 35 通りの実装をするのではなく、下記 7 本を入れると**24 件が同時に閉じる**。
これが「追加に追加を重ねた複雑化の解消」に対する本領域からの回答である。

| # | 処方 | 閉じる項目 | 件数 |
|---|---|---|---|
| **①** | **「測れない / 対象なし」を単一の結論不可型に統一する**（NP5+8 / P4 S6。DP10 / DP11 / DP23 / G-17 と同一） | CAL-A4, SVC-A6, ANLYT-A14, ANLYT-A15 | 4 |
| **②** | **同一の式・閾値の重複定義を単一 registry キー + 単一実装へ**（DP6 / P4 §3 の中核病理） | CAL-A8, SVC-A8, ANLYT-A16 | 3 |
| **③** | **fail-open を fail-closed へ（ADR-V3-006 の機械的適用。個別再裁定は不要）** | CAL-A6, CAL-A11, CAL-A12, ANLYT-A17 | 4 |
| **④** | **書き込み文脈の統合（トランザクション境界で不可分。E-14 / DP16 の規範適用）** | SVC-A2, SVC-A3 | 2 |
| **⑤** | **結論・自動判断に影響する全閾値を registry 経由に載せる（G-15 の一般解。値は変えない）** | CAL-A14, CAL-A15, ANLYT-A18, ANLYT-A20 | 4 |
| **⑥** | **較正の窓・基準値・クランプを単一関数から解決する**（P4 S9 + DP19 / DP22 / DP23） | CAL-A1, CAL-A3, CAL-A13 | 3 |
| **⑦** | **決定性・非重複評価の保証**（P4 §5 判定基準 3。同じ入力なら同じ出力） | SVC-A5, SVC-A7, ANLYT-A9, ANLYT-A11 | 4 |
| — | 上記に合流しない個別項目 | CAL-A2(KEEP), CAL-A5, CAL-A7(KEEP), CAL-A9, CAL-A10(OWNER), SVC-A1(OWNER), SVC-A4(OWNER), ANLYT-A10, ANLYT-A12, ANLYT-A13, ANLYT-A19 | 11 |

合流 24 件 + 個別 11 件 = **35 件**（climate 凍結 8 件を除く全数）。
処方 ③ は ADR-V3-006 の機械的適用にすぎず**新規の設計判断を含まない**ため、実質の設計論点は ①②④⑤⑥⑦ の 6 本。

---

## 7. オーナーに上げる 3 件（推奨つき）

いずれも「技術的にどちらも成立し、判断材料が運用側にある」もの。逃げではなく、**推奨を明記した上で**上げる。

| ID | 問い | 推奨 | 推奨しない場合の帰結 |
|---|---|---|---|
| **CAL-A10** | tier（auto-apply 権限段位）の降格条件に **recall 劣化**を加えるか | **加える**（前提: DP18 の recall ゲート修正）。NP1 上、自動適用が recall を下げても止まらない現状は最大の穴 | 加えない = 劣化が加速しうる。加える = 測定系のバグ（**D2 D-01「構造では直らない」**）が tier を誤って落とす経路が新設される。**この 2 リスクの比較がオーナー判断** |
| **SVC-A1** | ログインレート制限を**永続化するか、単一プロセス前提を仕様に固定するか** | **単一プロセス前提を仕様化**し永続化しない（表と retention を増やさない）。押し出し攻撃対策として退避規則のみ「試行回数最小」へ変更 | 永続化 = 新規表 + retention 方針が増える（複雑化）。仕様固定 = 将来の水平スケールで**実効上限が黙って台数倍**になる（S10 の検査項目でプロセス数を監視すれば沈黙は防げる） |
| **SVC-A4** | JWT 署名鍵の**自動生成 + config.env 追記**を続けるか | **生成は続けるが書き込み先を専用 secret ファイル / DB bootstrap に変える**（config.env は読み取り専用に）。DP17 と 1 本の処方で閉じる | 起動失敗方式 = 新規セットアップと復旧の手間が増える（単一運用者ツールでは実害大）。推奨案 = **CLAUDE.md 運用落とし穴 1 のバックアップ手順書の更新が必須** |

---

## 8. 本書の限界

1. **CAL-A11 の `impact_override` 実運用実績が未確認**。台帳の `applied_by` / `impact` 実績を 1 クエリで確認してから削除すること（未使用なら純粋な削除、使用実績があれば代替手段の設計が要る）。
2. **ANLYT-A10 の「3 ドメイン外の domain 値」の実体が未確認**。rationale の domain distinct を 1 回取れば確定する。
3. **ANLYT-A20 の一括置換の影響範囲は D1 でも未評価**（`S1-analytics-config.md §10-4` が「P 段階で可否判断」と留保）。本書は方向のみを示し、手順は P に委ねる。
4. **climate 凍結 8 件は再実装時に本書の物差しで再走査が必要**。凍結解除は「価値の実証 + 仕様の書き直し」が前提（ADR-V3-008）。
5. 本書は**一次分類**であり、DROP / SIMPLIFY の実装順序・パリティ差分の事前登録（ADR-V3-006）は P2 / P3 の作業パッケージ側で決める。
