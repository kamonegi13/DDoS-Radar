# v3 Rebuild — 段階的全面リビルド計画（単一情報源）

**Status**: Phase D（診断）進行中 — 開始 2026-08-03
**先行文書**: [v2-migration.md](v2-migration.md)（v2 原則移行、完了済）
**成果物置き場**: `docs/design/v3/`（本ファイルは索引・状態・ADR のみ。中身は小ファイル分割 — ADR-V3-002）

---

## 0. 経緯と決定

2026-08-03、オーナーから「機能の逐次追加によりツール全体が有機的に連結した最適形になっていない」
との問題提起と、完全スクラップ＆ビルド（蓄積データ破棄も許容）の提案があった。

評価の結果、**完全新規構築（データ破棄込み）は棄却**し、
**データ・テスト・較正実績を連続させた段階的全面リビルド**を採用（ADR-V3-001）。
最終状態は「コードの大半が新しく、境界が原則から再設計され、較正と検知実績が途切れていない系」。

棄却理由（前方視のみ。サンクコストは根拠に含めない）:
1. ツールの価値はコードではなく**較正済み稼働系**（ラベル・ベースライン・ledger・実戦検証済み挙動）にある
2. このドメインの回帰は沈黙する（calibration インシデント #1〜#3 は全テスト通過のまま数週間 prod を劣化させた）。
   検証は 30 日窓のラベル蓄積に律速され、コスト度外視でも暦時間は買えない
3. データ破棄はツール定義が「設計失敗」と定める恒常的結論不可（null-zone）状態を自ら選ぶ行為。
   DB 持ち越しはほぼ無料、再構築だけが高い
4. 旧コードを仕様書代わりにした転写は同型の系に収束し、原則だけから書くと細部挙動
   （dedup キー、ゲート条件）が失われる。ゆえに**仕様の完全下ろし（Phase S）が前提条件**

## 1. プログラム原則

| # | 原則 |
|---|------|
| R1 | **データ連続**: ground-truth ラベル・AP3 anchor ラベル・ベースライン・conclusions ledger は移行する。破棄しない |
| R2 | **仕様先行**: 新コードは旧コードからではなく、Phase S の挙動仕様書から書く |
| R3 | **パリティゲート**: cutover 条件は数値で先に決める（replay 30 日窓で recall ≥ 旧系）。「綺麗だから」で切り替えない |
| R4 | **旧系を止めない**: shadow 並走でパリティが取れるまで旧系が正（ADR-V2-003 プレイブック再利用） |
| R5 | **網羅性の証明**: 各フェーズは completeness oracle（全数分類・トレーサビリティ表）で「完全」を機械的に検証する |
| R6 | **P は S の後**: 計画詳細化は仕様カバレッジ表がクローズするまで着手しない |
| R7 | **構造の白紙設計**: アーキテクチャは現行コードの構造からではなく、ツール定義（4 文）+ NP1-NP7 / AP1-AP4 から導出する。現行から継承するのは**挙動仕様と知識**（検知セマンティクス・較正資産・外部 API の癖）であって**構造ではない**。D1 の分解表・サブシステムマップは証拠資料であり、新設計の境界を拘束しない（ADR-V3-003） |

## 2. フェーズ定義とゲート

### Phase D — 診断（進行中、推定 3〜5 セッション）

| # | 成果物 | ファイル | 網羅性の証明 | 状態 |
|---|--------|----------|--------------|------|
| D1 | モジュールインベントリ + 依存グラフ + レイヤー違反 | `v3/D1-inventory.md`（統合版）+ 領域別 3 冊 | 全ソースファイル分類済（**残: scripts/ 27 件**） | **統合済・レビュー可**（残作業は D1-inventory §5） |
| D2 | 欠陥リスト（重大度・証拠・影響原則・「書き直しで直るか」欄つき） | `v3/D2-defects.md` | 全欠陥に証拠必須（インシデント記録 / 共変更実測 / コード引用） | 素材収集済（共変更実測 + 3 領域の欠陥候補 25+ 件）、編纂待ち |
| D3 | keep / port / rewrite 判定表（モジュール粒度、根拠つき） | `v3/D3-verdicts.md` | D1 全モジュールに判定あり | 未着手（D1 各冊に初期判定の種あり） |
| D4 | データ資産台帳（全テーブル: 移行 / 破棄可 / 再生成可） | `v3/D4-data-assets.md` | 全テーブル分類済 | 素材収集済（79 テーブル実測 + 空テーブル 13 件特定）、編纂待ち |
| D5 | テスト資産分類（挙動テスト=移植可能な仕様 vs 構造依存） | `v3/D5-test-assets.md` | 全テストファイル分類済 | 未着手（Python 101 + JS 6 ファイル） |
| D6 | API 契約インベントリ（REST + WS 全エンドポイント） | `v3/D6-api-surface.md` | 全 route 定義列挙 | 実測完了（REST 167 / WS on4+emit6、D1-backend-core §4 に全列挙）。フロント参照 78 との差分 89 の用途切り分けが残 |

**出口ゲート**: D2 欠陥リストと D3 判定表をオーナーがレビューし承認。

### Phase S — 仕様の完全下ろし（推定 8〜12 セッション）

- S1: サブシステム別挙動仕様（sensors / scoring / intel pipeline / calibration 機構 / conclusions / AP1-AP4 / auth・通知）
- S2: API 契約書（フロントエンド greenfield の独立作業を可能にする境界）
- S3: データスキーマ仕様 + 新旧移行マップ
- S4: 非機能仕様（NP3 耐障害、セキュリティ、i18n 契約、パネルクローム契約 — 既存文書を編入）
- S5: 検証仕様（replay パリティゲート、recall baseline 継承）

規律:
- 各仕様条項は該当コード行を引用（NP6 の流儀を仕様書自体に適用）
- 偶然の挙動は `[ACCIDENTAL]` タグで明示し、保存/破棄をオーナーが判定
- **completeness oracle**: D5 の全挙動テストを仕様条項に対応付けるトレーサビリティ表。対応先のないテスト = 仕様の穴

**出口ゲート**: カバレッジ表クローズ（D1 全モジュールが「仕様化済み」または「理由付き除外」）。

### Phase P — 計画詳細化（推定 2 セッション）

- strangler 切断順序、フロントエンド greenfield 計画、shadow 並走 + パリティゲート数値、旧系 sunset 条件
- セッション 1 回で完結する作業パッケージへ分解（各パッケージに自己完結 context brief）

**出口ゲート**: 作業パッケージ一覧 + cutover 数値条件をオーナーが承認。

## 3. ADR-V3 台帳

| ID | 決定 | 日付 |
|----|------|------|
| ADR-V3-001 | 完全スクラップ＆ビルド（データ破棄込み）を棄却。データ・テスト連続の段階的全面リビルドを採用 | 2026-08-03 |
| ADR-V3-002 | 成果物は `docs/design/v3/` に小ファイル分割。単一巨大ファイル化を禁止（v2-migration.md 109KB の教訓） | 2026-08-03 |
| ADR-V3-003 | **目的駆動設計**（オーナー指示）: 新アーキテクチャは現行構造にとらわれず、目的に沿った本質的ロジックの整理統合・精緻化により全体整合を図って導出する。Phase S が保存するのは挙動、Phase P が導出するのは構造。両者を混同しない | 2026-08-03 |

## 4. 作業ジャーナル

- **2026-08-03**: プログラム開始。Phase D 着手 — D1 並列調査（backend-core / sensors / frontend）起動、
  親側で D4 素材（DB テーブル台帳）・D2 素材（git 共変更分析）収集。
  subagent 並列調査はオーナー許可済み。
- **2026-08-03（同日）**: D1 統合完了。`v3/D1-inventory.md`（横断所見）+ 領域別 3 冊
  （backend-core / sensors / frontend）。横断病理の筆頭は**コピペドリフト**（3 領域で独立検出）、
  最深所見は **GET 副作用スコアリング**（get_threat_data 2,665 行）。D6 の endpoint 実測完了
  （REST 167 / フロント参照 78）。残: scripts/ 27 件分類、D2/D3/D5 編纂。
  診断中に現役バグ候補 3 件発見（showToast 未定義 / bg_observer CB 不活性 / ground_osint STALE 無視）
  — 修正は Phase D 完了後（診断中の差分汚染を避ける）。
- **2026-08-03（同日）**: PR #9（calibration 修正 + 日本語専用 UI + Noroshi rename の 12 コミット）を
  main へマージ。`v3/phase-d` ブランチ新設、v3 文書群を初コミット。ADR-V3-003（目的駆動設計）を記録。
- **2026-08-03（同日）**: オーナー方針 — **実装フェーズは Opus セッションで実施**する。
  Phase P の作業パッケージは、前会話の文脈を持たない新規セッションが cold start で実行できる
  自己完結 context brief を必須とする（従来方針の再確認 + 実装者を跨ぐ前提の明文化）。
