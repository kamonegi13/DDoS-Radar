flask>=3.0
flask-cors>=4.0
flask-jwt-extended>=4.6
flask-limiter>=3.5
flask-socketio>=5.3
argon2-cffi>=23.0
requests>=2.31
python-dotenv>=1.0
gunicorn>=21.0
gevent>=24.0
gevent-websocket>=0.10
websocket-client>=1.7
h3>=3.7
defusedxml>=0.7
# Stage-2 tolerant XML recovery in v3/fetch/parse.py. Was previously
# present only as a transitive dep of pytrends: a production image
# without it lost ALL feed recovery silently, because the import sat
# inside a try/except that reported the result as UNPARSEABLE.
lxml>=5.0
pytrends>=4.9
