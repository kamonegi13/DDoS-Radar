config.env
# Docker Compose reads this for ${VAR} substitution. The v3 shadow's key
# material lives here (NOROSHI_V3_JWT_SECRET / _BOOTSTRAP_PASSWORD) —
# ignored BEFORE the file is created, so it can never appear in a diff.
.env
.claude/
CLAUDE.md
.DS_Store
persistence/
__pycache__/
radar/llm_client.py.bak

# Local venvs + operational backups (not for VCS)
.venv/
.venv-arm/
config.env.bak-*
backups/
